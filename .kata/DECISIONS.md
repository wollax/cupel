# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | TraceCollector ownership model | Per-invocation (passed at call time, not stored on pipeline) | Avoids thread-safety concerns; each run gets isolated diagnostic context; prevents concurrent runs from interfering | No |
| D002 | M001 | api | run_traced vs unified run method | `run_traced()` coexists with `run()` — additive, not replacing | Avoids breaking changes for existing callers; keeps the non-diagnostic path zero-cost | No |
| D003 | M001 | api | NullTraceCollector type | Zero-sized type (ZST) that monomorphizes to no-ops | Rust monomorphization eliminates dead code; ZST has zero size, zero runtime cost | No |
| D004 | M001 | arch | ExclusionReason variants | Data-carrying enum variants (not fieldless) | Enables programmatic inspection without parsing message strings; see spec rationale in `spec/src/diagnostics/exclusion-reasons.md` | No |
| D005 | M001 | arch | Reserved ExclusionReason variants | Must be defined even if never emitted by built-in stages | Forward-compat: reserved variants allocate type-system space for future spec versions without breaking changes | No |
| D006 | M001 | api | KnapsackSlice DP guard threshold | 50 million cells (capacity × items) | Matches requirement RAPI-06; prevents OOM without affecting typical workloads (<500 items at <4096 tokens) | Yes — if workloads grow |
| D007 | M001 | convention | Diagnostics conformance vector authoring | Author vectors in `spec/conformance/` first; drift guard syncs to `crates/cupel/conformance/` | Spec-first is the established pattern; drift guard ensures implementations can't diverge silently | No |
| D008 | M001 | scope | QH issue batch scope | ~15-20 high-signal .NET issues and ~10-15 Rust issues per quality slice | Exhaustive triage not worth the overhead; high-signal = correctness, panic paths, misleading behavior, test gaps. Cosmetic/low-impact issues stay open. | Yes — scope per slice |
| D009 | prior | arch | Fixed pipeline over middleware | Fixed 5-stage pipeline (Classify → Score → Deduplicate → Slice → Place) | Silent-drop failure mode of call-next middleware is worse than predictable fixed-stage behavior | No |
| D010 | prior | arch | Ordinal-only scoring invariant | Scorers rank but never eliminate | KnapsackSlice is provably correct only on complete candidate sets | No |
| D011 | prior | arch | Per-invocation tokenizer | Token counting is caller's responsibility | Keeps pipeline dependency-free; works with any tokenizer | No |
| D012 | prior | arch | Crate name `cupel` | Standalone name on crates.io | Shorter, cleaner; name was available | No |
| D013 | prior | arch | Standalone Cargo.toml | No workspace | Single-crate repo avoids workspace complexity | No |
| D014 | prior | arch | Validation-on-deserialize for ContextBudget | Custom deserializer routes through constructor | Prevents serde from bypassing constructor invariants | No |
| D015 | prior | arch | #[non_exhaustive] on CupelError and OverflowStrategy | Additive enum evolution without breaking downstream | Standard Rust pattern for library-owned enums | No |
| D016 | S01 planning | arch | S01 verification strategy | Contract-level only (compile + cargo test + doc + clippy + drift guard) | Diagnostic types have no runtime behavior to test; harness coverage of `expected.diagnostics.*` is a S03 concern; TOML vector correctness is manually verified against diagnostics-budget-exceeded.toml schema | No |
| D017 | S01 planning | convention | ExclusionReason serde deferred to S04 | S01 stubs `cfg_attr` annotations with `// custom serde impl in S04` comment | Adjacent-tagged wire format cannot be derived; implementing it in S01 would violate task scope and risk S02 rework if field shapes change | No |
| D018 | S02 planning | convention | DiagnosticTraceCollector callback exception policy | Panics unwind normally — no `catch_unwind` wrapper | Rust convention; `catch_unwind` requires `UnwindSafe` bounds that complicate the API; a panicking observer is the caller's responsibility; differs from .NET which silences callback exceptions | No |
| D019 | S02 planning | impl | DiagnosticTraceCollector excluded storage | `Vec<(ExcludedItem, usize)>` with insertion index; index stripped in `into_report()` | Stable score-desc sort requires insertion-order tiebreak; storing the index alongside the item is the only way to recover insertion order after sorting | No |
| D020 | S02 planning | api | TraceCollector trait extension methods | Three defaulted no-op methods (`record_included`, `record_excluded`, `set_candidates`) added to the trait | Enables S03's `run_traced<C: TraceCollector>` to call item recording generically; `NullTraceCollector` monomorphizes to no-ops; avoids two-path design in S03; adding defaulted methods is non-breaking for existing implementors | No |
| D021 | S02/T02 | impl | TraceEventCallback type alias | `type TraceEventCallback = Option<Box<dyn Fn(&TraceEvent)>>` introduced in `trace_collector.rs` | Satisfies `clippy::type_complexity`; named alias is more readable at constructor call sites; no behavioral change | No |
| D022 | S03 planning | arch | Stage function return type extensions | `classify` returns `(pinned, scoreable, neg_items)`; `deduplicate` returns `(survivors, excluded)`; `place_items` returns `(placed, truncated_with_scores)` | Gives `run_traced` direct access to excluded items without external diffing; avoids re-implementing stage logic; internal (`pub(crate)`) so no API break | No |
| D023 | S03 planning | impl | PinnedOverride detection rule at Slice stage | Classify Slice-stage exclusion as `PinnedOverride` when `pinned_tokens > 0 && item.tokens() > effective_target && item.tokens() <= budget.target_tokens() - budget.output_reserve()` | Distinguishes items displaced by pinned budget consumption from genuinely over-budget items; matches `diag-pinned-override.toml` vector; `displaced_by = pinned.first().content()` | Yes — if spec evolves |
| D024 | S03 planning | api | dry_run discards Vec<ContextItem> | `dry_run` returns `Result<SelectionReport, CupelError>` and discards the `Vec<ContextItem>` from `run_traced` | Callers who need both outputs call `run_traced` directly; `dry_run` is the convenience API for the common "explain why" use case without caring about the actual selection | No |
| D025 | S03 planning | impl | BudgetExceeded available_tokens at Slice stage | `available_tokens = effective_target - sum(sliced_item.tokens())` | Represents the remaining budget after all selected items; consistent with greedy/knapsack behavior; verified correct for `diagnostics-budget-exceeded.toml` where available=200-150=50 | No |
| D026 | S03/T02 | impl | ClassifyResult and PlaceResult type aliases | `type ClassifyResult = Result<(Vec<ContextItem>, Vec<ContextItem>, Vec<ContextItem>), CupelError>` and `type PlaceResult = Result<(Vec<ContextItem>, Vec<(ContextItem, f64)>), CupelError>` | Satisfies `clippy::type_complexity` after stage function return types were extended in T01; named aliases are more readable at call sites; matches D021 pattern (TraceEventCallback); no behavioral change | No |
| D027 | S04 planning | convention | ExclusionReason/InclusionReason serde — internally-tagged, not adjacent-tagged | `#[serde(tag = "reason")]` on both enums; `_Unknown` unit variant with `#[serde(other)]` on `ExclusionReason`; no custom Serialize/Deserialize impl needed | D017's "adjacent-tagged" comment was imprecise; the spec uses `{ "reason": "...", ...fields }` which is serde's _internal_ tagging, not adjacent. All `ExclusionReason` variants are struct or unit variants — exactly what `#[serde(tag = "...")]` requires to work. `_Unknown` with `#[serde(other)]` provides graceful handling of future spec variants without panicking. | No |
| D028 | S04/T02 | pattern | SelectionReport custom Deserialize via RawSelectionReport | `RawSelectionReport` with `#[serde(deny_unknown_fields)]`, deserialize into raw, then validate `total_candidates == included.len() + excluded.len()` | Mirrors `ContextBudget` Raw pattern (D014); `deny_unknown_fields` prevents silent forward-compat surprises; validation ensures the derived field is always consistent; error message format: `"total_candidates N does not equal included.len() M + excluded.len() K"` | No |
| D029 | S04/T02 | pattern | Vec<(T, usize)> hidden-index fields: serialize_with/deserialize_with free functions | `ser_excluded_items` strips the `usize` index; `de_excluded_items` reconstructs sequential indices (0,1,2,...) from a plain `Vec<T>`; both in `#[cfg(feature = "serde")]` block as free functions | Plain free functions (not methods) avoid `dead_code` warnings when the serde feature is off; keeping them in a `cfg` block is idiomatic; pattern reusable for any `Vec<(T, usize)>` where the index is internal bookkeeping not meant for the wire format | Yes — if other sorted collections need the same treatment |
| D030 | S05/T02 | convention | cargo-deny 0.19.0 `unmaintained` field uses scope values not severity values | `unmaintained = "workspace"` in deny.toml instead of the issue-specified `"warn"` | cargo-deny v0.19.0 changed the `unmaintained` field schema: accepts `all/workspace/transitive/none` (scope), not `deny/warn/allow` (severity). True warn-without-fail requires `-W unmaintained-advisory` at CI invocation level (`command-arguments` in EmbarkStudios/cargo-deny-action). `"workspace"` is the best config-only approximation. | Yes — update if CI action is extended to pass command-arguments |
| D031 | S06/T01 | pattern | KnapsackSlice OOM guard pattern | `(long)candidateCount * (capacity + 1) > 50_000_000L` with long cast on first operand; throw `InvalidOperationException` with `candidates=N, capacity=C, cells=K` message | Long cast on first operand promotes the full multiplication to long, preventing int overflow at large values; `>` (not `>=`) lets exactly 50M cells pass, matching R002 spec; diagnostic message includes all three fields needed to diagnose OOM risk in production | No |
| D032 | S06/T02 | convention | Error messages must not name internal types | `ISlicer`, `QuotaSlice`, `IAsyncSlicer` never appear in user-visible exception messages; use public API names only (e.g. `SlicerType.Stream`, `ScorerType.Scaled`) | Internal type names in error messages expose implementation details and change independently of the public API; callers cannot act on internal type names | No |
| D033 | S06/T02 | convention | Interface contract docs use interface types, not concrete implementations | `ITraceCollector` in all public doc cross-references; never `DiagnosticTraceCollector` or `NullTraceCollector` | Contracts should be described in terms of the abstraction; naming a concrete class leaks implementation choices into the public API contract | No |
| D034 | S06/T02 | impl | QuotaBuilder epsilon scope — total-sum only | `> 100.0 + 1e-9` applied only to the total-required sum check; per-kind `Require > Cap` checks left unchanged | The floating-point drift problem only affects accumulation over multiple equal-share kinds; per-kind checks are exact comparisons where epsilon would hide real bugs | No |
| D031 | S06/T02 | convention | Error messages name only public API surface | No internal types (ISlicer, QuotaSlice, IAsyncSlicer) in user-visible exceptions | Caller-facing error messages should name only public concepts (enum values, param names); internal type names leak implementation details and confuse callers | No |
| D032 | S06/T02 | impl | QuotaBuilder epsilon scope | Epsilon applied only to total-sum check (`totalRequired > 100.0 + 1e-9`); per-kind `Require > Cap` checks unchanged | Floating-point drift only affects the sum of three equal-share fractions; per-kind checks compare against integer caps where drift is not a concern | Yes — if per-kind checks also show drift |
| D033 | S06/T03 | convention | Interface contract docs use interface types | XML docs reference `ITraceCollector`, not `DiagnosticTraceCollector` or `NullTraceCollector` | Coupling docs to concrete types forces doc updates every time a new implementation is added; behavioral language is stable | No |
| D035 | S07 planning | api | `Slicer::slice` semver break accepted for v1.2.0 | Change `Slicer::slice` from `→ Vec<ContextItem>` to `→ Result<Vec<ContextItem>, CupelError>`; all 3 built-in impls are in-crate; break is compile-time-visible | Required to propagate `CupelError::TableTooLarge` from KnapsackSlice through QuotaSlice and pipeline call sites; no alternative without changing the public contract | No — intentional semver break at v1.2.0 |
| D036 | S07 planning | impl | `CupelError::CycleDetected` kept as reserved variant | Remove the cycle detection logic that emits it but keep the variant; update doc to `Never emitted — reserved for future use` | Removing the variant would be semver-breaking for any downstream `match` arm; `#[non_exhaustive]` prevents exhaustive-match breaks but not direct variant reference breaks | No |
| D037 | S07 planning | impl | `UShapedPlacer` refactor uses explicit left/right vecs | Replace `Vec<Option<ContextItem>>` + `.expect()` with `left: Vec<ContextItem>` (even ranks) and `right: Vec<ContextItem>` (odd ranks, reversed) chained together | Explicit vecs make correctness obvious without a runtime assertion; eliminates the usize-underflow guard that made the original fragile | No |
| D038 | S07/T03 | convention | Scorer unit tests use `std::slice::from_ref(&item)` for single-item `all_items` argument | `std::slice::from_ref(&item)` instead of `&[item.clone()]` or `slice::from_ref` via `use` | Required to satisfy `clippy::cloned_ref_to_slice_refs` lint under `-D warnings`; creates a single-element slice without cloning; pattern applies to any single-item scorer test that needs `all_items: &[ScoredItem]` | No |
| D039 | M002 discuss | scope | M002 is design-only — no implementation code | All M002 deliverables are spec chapters, design decision records, and brainstorm output; no .NET or Rust changes | v1.3 features require spec-first to avoid API surface being driven by type-system constraints rather than semantic clarity; principle established in M001 brainstorm for Rust diagnostics | No |
| D040 | M002 discuss | arch | Count-quota conflict detection: build-time vs run-time distinction is a hard requirement | Build-time: `RequireCount(2).CapCount(1)` must throw at policy construction; run-time: insufficient candidates is a behavioral spec, not an exception | Callers deserve build-time feedback for statically-detectable contradictions; run-time scarcity is a different class of problem that may warrant degraded behavior rather than a throw | Yes — revisit if count-quota is extended to cross-kind constraints |
| D041 | M002 discuss | api | Cupel.Testing vocabulary: no FluentAssertions dependency; no snapshot assertions | In-house chain plumbing (~100 lines); snapshot testing deferred until SelectionReport ordering stability for ties is guaranteed | FA is heavy test infrastructure; ordering stability is a precondition for deterministic snapshots (explicit decision from March 15 brainstorm) | Yes — revisit snapshot if ordering guarantee ships |
| D042 | M002 discuss | api | DecayScorer TimeProvider is mandatory, not optional | No overload that defaults to TimeProvider.System silently; caller must supply explicitly | Constructor-captured time is stale in long-lived pipelines; explicit injection makes the time dependency visible and enables deterministic tests without special test modes | No |
| D043 | M002 discuss | api | OTel attribute namespace is `cupel.*` (pre-stable); do not chase `gen_ai.*` | Own `cupel.pipeline.stage`, `cupel.item.kind`, etc.; mark as pre-stable in README | OTel LLM SIG semantic conventions have been in draft for 18+ months; premature alignment risks dashboard breakage on every SIG revision | Yes — align with `gen_ai.*` when LLM SIG stabilizes |
| D044 | M002/S01 brainstorm | api | Multi-budget DryRun API variant rejected | `FindMinBudgetFor` calls single-budget `DryRun` ~10-15 times via binary search; no combined multi-budget overload | Microsecond savings from shared stage analysis do not justify permanent API coupling; single-budget `DryRun` is already the right primitive; `FindMinBudgetFor` orchestrates it | No |
| D045 | M002/S01 brainstorm | arch | BudgetUtilization and KindDiversity belong in Wollax.Cupel core, not a separate analytics package | `BudgetUtilization(ContextBudget)` and `KindDiversity()` as extension methods on `SelectionReport` in Wollax.Cupel core | Placing them in Wollax.Cupel.Analytics creates an OTel → Analytics dependency chain; production callers need them for runtime logging and adaptive budget logic, not just tests; supersedes T02 explorer recommendation | No |
| D046 | M002/S01 brainstorm | api | CountRequireUnmet is a SelectionReport-level field, not a per-item ExclusionReason | `SelectionReport.quota_violations` or equivalent for unmet minimums; `CountCapExceeded` and `CountRequireCandidatesExhausted` are the correct per-item ExclusionReason variants | When a minimum count is unmet there is no specific item to attach the reason to; placing it on ExclusionReason breaks the "each excluded item has a reason" invariant | Yes — revisit if future count-quota semantics allow item-level attachment |
| D047 | M002/S01 brainstorm | api | Rust TimeProvider trait shape: minimal trait with Box<dyn> at DecayScorer construction | `trait TimeProvider: Send + Sync { fn now(&self) -> DateTime<Utc>; }` with `SystemTimeProvider` ZST; `Box<dyn TimeProvider + Send + Sync>` stored at construction | Minimal trait is testable, ergonomic, and avoids `Arc<dyn>` complexity; `Box<dyn>` is sufficient since DecayScorer is not `Clone`; D042 (mandatory injection) enforced by having no default | No |
| D048 | M002/S01 brainstorm | api | FindMinBudgetFor return type is int? / Option<i32> | `int?` (.NET) / `Option<i32>` (Rust); null/None = budget not found within searchCeiling | Not-found is a valid business outcome (target item requires more than caller is willing to search for); throwing an exception conflates "algorithm complete" with "error"; null/None is more honest than a sentinel integer | No |
| D049 | M002/S02 planning | convention | S02 planning task grouping | T01: diagnostics files (5 files, 11 changes), T02: algorithm files (6 files, 6 changes), T03: data-model/format/TOML/issue-deletion/tests | Grouped by file locality (diagnostics chapter vs algorithm chapters vs data-model+format+TOML) to minimize context switches within each task; T03 deferred issue deletion to after all edits complete, ensuring issue files are only removed when their spec changes are in place | No |
| D050 | M002/S02 planning | convention | spec-workflow-checksum-verification.md kept open | Deferred issue not closed in S02 | CI security concern is out of scope for a spec editorial slice (D039 — M002 is design-only; .github/workflows/ changes are CI work, not spec editorial) | No |
| D051 | M002/S03 planning | convention | S03 verification strategy | Contract-level only (document completeness checks via grep + test suite regression checks) | S03 produces a design record, not code; the stopping condition is a TBD-free document with all 5 questions answered; test suite checks confirm no regressions from the spec-only change | No |
| D052 | M002/S03 planning | api | CountQuotaSlice KnapsackSlice guard: conservative-first (5E) for v1 | Build-time rejection of CountQuotaSlice + KnapsackSlice combination; upgrade path to CountConstrainedKnapsackSlice documented | Conservative-first principle: GreedySlice inner covers the overwhelming majority of count-quota use cases; count-constrained knapsack is a significant implementation investment with no confirmed demand in M002 scope; CupelError::TableTooLarge guard provides the existing template | Yes — promote to 5A or 5D in M003+ if demand confirmed |
| D053 | M002/S03 planning | api | CountQuotaSlice cap scope: slicer-selected items only | Count-quota caps apply to items selected by the slicer, not to pinned items | Pinned items are committed before the slicer runs and are outside its scope; requiring callers to subtract pinned items from cap_count manually is error-prone; the slicer's budget for kind K after pinned items are accounted for is reduced accordingly | No |
| D054 | M002/S03/T01 | arch | CountQuotaSlice is a separate decorator, not a QuotaSlice extension | Separate type; callers compose `CountQuotaSlice(inner: QuotaSlice(...))` for combined count+percentage constraints | Count constraints and percentage constraints are semantically distinct systems; a unified extension would require token-cost estimation at policy construction time, which is non-deterministic (item token counts are not known until run time); the two-phase algorithm maps cleanly to a dedicated decorator | No |
| D055 | M002/S03/T01 | api | Count-quota tag semantics: non-exclusive and not configurable | An item tagged ["A","B"] counts +1 toward both RequireCount("A") and RequireCount("B") simultaneously; no per-policy toggle | Exclusive alternatives require either tag-ordering (not in Cupel spec — non-deterministic) or a priority mechanism (premature without confirmed use case); saturation at n=1 with multi-tagged items is correct behavior when documented; making semantics configurable introduces a footgun where changing the toggle is a breaking behavioral change | No |
| D056 | M002/S03/T02 | api | ScarcityBehavior::Degrade is the default; Throw is opt-in per slicer | `CountQuotaSlice` degrades gracefully when candidates are insufficient to satisfy require_count; `ScarcityBehavior::Throw` is opt-in per-slicer configuration; per-entry override deferred | Consistent with D040 (run-time scarcity is behavioral, not an exception by default); degrade is the safer default for production pipelines where a partial selection is better than an exception; per-slicer granularity sufficient for v1 | Yes — per-entry override if demand confirmed |
| D057 | M002/S03/T02 | api | SelectionReport positional deconstruction explicitly unsupported | Adding new fields to `sealed record SelectionReport` in .NET is safe for property-access callers; positional deconstruction callers break — this is explicitly unsupported and must be documented | `sealed record` in .NET generates a positional deconstructor; adding a property changes the arity and breaks positional `Deconstruct` calls; this is a known .NET record limitation; documenting it as unsupported prevents callers from building on a fragile surface | No |
| D058 | M002/S04 planning | convention | S04 verification strategy | Contract-level only (grep-based completeness checks + test suite regression checks) | S04 produces a spec chapter and two navigation file edits; stopping condition is a TBD-free chapter with all required sections, linked from SUMMARY.md and scorers.md index; test suite checks confirm no regressions from spec-only changes | No |
| D059 | M002/S04 planning | convention | MetadataTrustScorer .NET type handling: accept double or string | .NET implementation MUST handle both `string` and `double` (or boxed `object`) in `IReadOnlyDictionary<string, object?>` lookup; string is the canonical wire format for serialization interop | .NET `metadata` uses `IReadOnlyDictionary<string, object?>` so callers can store values as native `double`; forcing stringification for .NET callers creates unnecessary friction; the spec standardizes on string at the wire format level while allowing implementations to accept both | No |
| D060 | M002/S04 planning | convention | cupel:source-type is an open string convention, not a closed enum | Four RECOMMENDED values defined; implementations MUST NOT validate or reject unknown source-type values at construction | Closing the enum now prevents future additions without a spec revision; the four known values cover current use cases; open extension is the safer default for a v1 convention | No |
| D061 | M002/S05 planning | convention | S05 verification strategy | Contract-level only (grep-based completeness checks + test suite regression checks) | S05 produces a spec chapter; stopping condition is TBD-free document with ≥10 (target 13) patterns, each with error message format; test suites confirm no regressions | No |
| D062 | M002/S05 planning | api | `HaveTokenUtilizationAbove` naming from S05-CONTEXT.md resolved: `HaveBudgetUtilizationAbove(threshold, budget)` with denominator `budget.MaxTokens` | `HaveBudgetUtilizationAbove` uses `sum(included.tokens)/budget.MaxTokens`; no `TotalTokensConsidered`-based utilization assertion | `TotalTokensConsidered` is a candidate-set coverage metric, not a budget utilization metric; conflating the two produces misleading assertions; MaxTokens is the hard ceiling (DI-3 + D045 confirmed) | No |
| D063 | M002/S05 planning | convention | S05 targets 13 well-specified patterns | 10 from CONTEXT.md baseline scope + 3 well-specified additions (IncludeExactlyNItemsWithKind, ExcludedItemsAreSortedByScoreDescending, PlaceTopNScoredAtEdges) | Milestone requires ≥10; 13 covers all common use cases with no vague patterns; ExcludeItemWithBudgetDetails included with language-asymmetry note | No |
| D064 | M002/S05 planning | api | Floating-point threshold comparisons in vocabulary: exact operator, no epsilon | `>=` for lower-bound, `<=` for upper-bound; no default epsilon in method signature | Test authoring responsibility: callers choose thresholds that avoid floating-point boundary cases; baking in epsilon hides real threshold violations and couples the spec to a particular float implementation | No |
| D065 | M002/S05/T01 | api | Cupel.Testing assertion predicate type: IncludedItem/ExcludedItem, not raw ContextItem | Inclusion-group assertions use `Func<IncludedItem, bool>` predicates; Exclusion-group assertions use `Func<ContextItem, bool>` or `Func<ExcludedItem, bool>`; no raw `ContextItem` where a richer type is available | `IncludedItem` carries both the `ContextItem` and its final computed score; exposing only `ContextItem` to the predicate discards score information that ordering assertions require; using the richest available type in predicates avoids wrapper lookups and is more honest about what the assertion is checking | No |
| D066 | M002/S05/T01 | api | Cupel.Testing chain entry point: `SelectionReport.Should()` returning `SelectionReportAssertionChain`; failure via dedicated `SelectionReportAssertionException` | Single entry point; all assertion methods return `SelectionReportAssertionChain` for chaining; failure throws `SelectionReportAssertionException` (not `AssertionException` or `Exception`) | A dedicated entry point mirrors xUnit/NUnit conventions; a dedicated exception type allows callers to catch assertion failures specifically without catching unrelated exceptions; `SelectionReportAssertionException` must carry the structured message without depending on FluentAssertions (D041) | No |
| D067 | M002/S06 planning | convention | S06 verification strategy | Contract-level only (grep-based completeness checks + test suite regression checks) | S06 produces three spec chapters; stopping condition is all three files TBD-free with required sections present and linked from SUMMARY.md; test suites confirm no regressions from spec-only changes (no code changes per D039) | No |
| D068 | M002/S06 planning | arch | OTel stage count: 5 Activities (Sort omitted) | Root `cupel.pipeline` Activity + 5 child `cupel.stage.{name}` Activities: classify, score, deduplicate, slice, place; Sort receives no Activity | Sort is already omitted from the `PipelineStage` enum in events.md ("Sort is an internal ordering step with no user-visible diagnostic boundary"); OTel Activities follow the same stage boundary set; adding a Sort Activity would add cardinality with no diagnostic value and create a conformance obligation for all implementors | No |
| D069 | M002/S06 planning | api | GetMarginalItems budget parameter: explicit `ContextBudget budget` + `int slackTokens` | Signature: `GetMarginalItems(IReadOnlyList<ContextItem> items, ContextBudget budget, int slackTokens)`; reduced-budget run uses `budget.MaxTokens - slackTokens`; `budget` overrides the pipeline's stored budget for internal DryRun calls | Current `DryRun` uses the pipeline's fixed `_budget`; budget simulation methods must run DryRun at different budgets; passing budget explicitly (option 1) is cleanest — avoids cloning pipeline internals and makes the budget arithmetic visible at the call site; consistent with the roadmap signature `GetMarginalItems(items, budget, slackTokens)` | No |
| D070 | M002/S06 planning | api | Step curve `windows` type and comparison | `windows` is an ordered list of `(maxAge: Duration, score: double)` pairs from youngest to oldest boundary; scorer returns score for first window where `window.maxAge > age` (strict greater-than); last window's score applies when age exceeds all boundaries; empty list forbidden (throw at construction); any window with `maxAge = Duration::ZERO` forbidden (throw at construction) | Strict `>` avoids ambiguity at exact boundary values; youngest-to-oldest ordering matches natural mental model (most-recent window first); empty list and zero-width windows are degenerate inputs with no meaningful semantics — throw-at-construction is consistent with the Exponential zero half-life rule | No |
| D071 | M002/S06 planning | api | Window curve boundary: half-open interval `[0, maxAge)` — age == maxAge returns 0.0 | `Window(maxAge)` returns 1.0 when `age < maxAge`; returns 0.0 when `age >= maxAge` | Half-open intervals are the standard convention for range checks (consistent with array indexing, Step curve logic); a closed interval `[0, maxAge]` would require the spec to define "at boundary" separately; committing to 0.0 at the boundary is simpler, predictable, and avoids a conformance ambiguity | No |
| D072 | M003/S01 planning | convention | S01 verification strategy | Contract-level only (`cargo test --all-targets`, `dotnet test`, drift guard diff) | DecayScorer is a pure function; conformance vectors cover all 5 spec-defined scenarios; drift guard ensures spec/crate TOML parity; no integration or operational verification required for a scorer implementation | No |
| D073 | M003/S01 planning | impl | .NET DecayCurve modelled as abstract class with sealed nested subtypes | `public abstract class DecayCurve` with nested `public sealed class Exponential`, `Window`, `Step`; preconditions throw `ArgumentException` (not `ArgumentOutOfRangeException`) naming the parameter | Sealed nested classes are idiomatic for closed discriminated union in .NET; nested placement keeps the hierarchy co-located; `ArgumentException` matches the spec's "throw at construction" language and the pattern in TagScorer | No |
| D074 | M003/S01 planning | impl | Rust `DecayCurve` uses free-function constructor pattern (`DecayCurve::exponential`, `::window`, `::step`) returning `Result<Self, CupelError>` | Precondition validation is in the `DecayCurve` constructor methods, not in `DecayScorer::new` | Validation at the curve level is more composable and matches the "curve factories MUST validate preconditions at construction time" language in the spec; `DecayScorer::new` only validates `null_timestamp_score` | No |
| D075 | M003/S01/T01 | impl | Rust age uses millisecond precision (`num_milliseconds() / 1000.0`) not `num_seconds()` | `age_secs = duration.num_milliseconds() as f64 / 1000.0` throughout curve computation | `num_seconds()` truncates sub-second durations to zero, incorrectly giving maximum score to items that are almost-but-not-quite stale; millisecond precision is correct for all curve types and avoids edge-case scoring errors near the half-life | No |
| D076 | M003/S01/T03 | impl | .NET age clamping uses explicit zero-check, not `.Duration()` | `var age = rawAge < TimeSpan.Zero ? TimeSpan.Zero : rawAge` in `DecayScorer.Score` | `TimeSpan.Duration()` returns the absolute value — a future-dated item with a +1h timestamp would produce a 1h age instead of being clamped to zero; the explicit check matches Rust's `raw_age.max(Duration::zero())`; this was a plan-text imprecision, not a scope deviation | No |
| D077 | M003/S01/T03 | api | Protected constructor `DecayCurve()` must be listed in `PublicAPI.Unshipped.txt` | `protected Wollax.Cupel.Scoring.DecayCurve.DecayCurve() -> void` listed in PublicAPI.Unshipped.txt even though it only prevents external subclassing | RS0016 (PublicApiAnalyzers) requires every public-or-protected member to be declared; omitting the protected ctor blocks the build with an actionable but potentially confusing error; listing it is the correct resolution and becomes the pattern for all future abstract base classes | No |
| D078 | M003/S02 planning | impl | S02 verification strategy | Contract-level only (`cargo test --all-targets`, `dotnet test`, drift guard diff) | MetadataTrustScorer is a pure function with no I/O dependencies; conformance vectors cover all 5 spec-defined edge cases; drift guard ensures spec/crate TOML parity; no integration or operational verification required for an absolute scorer | No |
| D079 | M003/S02 planning | impl | TOML conformance vector metadata format for MetadataTrustScorer | Inline table on `[[items]]` entries: `metadata = { "cupel:trust" = "0.85" }`; `build_items` extended to parse this into `HashMap<String, String>` | TOML inline tables are the standard format for simple key-value maps within array-of-table entries; no nested `[[items.metadata]]` needed; the existing TOML parser (`toml` crate) handles inline tables via `.as_table()`; this extension enables metadata-bearing vectors for all future scorers (not just MetadataTrustScorer) | No |
| D080 | M003/S02 planning | impl | `MetadataTrustScorer` construction error uses `ArgumentOutOfRangeException` in .NET (not `ArgumentException`) | `throw new ArgumentOutOfRangeException(nameof(defaultScore), defaultScore, "defaultScore must be in [0.0, 1.0]")` | The parameter is a numeric value with explicit valid range — `ArgumentOutOfRangeException` is the semantically correct BCL exception for out-of-bounds numeric parameters; distinct from `ArgumentException` used for DecayCurve (shape/type violations); naming the value in the exception message aids diagnosis | No |
| D081 | M003/S02/T01 | impl | `is_finite()` check MUST follow `parse::<f64>()` (not replace it) in MetadataTrustScorer Rust `score()` | Order: key lookup → `str::parse::<f64>()` → `is_finite()` check → `clamp(0.0, 1.0)` | `"NaN".parse::<f64>()` returns `Ok(f64::NAN)` — a NaN successfully parses; replacing the parse step with `is_finite()` alone would incorrectly accept NaN as a valid value; the two guards are orthogonal: parse handles malformed strings, is_finite handles parsed-but-non-finite values | No |
| D082 | M003/S02/T01 | convention | Conformance vectors must be copied to THREE locations: `spec/conformance/`, `conformance/` (repo root), and `crates/cupel/conformance/` | Pre-commit hook enforces sync between root `conformance/` and `crates/cupel/conformance/`; task plans reference `spec/conformance/` as canonical origin | The pre-commit hook does not check `spec/conformance/` — it diffs `conformance/required/` (root) vs `crates/cupel/conformance/required/`; skipping the root copy blocks the commit; all three directories must stay in sync for spec traceability + CI drift guard + commit hook | No |
| D083 | M003/S02/T02 | impl | D059 dual-type dispatch: `double` branch checked before `string` branch in .NET `Score()` | `if (raw is double d)` evaluated before `else if (raw is string s)` | Native `double` callers should receive their value directly without falling through to string parsing; checking `string` first could incorrectly route a boxed double through `TryParse`, risking precision loss or unexpected fallback to `_defaultScore` | No |
| D084 | M003/S03 planning | impl | S03 verification strategy | Contract-level only (`cargo test --all-targets`, `dotnet test`, drift guard diff); no pipeline-level integration tests for slicers | CountQuotaSlice is exercised via direct slicer unit tests and conformance vectors; `Slicer::slice` interface contract is sufficient; pipeline-level shortfall wiring (to `SelectionReport`) is a future concern | No |
| D085 | M003/S03 planning | impl | `is_knapsack()` default method on `Slicer` trait (returns `false`; overridden in `KnapsackSlice`) | Additive defaulted method on `Slicer` trait; non-breaking for existing implementors | Avoids `Any`/TypeId/downcast complexity; clean API without leaking implementation details; documented as internal use for the KnapsackSlice guard in CountQuotaSlice | No |
| D086 | M003/S03 planning | impl | Cap-reason exclusions not observable at `Slicer::slice` level in Rust | `CountCapExceeded` items are filtered out of the return value; trace collector has no path into `Slicer::slice`; conformance vectors for cap exclusion verify selected item count via `dry_run` at pipeline level or via direct unit tests checking result length | Cap reasons require a `TraceCollector` to record; `Slicer::slice` signature does not accept one; adding it would be a semver break; v1 records cap exclusions at the .NET slicer level via `traceCollector` (ISlicer.Slice accepts ITraceCollector) but not at the Rust slicer level | Yes — Rust Slicer trait could accept a TraceCollector in a future version |
| D087 | M003/S03 planning | impl | `.NET CountQuotaSlice.LastShortfalls` property as test inspection surface | `public IReadOnlyList<CountRequirementShortfall> LastShortfalls { get; private set; } = []` on CountQuotaSlice; populated during each `Slice` call | `ISlicer.Slice` returns only `IReadOnlyList<ContextItem>`; shortfalls cannot flow through the interface without a new method or ITraceCollector extension; `LastShortfalls` avoids both options for v1 and is sufficient for unit test verification; not part of `ISlicer` contract | Yes — ITraceCollector extension for shortfalls in future slice |
| D088 | M003/S04 planning | convention | S04 verification strategy | Contract-level + integration (analytics: unit tests in both languages; Cupel.Testing: test project with ≥1 test per pattern + consumption test reference) | Analytics are pure functions — unit tests are sufficient; Cupel.Testing requires integration proof (package must be installable; assertions must run against a real SelectionReport) — consumption test reference is the installability gate; drift guard not applicable (no conformance vectors in S04) | No |
| D089 | M003/S04 planning | impl | Rust analytics module location: separate `analytics.rs`, not `impl SelectionReport` block | `pub fn budget_utilization(report: &SelectionReport, budget: &ContextBudget) -> f64` etc. as free functions in a new `crates/cupel/src/analytics.rs` module | `SelectionReport` is `#[non_exhaustive]` but in-crate field access is fine; separate module is cleaner and mirrors the pattern used for other feature-scoped modules (`scorer/`, `slicer/`); free functions are idiomatic Rust for extension-method-like functionality on types from the same crate | No |
| D090 | M003/S04 planning | impl | .NET pattern 6 degenerate form: `HaveExcludedItemWithBudgetExceeded(Func<ContextItem, bool>)` | .NET `ExclusionReason` is a flat enum with no associated data — `item_tokens` and `available_tokens` fields from the Rust variant are not available; method degenerates to existence check for `BudgetExceeded` + predicate match | Spec vocabulary.md explicitly documents this language asymmetry with the `.NET implementations MAY omit the two token-detail parameters` note; the degenerate form degrades to `ExcludeItemMatchingWithReason(predicate, ExclusionReason.BudgetExceeded)` semantics; method name distinguishes it from pattern 5 for readability | No |
| D091 | M003/S04 planning | impl | `Wollax.Cupel.Testing` test project uses direct `SelectionReport` record construction for test data, not DiagnosticTraceCollector | `new SelectionReport { Events = [], Included = [...], Excluded = [...], TotalCandidates = N, TotalTokensConsidered = T }` for test fixtures in `AssertionChainTests.cs` | Running a full pipeline for each assertion test adds ~30 lines of boilerplate per test and introduces indirect dependencies on pipeline behavior; direct record construction is deterministic, minimal, and makes the assertion contract the test focus; `SelectionReport` is a `sealed record` with `required` properties — direct construction is the intended consumer pattern | No |
| D092 | M003/S04/T01 | impl | `ContextBudget::new` in Rust takes `HashMap<ContextKind, i64>` directly (no `Option` wrapper) | Test helpers pass `Default::default()` for empty reserved_slots | `HashMap::default()` is the idiomatic Rust way to pass an empty map; using `Option` would require unwrapping or defaulting in the constructor; `Default::default()` is zero-cost and avoids confusion about absent vs empty reserved slots | No |
| D093 | M003/S04/T01 | impl | Rust analytics uses `.kind()` accessor method, not direct field access on `ContextItem` | `item.item.kind()` (accessor) not `item.item.kind` (field) in analytics.rs | `ContextItem.kind` is a private field in the Rust struct; `.kind()` is the public accessor method; this is the only access path even from within the same crate unless using field-init shorthand | No |
| D094 | M003/S04/T02 | api | `SelectionReportAssertionChain` constructor is `internal`, not in `PublicAPI.Unshipped.txt` | Chain is created exclusively via `SelectionReport.Should()` static extension method | Exposing the constructor publicly would allow callers to construct chains without going through `Should()`, breaking the single-entry-point invariant (D066); `internal` is the correct visibility since the test project and chain are in the same package | No |
| D095 | M003/S04/T03 | convention | Consumption tests local NuGet feed path is `./packages`, not `./nupkg` | `nuget.config` configures `./packages` as the local feed source; packed `.nupkg` files must be copied there, not referenced from `./nupkg` directly | `dotnet pack --output ./nupkg` is the conventional pack-output destination; the consumption test project's `nuget.config` independently declares `./packages` as the local source; callers must copy the artifact between directories to complete the local-feed workflow | No |
| D096 | M003/S04/T03 | impl | `PlaceTopNScoredAtEdges` tie handling: minTopScore comparison against topN items using HashSet membership | Alternating edge placement (lo++, hi-- pattern); ties determined by `score >= minTopScore` (where minTopScore = topN items' minimum score); edge HashSet tracks assigned positions | HashSet membership check against topN items handles the common case where topN items are distinct-scored; the minTopScore comparison ensures tie-score items outside the explicit topN set are also recognized and placed at edges; the alternating lo/hi pattern matches the spec's "topN items placed at edges" semantics | Yes — revisit if spec adds deterministic tie-breaking |
| D097 | M003/S06 planning | convention | S06 verification strategy | Integration-level with failing-first .NET budget-simulation tests, explicit .NET/Rust GreedySlice tie-break regressions, full-suite regression checks, and grep-based spec alignment checks | S06 closes both runtime and documentation gaps; a passing build alone would not prove budget override semantics, deterministic ties, or spec/index/changelog accuracy | No |
| D098 | M003/S06 planning | api | Budget simulation API shape | Both public `.NET` methods take an explicit `ContextBudget budget` parameter and reuse an internal `CupelPipeline` temporary-budget execution seam | Reconciles the roadmap/spec mismatch for `FindMinBudgetFor`, keeps the override visible at the call site, and avoids duplicating pipeline logic or reconstructing pipelines externally | No |
| D099 | M003/S06 planning | contract | GreedySlice tie-break contract | Equal-density items use stable original-order / original-index ascending tie-breaking; no new `ContextItem.Id` surface is introduced in M003 | The code already implements deterministic index-based ordering in both languages, while the roadmap shorthand `id ascending` is not literally implementable because `ContextItem` has no `Id` field | No |
| D100 | M003/S05 planning | arch | OTel bridge core seam | Add an additive `ITraceCollector` completion hook plus a structured `StageTraceSnapshot` handoff; never derive OTel data by parsing `TraceEvent.Message` | The bridge needs exact stage counts, timing, budget, and final-report data. A structured end-of-run handoff keeps core zero-dependency while giving the companion package enough truth to emit spec-accurate telemetry. | No |
| D101 | M003/S05 planning | convention | S05 verification strategy | Integration-level with failing-first core/package tests, real OpenTelemetry SDK in-memory exporter capture, local-feed consumption proof, and full `dotnet test` regression | R022 is an observability integration slice. A green build alone would not prove the canonical source name, 5-Activity hierarchy, verbosity tiers, or installability of the companion package. | No |
| D102 | M003/S05 planning | api | OTel companion public surface | Companion package uses its own `CupelOpenTelemetryVerbosity` enum and emits only `cupel.*` operational fields (`kind`, `tokens`, `score`, `reason`, stage names, budget, verbosity) — never item content or raw metadata values | `TraceDetailLevel` in core has only two tiers and does not match the spec. Keeping a package-specific verbosity enum preserves the zero-dependency core contract and enforces the slice's redaction boundary. | No |
| D103 | M004 discuss | api | SelectionReport equality uses exact f64 comparison | `PartialEq`/`Eq` (Rust) + `IEquatable<T>` (.NET) with no epsilon | Deterministic pipelines produce identical scores; callers needing fuzzy comparison build their own; epsilon complicates Eq/Hash contract and hides real divergence | No |
| D104 | M004 discuss | api | PolicySensitivityReport returns labeled reports + structured diff | `RunPolicySensitivity` returns both `[(label, SelectionReport)]` and a `PolicySensitivityDiff` showing items that changed status | Labeled reports are the raw data for flexible downstream use; the structured diff is the value-add that answers "what swings?" without caller LINQ work; supporting both avoids a second API call | No |
| D105 | M004 discuss | arch | QuotaUtilization uses IQuotaPolicy abstraction | New `IQuotaPolicy` interface extracted from QuotaSlice and CountQuotaSlice rather than taking CountQuotaSlice config directly | User chose abstraction over direct config; IQuotaPolicy is more extensible if future quota types are added; backward-compatible additive interface implementation | Yes — simplify if only one quota type ever needs utilization |
| D106 | M004 discuss | convention | Snapshot testing uses JSON format with CUPEL_UPDATE_SNAPSHOTS=1 env var | JSON snapshots (not TOML or custom text); env var triggers in-place rewrite during test run | JSON is widely tooled, diffable in PRs, and consistent with .NET ecosystem conventions; env var is simpler than a CLI tool and matches the `insta` crate pattern | No |
| D107 | M004 discuss | scope | Rust snapshot testing out of scope | Rust has `insta` crate; Cupel.Testing snapshot is .NET only | No point hand-rolling in Rust what `insta` already does better; `insta` supports JSON, YAML, and custom formats with `cargo insta review` | No |
| D108 | M004/S01 planning | convention | S01 verification strategy | Contract-level only (`cargo test --all-targets`, `dotnet test --configuration Release`, PublicAPI analyzer, clippy) | Equality is a pure function with no I/O or runtime dependencies; unit tests directly exercise `==` on constructed types; no integration or operational verification needed | No |
| D109 | M004/S01 planning | impl | Rust diagnostic types get PartialEq but NOT Eq | `PartialEq` derived on 6 structs; `Eq` omitted because `f64` fields (`score`, `duration_ms`, `tokens_over_budget`) prevent it | IEEE 754: `NaN != NaN` makes `Eq` unsound for f64-containing types; `PartialEq` is sufficient for `==` comparison; consistent with D103 exact f64 semantics | No |
| D110 | M004/S01/T01 | impl | ContextBudget gets PartialEq as transitive requirement | `PartialEq` added to `ContextBudget` derive because `OverflowEvent` contains a `ContextBudget` field | Rust derive requires `PartialEq` on all fields transitively; `ContextBudget` has f64 `estimation_safety_margin_percent` so `Eq` omitted; public API change — additive and non-breaking | No |
| D111 | M004/S01/T02 | pattern | .NET record equality: compiler-generated == and != delegate to custom Equals | No explicit `operator ==`/`operator !=` declarations needed on records with custom `Equals` override | C# record compiler generates `==` and `!=` that call `EqualityContract` + `Equals`; manually declaring them causes CS0111 (duplicate); PublicAPI analyzer requires only `Equals` and `GetHashCode` entries | No |
| D112 | M004/S02 planning | convention | S02 verification strategy | Integration-level with real pipeline `dry_run` calls in both languages; tests exercise ≥2 variants producing meaningful diffs | Fork diagnostic is thin orchestration over `dry_run`; integration test with real pipelines proves the diff is correct without needing conformance vectors; no operational verification needed for a pure analytics function | No |
| D113 | M004/S02 planning | impl | PolicySensitivityDiff uses content-keyed matching, not reference equality | Items matched across variants by `content()` / `Content` string; each variant's `dry_run` produces distinct `ContextItem` instances so reference equality is impossible | Content is the stable identity for items across independent pipeline runs; `ContextItem` has no `Id` field (D099); content-based matching is the only reliable cross-variant identity | No |
| D114 | M004/S02 planning | api | .NET PolicySensitivity uses internal DryRunWithBudget for budget override | `PolicySensitivityExtensions.PolicySensitivity(items, budget, variants)` calls `DryRunWithBudget(items, budget)` — internal to same assembly | Matches Rust semantics where `dry_run` takes an explicit budget parameter; avoids requiring callers to construct separate pipeline instances per budget; `DryRunWithBudget` was designed for exactly this use case (budget simulation) | No |
| D115 | M004/S03 planning | convention | S03 verification strategy | Contract + integration level (`cargo test --all-targets`, `dotnet test --configuration Release`, PublicAPI analyzer); tests exercise both QuotaSlice and CountQuotaSlice through the QuotaPolicy/IQuotaPolicy abstraction | Pure analytics function; unit tests with real slicer instances are sufficient; no conformance vectors needed (no spec chapter for QuotaUtilization); no operational verification | No |
| D116 | M004/S03 planning | api | QuotaConstraint uses f64 for both percentage and count modes | `QuotaConstraint { kind, mode, require: f64, cap: f64 }` — percentage mode stores 0-100 values, count mode stores count values as f64 | Unified f64 type avoids a sum type for require/cap fields; count values are small integers that fit losslessly in f64; utilization ratio computation is uniform (`actual / cap`) regardless of mode | No |
| D117 | M004/S03 planning | api | quota_utilization requires explicit ContextBudget parameter | `quota_utilization(report, policy, budget)` in Rust; `QuotaUtilization(report, policy, budget)` in .NET | Percentage-mode utilization needs `target_tokens` as the denominator; the report does not carry the budget that produced it; explicit budget parameter matches D069 (explicit budget) pattern and D045 (budget_utilization already takes budget) | No |
| D118 | M004/S04 planning | convention | S04 verification strategy | Integration-level with real file I/O lifecycle tests (create→match→fail→update cycle in temp directories); `dotnet test --configuration Release` + `cargo test --all-targets` regression | Snapshot testing is fundamentally a file I/O feature; contract-level verification (compile check) would not prove the create/read/update/diff logic works; temp directories isolate tests from the source tree | No |
| D119 | M004/S04 planning | impl | MatchSnapshot uses internal MatchSnapshotCore for testability | Public `MatchSnapshot(string name, [CallerFilePath] string callerFilePath = "")` delegates to internal `MatchSnapshotCore(string name, string callerFilePath)`; tests call the internal overload with explicit temp paths | `[CallerFilePath]` is compile-time — tests cannot override it; an internal overload lets tests control the snapshot directory without polluting the test project source tree; `InternalsVisibleTo` exposes only the test seam | No |
| D120 | M004/S04 planning | impl | Snapshot JSON uses System.Text.Json with camelCase + enum strings + indented | `JsonSerializerOptions` with `PropertyNamingPolicy = CamelCase`, `JsonStringEnumConverter`, `WriteIndented = true`; no dependency on `Wollax.Cupel.Json` package | `Wollax.Cupel.Json` serializes `CupelPolicy` and `ContextBudget` via source-gen context; `SelectionReport` is not registered there; adding a dependency on a separate package for a testing utility is unnecessary; `System.Text.Json` is a BCL dependency with no additional package overhead | No |
| D121 | M004/S04/T02 | convention | Snapshot tests use `[NotInParallel]` due to process-global env var | `CUPEL_UPDATE_SNAPSHOTS` is process-global; parallel test execution causes the Update test's env var to leak into Fail/NoUpdate tests | `Environment.SetEnvironmentVariable` affects the entire process; TUnit's default parallel execution creates a race condition where the env var set by one test is visible to concurrently-running tests; `[NotInParallel]` is the correct TUnit mechanism to serialize tests with shared mutable process state | No |
| D122 | M004/S05 planning | convention | S05 verification strategy | Contract + integration level (`cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings`); real Pipeline + dry_run calls in tests | Budget simulation is thin orchestration over `dry_run`; integration tests with real pipelines prove the diff/search logic is correct; no conformance vectors needed (spec chapter exists but vectors are not required for Rust parity) | No |
| D123 | M004/S05 planning | impl | Budget simulation methods live on `Pipeline` as `impl` methods, not free functions in `analytics.rs` | `get_marginal_items` and `find_min_budget_for` as `pub fn` on `impl Pipeline` | Methods need `&self` access to the pipeline's slicer for monotonicity guards; matches .NET extension-method-on-pipeline pattern; free functions would require passing both pipeline and items, losing the natural `pipeline.get_marginal_items(items, budget, slack)` call site | No |
| D124 | M004/S05 planning | impl | Error variant for monotonicity guards: `CupelError::PipelineConfig(String)` | `PipelineConfig` over `SlicerConfig` for budget simulation guard errors | The guard is a runtime check on the pipeline's slicer configuration in the context of budget simulation — it's a pipeline-level configuration incompatibility, not a slicer construction error; consistent with "this pipeline configuration doesn't support budget simulation" semantics | No |
| D125 | M004/S05 planning | impl | `is_quota()` and `is_count_quota()` as separate defaulted methods on `Slicer` trait | Two methods (not a single `is_monotonic()` inverse) following D085 `is_knapsack()` pattern | Separate methods allow asymmetric guards: `get_marginal_items` checks only `is_quota()`, `find_min_budget_for` checks both; a single `is_monotonic()` method would lose the distinction needed to match .NET's asymmetric QuotaSlice-only vs QuotaSlice+CountQuotaSlice guards | No |
| D126 | M005 discuss | arch | Rust testing vocabulary is a separate crate, not a feature flag | `cupel-testing` crate in `crates/cupel-testing/` | Mirrors .NET `Wollax.Cupel.Testing` as a distinct package; no feature-flag complexity; callers add as dev-dependency; keeps core crate lean | No |
| D127 | M005 discuss | api | Fluent chain API: `report.should().include_item_with_kind(kind)` | `SelectionReportAssertions` trait + `SelectionReportAssertionChain` struct | Matches spec vocabulary chain model (PD-4); familiar to .NET users; consistent with spec `SelectionReport.Should()` entry point | No |
| D128 | M005 discuss | convention | Assertion failures panic (not Result) | Standard Rust test convention; `#[should_panic]` works; chain methods return `&mut Self` | Returning `Result` would require `.unwrap()` or `?` on every assertion call, defeating fluent chaining; Rust test convention is to panic on assertion failure | No |
| D129 | M005/S01 planning | convention | S01 verification strategy | Contract-level only (`cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings` across both crates) | S01 produces crate scaffold and chain plumbing with no runtime behavior; a smoke test proving `should()` returns the chain struct is sufficient; assertion methods are S02's concern | No |
| D130 | M005/S02 planning | convention | S02 verification strategy | Integration-level: 26 integration tests in `crates/cupel-testing/tests/assertions.rs` (2 per pattern × 13) using real mini-pipelines via `DiagnosticTraceCollector::new(TraceDetailLevel::Item)`; positive tests assert assertions pass; negative tests use `#[should_panic(expected = "...")]` with spec message substring | Integration proof is appropriate — assertion methods call `self.report` fields that only exist in real pipeline output since `SelectionReport` is `#[non_exhaustive]`; unit tests cannot construct the report directly | No |
| D131 | M005/S02 planning | impl | Pattern 13 index-based approach: `Vec<(f64, usize)>` sorted by score, `HashSet<usize>` for edge positions | Collect `(score, original_index)` pairs, sort by score descending (f64::total_cmp), take first N, enumerate edge positions (lo/hi), build edge set of indices, verify each top-N index is in edge set | `IncludedItem` contains `f64` (score) which prevents `Hash` derivation; `HashSet<&IncludedItem>` from the .NET reference is blocked; index-based approach is idiomatic Rust and avoids the Hash constraint entirely (open risk from S02 research confirmed and closed here) | No |
| D132 | M005/S02 planning | impl | ExclusionReason variant matching uses `std::mem::discriminant` | `std::mem::discriminant(&e.reason) == std::mem::discriminant(&reason)` for patterns 4 and 5; reason arg passed by value | `ExclusionReason` is a data-carrying enum; `==` would require all field values to match, not just the variant; discriminant comparison is the correct "same variant?" check; `matches!()` would require hardcoding each variant in chain.rs which breaks on new variants | No |
| D133 | M005/S03 planning | convention | S03 verification strategy | Final-assembly level: `cargo package --allow-dirty` exits 0; one chained integration test (≥3 methods on same `should()` chain) using real `Pipeline::run_traced()` output; `cargo test --all-targets` + clippy clean in both crates | S03 is the milestone's final gate; package proof requires a real `.crate` tarball attempt; chained test proves `&mut Self` composition end-to-end; per-pattern tests (S02) are not sufficient alone for the milestone definition of done | No |
| D134 | M005/S03 planning | impl | `include` field for `cupel-testing` matches `cupel` pattern without conformance/ or examples/ | `include = ["src/**/*.rs", "tests/**/*.rs", "Cargo.toml", "LICENSE", "README.md"]` — no `conformance/**/*.toml` or `examples/**/*.rs` | `cupel-testing` has no `conformance/` or `examples/` directories; including non-existent glob patterns is harmless but unnecessary; minimal `include` list reduces noise in `cargo package --list` output | No |
| D135 | M005/S03 planning | impl | cupel dep version = "1.1" in cupel-testing Cargo.toml | `cupel = { version = "1.1", path = "../cupel" }` — version field required by cargo package; path-only deps disallowed when packaging; `--no-verify` used until cupel is published to crates.io | `cargo package` rejects path-only dependency declarations without a version; the version field satisfies the packaging requirement without forcing an actual crates.io download | No |
| D136 | M006 planning | scope | M006 is implementation-only; design fully settled | No new spec chapters or design decisions required; all DI rulings from M002/S03 are final; `.planning/design/count-quota-design.md` is authoritative | The count-quota design was the explicit output of M002/S03; any re-designing during M006 would invalidate the settled pseudocode and conformance vector outlines | No |
| D137 | M006/S02 planning | impl | S02 verification strategy | Integration-level: 5 integration tests via real `CupelPipeline.DryRun()` asserting `CountRequirementShortfalls` and `CountCapExceeded` from real pipeline output; full `dotnet build` (0 warnings) + full `dotnet test` regression | Pipeline-level wiring cannot be verified by unit tests against `slicer.Slice()` directly; only `DryRun()` exercises the complete `Execute()` path including the re-association loop and `ReportBuilder.Build()` | No |
| D138 | M006/S02 planning | impl | Cap-excluded vs budget-excluded classification priority: budget constraint wins | When an item both exceeds the adjusted budget target tokens AND is cap-excluded, classify as `BudgetExceeded` (budget constraint is the primary reason; cap is moot when the item doesn't fit regardless) | Consistent with research: an item that can't fit in the budget would never have been selected even without a cap; reporting `CountCapExceeded` for a budget-exceeding item is misleading | No |
| D139 | M006/S02 planning | impl | Pinned-count decrement scenario (conformance vector 3) explicitly out of scope for S02 | `.NET` integration tests skip this scenario; ISlicer.Slice has no pinned parameter; the pipeline excludes pinned items before calling the slicer; a pinned scenario cannot be tested end-to-end without ISlicer API changes | D086 documents this as an architectural limitation for v1; adding the scenario would require changing the ISlicer contract, which is out of scope for M006 | No |
| D140 | M006/S02/T01 | impl | CountQuotaSlice.Entries exposed as internal property (not public) | `internal IReadOnlyList<CountQuotaEntry> Entries => _entries;` — accessible within Wollax.Cupel assembly only | `CupelPipeline` and `CountQuotaSlice` are in the same assembly; internal visibility provides access without adding public API surface that would require PublicAPI.Unshipped.txt update; mirrors the QuotaSlice pattern for internal-assembly access | No |
| D141 | M006/S02/T01 | impl | selectedKindCounts built from slicedItems (pipeline output), not CountQuotaSlice internals | After `_slicer.Slice()` returns, iterate `slicedItems` and increment `selectedKindCounts[item.Kind]` per item | Reconstructing from the actual Slice() output guarantees the count reflects what the pipeline accepted, not what CountQuotaSlice computed internally — avoids subtle divergence if the inner slicer or pipeline trims items post-selection | No |
| D142 | M006/S02/T02 | impl | Integration tests require WithScorer(new ReflexiveScorer()) even when CountQuotaSlice drives ranking | `PipelineBuilder.Build()` throws if no scorer is registered; ReflexiveScorer reads FutureRelevanceHint which maps to the test items' scores | Scorer is mandatory in PipelineBuilder regardless of slicer type; ReflexiveScorer is the canonical no-op scorer for integration tests where score is externally provided | No |
| D143 | M006/S03 planning | convention | S03 verification strategy | Integration-level with real `dry_run()` / `DryRun()` composition tests in both languages; full-suite regression checks in both languages; PublicAPI build-level audit; R061 validation in REQUIREMENTS.md | S03 is the milestone's final gate; composition test is the one integration not proven in S01 or S02; PublicAPI analyzer provides build-time proof of completeness; full regression ensures no regressions from adding composition test files | No |
| D144 | M006/S03 planning | impl | `.NET` QuotaSlice requires `QuotaSet` built via `QuotaBuilder` — no direct `IReadOnlyList<QuotaEntry>` constructor | Composition test uses `new QuotaBuilder().Require(kind, pct).Cap(kind, pct).Build()` to produce `QuotaSet`; `QuotaSet` internal constructor is not accessible from test projects | `QuotaSet` encapsulates validated quota configuration; construction via `QuotaBuilder` enforces `Require ≤ Cap` and total-sum invariants at build time; bypassing it would produce unvalidated state | No |
| D145 | M006/S03/T01 | impl | `Slicer::count_cap_map()` default method on Slicer trait | Returns empty `HashMap<String, usize>` by default; `CountQuotaSlice` overrides to expose per-kind caps | Exposes cap limits to the pipeline without widening the trait's minimal surface; default is safe for all existing slicers that have no count caps; avoids requiring all slicer implementors to handle count-cap semantics | No |
| D146 | M006/S03/T01 | impl | Pipeline Stage 5: CountCapExceeded classification from reconstructed kind counts | When `slicer.is_count_quota()`, reconstruct `selectedKindCounts` by iterating `slicedItems`; classify slicer-excluded budget-fitting items as `CountCapExceeded{kind,cap,count}` when `selectedKindCounts[kind] >= cap` | Mirrors .NET D141 pattern; reconstruction from actual slicer output guarantees the count reflects what the pipeline accepted, not slicer internals — avoids subtle divergence if the inner slicer trims items post-selection | No |
| D147 | M006/S03/T01 | impl | Composition test budget 600 tokens (not 400 as planned) | 5 items × 100 = 500 total tokens; 600 ensures budget exhaustion never fires before the count cap, making count cap the sole binding constraint | At 400 tokens, 2 items are budget-excluded before the count cap fires (500 > 400); count cap cannot be observed as the binding constraint at 400; 600 gives enough headroom for all items to fit while count cap remains the active constraint | No |
| D148 | M007 planning | arch | DryRunWithPolicy accepts explicit budget, not inherited from pipeline | `DryRunWithPolicy(items, budget, policy)` takes a required `budget` parameter; there is no "inherit pipeline's budget" option | Policy does not carry a budget; combining a new policy with an implicit budget would produce silent surprises when the pipeline's budget was set for a different configuration; explicit budget makes the call fully self-describing | No |
| D149 | M007 planning | arch | Rust `Policy` struct uses `Arc<dyn Trait>` for scorer/slicer/placer | `Policy { scorer: Arc<dyn Scorer>, slicer: Arc<dyn Slicer>, placer: Arc<dyn Placer>, deduplication: bool, overflow_strategy: OverflowStrategy }` | `Box<dyn Trait>` is `!Clone`; `policy_sensitivity` needs to run multiple variants without consuming the policy; `Arc` allows shared ownership across runs without cloning trait objects | No |
| D150 | M007 planning | arch | Rust `policy_sensitivity` is a free function, not a `Pipeline` method | `pub fn policy_sensitivity(items, budget, variants: &[(label, &Policy)]) -> PolicySensitivityReport` | Fork diagnostics conceptually operate over policies, not pipelines; a free function makes the "no single pipeline" framing explicit; mirrors the .NET extension-method design which is not on the pipeline instance | No |
| D151 | M007 planning | pattern | CupelPolicy cannot express CountQuotaSlice — documented gap, no code workaround | `CupelPolicy` uses `SlicerType` enum with no `CountQuota` variant; `DryRunWithPolicy` via `CupelPolicy` cannot run count-quota pipelines | `CountQuota` requires runtime-constructed `CountQuotaSlice` with non-enum configuration; adding an enum variant would require expanding `CupelPolicy` significantly; the gap is documented in XML docs; callers needing count-quota fork diagnostics use the pipeline-based `PolicySensitivity` overload | Yes — if CupelPolicy gains a count-quota configuration surface |
| D152 | M007/S01 planning | convention | S01 verification strategy | Contract + integration level (`dotnet test` with 9 new tests: 6 DryRunWithPolicy + 3 PolicySensitivity policy-overload; full-suite regression) | Both new APIs are thin wrappers over existing machinery; integration tests with real pipelines prove scorer/slicer/placer are respected and budget is correctly applied; no conformance vectors needed | No |
| D153 | M007/S01 planning | impl | policy-based PolicySensitivity overload restates the content-keyed diff loop | The diff algorithm loop is re-stated in the policy-based overload (not extracted to a shared helper) to match the structural pattern of the existing pipeline-based overload | Extracting a shared helper would require a breaking change to the private diff-computation signature; the loop is ~30 lines and low-risk to duplicate; future refactor can extract if a third overload is ever added | No |
| D154 | M007/S02 planning | impl | S02 verification strategy | Integration-level: 5 integration tests in `crates/cupel/tests/dry_run_with_policy.rs` (scorer_is_respected, slicer_is_respected, deduplication_false_allows_duplicates, deduplication_true_excludes_duplicates, overflow_strategy_is_respected); full `cargo test --all-targets` + clippy regression | `dry_run_with_policy` is thin orchestration over `run_with_components`; integration tests with real scorers/slicers prove each policy field is respected; contract-level tests would not detect a `self.scorer` instead of `policy.scorer` bug | No |
| D155 | M007/S02 planning | impl | `run_with_components` extracted from `run_traced` as a private helper accepting injected trait-object references | `fn run_with_components<C: TraceCollector>(&self, items, budget, scorer: &dyn Scorer, slicer: &dyn Slicer, placer: &dyn Placer, deduplication: bool, overflow_strategy: OverflowStrategy, collector: &mut C)` — private; both `run_traced` and `dry_run_with_policy` delegate to it | `Pipeline` fields are private; no other way to inject policy components without duplicating ~280 lines of stage logic; extraction is a pure refactor with zero observable behavior change, proven by running `cargo test --all-targets` before adding new types | No |
| D156 | M007/S02/T02 | impl | `#[allow(clippy::too_many_arguments)]` on `run_with_components` | The private 9-arg helper gets the clippy attribute directly; no grouping struct added | Grouping scorer/slicer/placer + deduplication + overflow_strategy into a struct would require a new type with no semantic meaning beyond parameter grouping; the attribute is appropriate on a private, non-public-facing method | No |
| D157 | M007/S02/T02 | impl | `KnapsackSlice::new(1)` (not `with_default_bucket_size()`) in tight-budget integration tests | `slicer_is_respected` uses `KnapsackSlice::new(1)`; `with_default_bucket_size()` sets bucket_size=100, which produces capacity=0 when budget target < 100 — knapsack returns empty, defeating the test | `KnapsackSlice` computes capacity as `target / bucket_size`; any test with target < bucket_size silently gets capacity=0; use `new(1)` for exact per-token resolution in tight-budget tests | No |
