name: Publish to crates.io

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Dry run — test and verify without publishing'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@1.85.0
        with:
          components: rustfmt, clippy

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "crates/cupel -> target"

      - name: Format
        run: cargo fmt --check --manifest-path crates/cupel/Cargo.toml

      - name: Clippy (default features)
        run: cargo clippy --all-targets --manifest-path crates/cupel/Cargo.toml -- -D warnings

      - name: Clippy (serde)
        run: cargo clippy --features serde --all-targets --manifest-path crates/cupel/Cargo.toml -- -D warnings

      - name: Test (default features)
        run: cargo test --manifest-path crates/cupel/Cargo.toml

      - name: Test (serde)
        run: cargo test --features serde --manifest-path crates/cupel/Cargo.toml

      - name: Deny
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          manifest-path: crates/cupel/Cargo.toml

      - name: Verify package contents
        run: cargo package --features serde --manifest-path crates/cupel/Cargo.toml --list

  publish:
    runs-on: ubuntu-latest
    needs: test
    if: ${{ inputs.dry-run != true }}
    environment: release
    steps:
      - name: Verify running from main
        if: github.ref != 'refs/heads/main'
        run: |
          echo "::error::Release workflow must be run from the main branch, not ${{ github.ref }}"
          exit 1

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@1.85.0

      - name: Get version
        id: version
        run: |
          VERSION=$(cargo metadata --manifest-path crates/cupel/Cargo.toml --format-version 1 --no-deps | jq -r '.packages[0].version')
          if [ -z "$VERSION" ] || [ "$VERSION" = "null" ]; then
            echo "::error::Failed to extract version from Cargo.toml"
            exit 1
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Authenticate with crates.io
        uses: rust-lang/crates-io-auth-action@v1
        id: crates-io-auth
        continue-on-error: true

      - name: Publish to crates.io
        run: cargo publish --manifest-path crates/cupel/Cargo.toml
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token || secrets.CARGO_REGISTRY_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: rust-v${{ steps.version.outputs.version }}
          name: cupel rust-v${{ steps.version.outputs.version }}
          generate_release_notes: true
          make_latest: true
