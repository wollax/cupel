---
phase: 01-project-scaffold-core-models
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - benchmarks/Wollax.Cupel.Benchmarks/EmptyPipelineBenchmark.cs
  - src/Wollax.Cupel/PublicAPI.Unshipped.txt
autonomous: true

must_haves:
  truths:
    - "BenchmarkDotNet benchmark compiles and runs with ContextItem instances"
    - "PublicAPI.Unshipped.txt contains all public API entries — solution builds with zero RS0016 warnings"
    - "dotnet list Wollax.Cupel package shows zero runtime dependencies"
    - "Full solution builds with zero errors and zero warnings"
    - "All tests pass (smart enums, ContextItem, ContextBudget)"
  artifacts:
    - path: "benchmarks/Wollax.Cupel.Benchmarks/EmptyPipelineBenchmark.cs"
      provides: "Baseline benchmark iterating ContextItem array — establishes performance floor"
      contains: "EmptyPipelineBenchmark"
    - path: "src/Wollax.Cupel/PublicAPI.Unshipped.txt"
      provides: "PublicApiAnalyzers tracking file with all Phase 1 public API entries"
      contains: "ContextItem"
  key_links:
    - from: "EmptyPipelineBenchmark"
      to: "ContextItem"
      via: "Benchmark creates and iterates ContextItem[] — proves model works in perf context"
      pattern: "new ContextItem"
    - from: "PublicAPI.Unshipped.txt"
      to: "All public types"
      via: "PublicApiAnalyzers RS0016 — every public member must be listed"
      pattern: "Wollax.Cupel.ContextItem"
---

<objective>
Add the baseline benchmark and populate PublicAPI.Unshipped.txt — the final verification that Phase 1 is complete.

Purpose: The benchmark establishes the performance floor that all future pipeline benchmarks compare against. PublicAPI.Unshipped.txt ensures PublicApiAnalyzers is actually working and tracking the API surface. This plan also serves as the integration gate — if everything builds clean, Phase 1 requirements are met.

Output: A runnable EmptyPipelineBenchmark that iterates ContextItem arrays, a populated PublicAPI.Unshipped.txt with all public API entries, and a fully green build.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/01-project-scaffold-core-models/01-RESEARCH.md (sections: BenchmarkDotNet Empty Baseline, PublicAPI Text Files, Pitfall 3 — PublicApiAnalyzers Missing Nullable Header, Pitfall 6 — BenchmarkDotNet TreatWarningsAsErrors)
@.planning/ROADMAP.md (Phase 1 Success Criteria)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create empty-pipeline benchmark baseline</name>
  <files>
    benchmarks/Wollax.Cupel.Benchmarks/EmptyPipelineBenchmark.cs
  </files>
  <action>
    Create `benchmarks/Wollax.Cupel.Benchmarks/EmptyPipelineBenchmark.cs`:

    ```csharp
    using BenchmarkDotNet.Attributes;
    using Wollax.Cupel;

    [MemoryDiagnoser]
    public class EmptyPipelineBenchmark
    {
        private ContextItem[] _items = null!;

        [Params(100, 250, 500)]
        public int ItemCount { get; set; }

        [GlobalSetup]
        public void Setup()
        {
            _items = Enumerable.Range(0, ItemCount)
                .Select(i => new ContextItem
                {
                    Content = $"Item {i}",
                    Tokens = 10
                })
                .ToArray();
        }

        [Benchmark(Baseline = true)]
        public int BaselineIteration()
        {
            var sum = 0;
            foreach (var item in _items)
                sum += item.Tokens;
            return sum;
        }
    }
    ```

    This benchmark establishes the absolute floor — just iterating items and summing tokens. Future pipeline benchmarks will compare against this baseline.

    Verify the benchmark compiles and the BenchmarkDotNet runner lists it: `dotnet run --project benchmarks/Wollax.Cupel.Benchmarks -- --list flat`

    Do NOT run the full benchmark (takes minutes). Just verify it compiles and is discoverable.
  </action>
  <verify>
    `dotnet build benchmarks/Wollax.Cupel.Benchmarks` — zero errors, zero warnings.
    `dotnet run --project benchmarks/Wollax.Cupel.Benchmarks -- --list flat` — lists EmptyPipelineBenchmark.BaselineIteration.
  </verify>
  <done>EmptyPipelineBenchmark compiles and is discoverable by BenchmarkDotNet runner with 3 parameter values (100, 250, 500).</done>
</task>

<task type="auto">
  <name>Task 2: Populate PublicAPI.Unshipped.txt and final verification</name>
  <files>
    src/Wollax.Cupel/PublicAPI.Unshipped.txt
  </files>
  <action>
    1. Build the core project and check for RS0016 warnings — these indicate public API members not listed in PublicAPI.Unshipped.txt:
       `dotnet build src/Wollax.Cupel -warnaserror`

    2. If RS0016 warnings exist (they will — we have public types not yet listed):
       - Use the dotnet format or manually populate `PublicAPI.Unshipped.txt` with all public API entries.
       - The file should list every public member of ContextItem, ContextBudget, ContextKind, and ContextSource.
       - Format: one entry per line, following PublicApiAnalyzers conventions (e.g., `Wollax.Cupel.ContextItem.Content.get -> string`).
       - Keep `#nullable enable` as the first line.

    3. Alternatively, temporarily disable TreatWarningsAsErrors, build, capture the RS0016 output, extract the suggested API entries, add them to PublicAPI.Unshipped.txt, then re-enable TreatWarningsAsErrors and rebuild.

    4. Final verification — run ALL of these and confirm zero issues:
       - `dotnet build Cupel.sln` — zero errors, zero warnings
       - `dotnet test Cupel.sln` — all tests pass
       - `dotnet list src/Wollax.Cupel package` — verify zero runtime dependencies

    5. Remove the smoke test file created in plan 01 (tests/Wollax.Cupel.Tests/SmokeTests.cs) if it still exists — it's superseded by the real model tests.
  </action>
  <verify>
    `dotnet build Cupel.sln` — zero errors, zero warnings (including RS0016).
    `dotnet test Cupel.sln` — all tests pass.
    `dotnet list src/Wollax.Cupel package` — shows only dev-time packages (PublicApiAnalyzers, SourceLink, MinVer).
    `head -1 src/Wollax.Cupel/PublicAPI.Unshipped.txt` — outputs `#nullable enable`.
    `wc -l src/Wollax.Cupel/PublicAPI.Unshipped.txt` — more than 1 line (API entries exist).
  </verify>
  <done>PublicAPI.Unshipped.txt contains all Phase 1 public API entries. Full solution builds with zero warnings. All tests pass. Zero runtime dependencies in core package. Phase 1 is complete.</done>
</task>

</tasks>

<verification>
1. `dotnet build Cupel.sln` — zero errors, zero warnings across all 3 projects
2. `dotnet test Cupel.sln` — all tests pass (smart enums + ContextItem + ContextBudget)
3. `dotnet list src/Wollax.Cupel package` — zero runtime dependencies
4. `dotnet run --project benchmarks/Wollax.Cupel.Benchmarks -- --list flat` — lists the baseline benchmark
5. PublicAPI.Unshipped.txt has `#nullable enable` header and lists all public API members
6. All Phase 1 success criteria from ROADMAP.md are satisfied
</verification>

<success_criteria>
- EmptyPipelineBenchmark exists and is runnable
- PublicAPI.Unshipped.txt accurately tracks all public API surface from Phase 1
- Full solution builds with zero warnings (including PublicApiAnalyzers RS0016)
- All tests pass
- Zero runtime dependencies in Wollax.Cupel
- Phase 1 ROADMAP.md success criteria 1-5 are all met
</success_criteria>

<output>
After completion, create `.planning/phases/pending/01-project-scaffold-core-models/01-05-SUMMARY.md`
</output>
