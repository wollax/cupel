---
phase: 01-project-scaffold-core-models
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/Wollax.Cupel/ContextKind.cs
  - src/Wollax.Cupel/ContextSource.cs
  - tests/Wollax.Cupel.Tests/Models/ContextKindTests.cs
  - tests/Wollax.Cupel.Tests/Models/ContextSourceTests.cs
autonomous: true

must_haves:
  truths:
    - "ContextKind well-known values (Message, Document, ToolOutput, Memory, SystemPrompt) are accessible as static readonly fields"
    - "ContextSource well-known values (Chat, Tool, Rag) are accessible as static readonly fields"
    - "new ContextKind('message') equals ContextKind.Message (case-insensitive)"
    - "ContextKind serializes as plain string 'Message', not {\"Value\":\"Message\"}"
    - "Custom kinds via new ContextKind('custom') work and compare correctly"
    - "Constructing with null or whitespace throws ArgumentException"
  artifacts:
    - path: "src/Wollax.Cupel/ContextKind.cs"
      provides: "Smart enum for context item kind with well-known values and JsonConverter"
      contains: "sealed class ContextKind"
    - path: "src/Wollax.Cupel/ContextSource.cs"
      provides: "Smart enum for context item source with well-known values and JsonConverter"
      contains: "sealed class ContextSource"
    - path: "tests/Wollax.Cupel.Tests/Models/ContextKindTests.cs"
      provides: "TDD tests for ContextKind equality, case-insensitivity, serialization"
    - path: "tests/Wollax.Cupel.Tests/Models/ContextSourceTests.cs"
      provides: "TDD tests for ContextSource equality, case-insensitivity, serialization"
  key_links:
    - from: "ContextKindJsonConverter"
      to: "ContextKind"
      via: "[JsonConverter] attribute on ContextKind class"
      pattern: "JsonConverter.*ContextKindJsonConverter"
    - from: "ContextSourceJsonConverter"
      to: "ContextSource"
      via: "[JsonConverter] attribute on ContextSource class"
      pattern: "JsonConverter.*ContextSourceJsonConverter"
---

<objective>
Implement ContextKind and ContextSource smart enums using TDD — the two extensible type-safe enumerations that ContextItem and ContextBudget depend on.

Purpose: Smart enums are load-bearing types used by every model in the library. Case-insensitive equality, IEquatable<T>, and plain-string JSON serialization must be correct before building models on top.

Output: Two smart enum classes with custom JsonConverters, fully tested for equality, hashing, serialization, extensibility, and input validation.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/01-project-scaffold-core-models/01-RESEARCH.md (sections: Pattern 2 — Smart Enum, Pitfall 5 — Dictionary Key Serialization)
@.planning/phases/pending/01-project-scaffold-core-models/phase-1-CONTEXT.md (sections: Smart enum design, Well-known ContextKind values, Well-known ContextSource values)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — ContextKind smart enum</name>
  <files>
    src/Wollax.Cupel/ContextKind.cs
    tests/Wollax.Cupel.Tests/Models/ContextKindTests.cs
  </files>
  <action>
    Follow RED-GREEN-REFACTOR for ContextKind:

    **RED — Write failing tests first** in `tests/Wollax.Cupel.Tests/Models/ContextKindTests.cs`:
    - Well-known static values exist: ContextKind.Message, Document, ToolOutput, Memory, SystemPrompt
    - Equality is case-insensitive: `new ContextKind("message")` equals `ContextKind.Message`
    - GetHashCode is consistent with Equals (same hash for case-insensitive equal values)
    - `==` and `!=` operators work correctly
    - ToString() returns the Value string
    - Constructor throws ArgumentException for null, empty, and whitespace input
    - Custom kinds work: `new ContextKind("custom")` constructs successfully and compares correctly
    - JSON serialization: `JsonSerializer.Serialize(ContextKind.Message)` produces `"Message"` (plain string, no object wrapper)
    - JSON deserialization: `JsonSerializer.Deserialize<ContextKind>("\"Message\"")` returns a value equal to ContextKind.Message
    - JSON round-trip: serialize then deserialize preserves equality

    Run tests — they must FAIL (ContextKind doesn't exist yet). Commit failing tests.

    **GREEN — Implement ContextKind** in `src/Wollax.Cupel/ContextKind.cs`:
    - Sealed class implementing IEquatable<ContextKind>
    - `public string Value { get; }` property
    - Constructor: `public ContextKind(string value)` — validates with `ArgumentException.ThrowIfNullOrWhiteSpace(value)`, stores value
    - Static readonly well-known values: Message, Document, ToolOutput, Memory, SystemPrompt
    - Equals: case-insensitive via `StringComparison.OrdinalIgnoreCase`
    - GetHashCode: via `StringComparer.OrdinalIgnoreCase.GetHashCode(Value)`
    - `==` and `!=` operator overloads
    - ToString(): returns Value
    - `[JsonConverter(typeof(ContextKindJsonConverter))]` attribute on the class
    - Internal ContextKindJsonConverter: Read creates `new ContextKind(reader.GetString())`, Write outputs `writer.WriteStringValue(value.Value)`

    Run tests — they must PASS. Commit implementation.

    **REFACTOR** if needed — ensure no unnecessary code. Run tests again to confirm.
  </action>
  <verify>
    `dotnet test tests/Wollax.Cupel.Tests` — all ContextKind tests pass.
    `dotnet build Cupel.sln` — zero warnings.
  </verify>
  <done>ContextKind smart enum is fully implemented with case-insensitive equality, 5 well-known values, plain-string JSON serialization, and input validation — all proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: TDD — ContextSource smart enum</name>
  <files>
    src/Wollax.Cupel/ContextSource.cs
    tests/Wollax.Cupel.Tests/Models/ContextSourceTests.cs
  </files>
  <action>
    Follow the same RED-GREEN-REFACTOR pattern as ContextKind:

    **RED — Write failing tests** in `tests/Wollax.Cupel.Tests/Models/ContextSourceTests.cs`:
    - Well-known static values: ContextSource.Chat, Tool, Rag
    - Case-insensitive equality
    - GetHashCode consistency
    - Operator overloads
    - ToString()
    - Constructor validation (null/empty/whitespace throws)
    - Custom sources work
    - JSON serialization as plain string
    - JSON round-trip

    These tests follow the exact same pattern as ContextKindTests but with ContextSource-specific values. Run tests — must FAIL.

    **GREEN — Implement ContextSource** in `src/Wollax.Cupel/ContextSource.cs`:
    - Identical pattern to ContextKind: sealed class, IEquatable<ContextSource>, Value property, constructor validation
    - Static readonly well-known values: Chat, Tool, Rag
    - Internal ContextSourceJsonConverter for plain-string serialization
    - All the same equality/hashing/operator infrastructure

    Run tests — must PASS.

    **REFACTOR** — Consider whether ContextKind and ContextSource share enough to extract a base, but do NOT extract a base class. The ~50 lines per type is acceptable duplication. Smart enums are sealed — no inheritance. If the code is clean, skip refactor.
  </action>
  <verify>
    `dotnet test tests/Wollax.Cupel.Tests` — all ContextSource tests pass alongside ContextKind tests.
    `dotnet build Cupel.sln` — zero warnings.
  </verify>
  <done>ContextSource smart enum is fully implemented with case-insensitive equality, 3 well-known values, plain-string JSON serialization, and input validation — all proven by tests.</done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Wollax.Cupel.Tests` — all smart enum tests pass
2. `dotnet build Cupel.sln` — zero errors, zero warnings
3. JSON serialization of ContextKind.Message produces `"Message"` not `{"Value":"Message"}`
4. `new ContextKind("message") == ContextKind.Message` evaluates to true
5. `new ContextKind("")` throws ArgumentException
</verification>

<success_criteria>
- Both smart enums compile with zero warnings under TreatWarningsAsErrors
- Case-insensitive equality works across all test cases
- JSON round-trip preserves equality for both types
- Custom user-defined values construct and compare correctly
- Constructor rejects invalid input (null, empty, whitespace)
</success_criteria>

<output>
After completion, create `.planning/phases/pending/01-project-scaffold-core-models/01-02-SUMMARY.md`
</output>
