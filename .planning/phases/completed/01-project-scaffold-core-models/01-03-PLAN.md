---
phase: 01-project-scaffold-core-models
plan: 03
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/Wollax.Cupel/ContextItem.cs
  - tests/Wollax.Cupel.Tests/Models/ContextItemTests.cs
autonomous: true

must_haves:
  truths:
    - "ContextItem requires Content (string) and Tokens (int) at construction — omitting either is a compile error"
    - "ContextItem is immutable — all properties are { get; init; }"
    - "ContextItem defaults are sensible: Kind=Message, Source=Chat, Tags=empty, Metadata=empty, Pinned=false"
    - "ContextItem round-trips through JSON with [JsonPropertyName] controlling all property names"
    - "ContextItem supports with-expressions for creating modified copies"
    - "ContextItem has value equality (record semantics)"
  artifacts:
    - path: "src/Wollax.Cupel/ContextItem.cs"
      provides: "Immutable sealed record with all PIPE-01 properties"
      contains: "sealed record ContextItem"
    - path: "tests/Wollax.Cupel.Tests/Models/ContextItemTests.cs"
      provides: "TDD tests for construction, defaults, immutability, JSON serialization, value equality"
  key_links:
    - from: "ContextItem.Kind"
      to: "ContextKind"
      via: "Property type — defaults to ContextKind.Message"
      pattern: "ContextKind Kind"
    - from: "ContextItem.Source"
      to: "ContextSource"
      via: "Property type — defaults to ContextSource.Chat"
      pattern: "ContextSource Source"
---

<objective>
Implement ContextItem as an immutable sealed record using TDD — the core data model that represents a single piece of context in the pipeline.

Purpose: ContextItem is the most-referenced type in the entire library. Every scorer, slicer, and placer operates on ContextItem instances. Getting the shape, defaults, and serialization right here prevents cascading changes in all subsequent phases.

Output: A sealed record with all 11 PIPE-01 properties, sensible defaults, required Content/Tokens, JSON annotations, and comprehensive tests.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/01-project-scaffold-core-models/01-RESEARCH.md (sections: Pattern 1 — Sealed Record with Init Properties, Pitfall 2 — JsonPropertyName on Record Primary Constructor)
@.planning/phases/pending/01-project-scaffold-core-models/phase-1-CONTEXT.md (section: ContextItem property types)
@.planning/REQUIREMENTS.md (PIPE-01, API-05, JSON-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — ContextItem sealed record</name>
  <files>
    src/Wollax.Cupel/ContextItem.cs
    tests/Wollax.Cupel.Tests/Models/ContextItemTests.cs
  </files>
  <action>
    Follow RED-GREEN-REFACTOR for ContextItem:

    **RED — Write failing tests** in `tests/Wollax.Cupel.Tests/Models/ContextItemTests.cs`:

    Construction and required properties:
    - Minimal construction: `new ContextItem { Content = "hello", Tokens = 5 }` compiles and sets both properties
    - Content and Tokens are required — this is a compile-time guarantee (no runtime test needed, but verify values are accessible)

    Default values:
    - Kind defaults to ContextKind.Message
    - Source defaults to ContextSource.Chat
    - Priority defaults to null
    - Tags defaults to empty IReadOnlyList<string>
    - Metadata defaults to empty IReadOnlyDictionary<string, object?>
    - Timestamp defaults to null
    - FutureRelevanceHint defaults to null
    - Pinned defaults to false
    - OriginalTokens defaults to null

    Immutability (with-expression):
    - `item with { Tokens = 10 }` creates a new instance with updated Tokens, original unchanged
    - `item with { Kind = ContextKind.Document }` creates a new instance with updated Kind

    Value equality (record semantics):
    - Two ContextItems with identical properties are equal
    - Two ContextItems with different Content are not equal

    JSON serialization (requires `using System.Text.Json`):
    - Serialized JSON uses camelCase property names from [JsonPropertyName]: "content", "tokens", "kind", "source", "priority", "tags", "metadata", "timestamp", "futureRelevanceHint", "pinned", "originalTokens"
    - Round-trip: serialize then deserialize produces an equal ContextItem
    - Null optional properties are omitted or handled correctly in JSON
    - Kind serializes as plain string (via ContextKind's JsonConverter)

    Full construction:
    - All 11 properties can be set via object initializer

    Run tests — must FAIL.

    **GREEN — Implement ContextItem** in `src/Wollax.Cupel/ContextItem.cs`:
    - `public sealed record ContextItem` (NOT positional — no primary constructor)
    - `[JsonPropertyName("content")] public required string Content { get; init; }`
    - `[JsonPropertyName("tokens")] public required int Tokens { get; init; }`
    - `[JsonPropertyName("kind")] public ContextKind Kind { get; init; } = ContextKind.Message;`
    - `[JsonPropertyName("source")] public ContextSource Source { get; init; } = ContextSource.Chat;`
    - `[JsonPropertyName("priority")] public int? Priority { get; init; }`
    - `[JsonPropertyName("tags")] public IReadOnlyList<string> Tags { get; init; } = [];`
    - `[JsonPropertyName("metadata")] public IReadOnlyDictionary<string, object?> Metadata { get; init; } = new Dictionary<string, object?>();`
    - `[JsonPropertyName("timestamp")] public DateTimeOffset? Timestamp { get; init; }`
    - `[JsonPropertyName("futureRelevanceHint")] public double? FutureRelevanceHint { get; init; }`
    - `[JsonPropertyName("pinned")] public bool Pinned { get; init; }`
    - `[JsonPropertyName("originalTokens")] public int? OriginalTokens { get; init; }`

    Add necessary using statements: `System.Text.Json.Serialization`, `System.Collections.Generic`.

    Run tests — must PASS.

    **REFACTOR** — Verify no unnecessary code. The record should be clean and minimal. Run tests to confirm.
  </action>
  <verify>
    `dotnet test tests/Wollax.Cupel.Tests` — all ContextItem tests pass alongside smart enum tests.
    `dotnet build Cupel.sln` — zero warnings.
    Verify JSON output manually: `JsonSerializer.Serialize(new ContextItem { Content = "test", Tokens = 1 })` contains `"content":"test"` and `"tokens":1` and `"kind":"Message"`.
  </verify>
  <done>ContextItem sealed record compiles with all 11 PIPE-01 properties, required Content/Tokens enforced at compile time, sensible defaults on all optional properties, [JsonPropertyName] on every property, value equality via record semantics, and with-expression support — all proven by tests.</done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Wollax.Cupel.Tests` — all tests pass
2. `dotnet build Cupel.sln` — zero errors, zero warnings
3. ContextItem requires Content and Tokens (compile-time, verified by successful test construction)
4. JSON serialization uses camelCase names from [JsonPropertyName] attributes
5. ContextKind and ContextSource serialize as plain strings within ContextItem JSON
</verification>

<success_criteria>
- ContextItem is a sealed record with { get; init; } on all properties
- Content (string) and Tokens (int) are required — non-nullable, enforced at compile time
- All optional properties have documented defaults
- JSON round-trip works with correct property names
- Value equality and with-expressions work as expected from record semantics
- Requirement PIPE-01, API-05, and JSON-01 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/pending/01-project-scaffold-core-models/01-03-SUMMARY.md`
</output>
