---
phase: 15-conformance-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/conformance/required/slicing/greedy-density.toml
  - spec/conformance/required/slicing/greedy-exact-fit.toml
  - spec/conformance/required/slicing/greedy-zero-tokens.toml
  - spec/conformance/required/slicing/knapsack-basic.toml
  - spec/conformance/required/slicing/knapsack-zero-tokens.toml
  - spec/conformance/required/slicing/quota-basic.toml
  - spec/conformance/required/placing/chronological-basic.toml
  - spec/conformance/required/placing/chronological-null-timestamps.toml
  - spec/conformance/required/placing/u-shaped-basic.toml
  - spec/conformance/required/placing/u-shaped-equal-scores.toml
autonomous: true
must_haves:
  truths:
    - All 6 slicing vectors exist in spec/conformance/required/slicing/
    - All 4 placing vectors exist in spec/conformance/required/placing/
    - quota-basic.toml is in the required tier (not optional) — this is the key conformance promotion
    - Each vector is byte-exact identical to the Rust crate's vendored copy
  artifacts:
    - spec/conformance/required/slicing/*.toml (6 files)
    - spec/conformance/required/placing/*.toml (4 files)
  key_links:
    - Spec slicer algorithms -> TOML vector expected selections
    - Spec placer algorithms -> TOML vector expected orderings
    - quota-basic.toml promotion -> conformance hardening goal
---

<objective>
Author all 6 slicing and 4 placing conformance test vectors in the spec's canonical directory.

Purpose: Complete the slicer and placer coverage in the spec conformance suite. Notably, `quota-basic.toml` being in `required/` (not `optional/`) fulfills the primary conformance hardening goal — promoting QuotaSlice to required tier.

Output: 10 TOML files across `spec/conformance/required/slicing/` and `spec/conformance/required/placing/`, each byte-exact with the Rust crate's vendored copy.
</objective>

<execution_context>
Same authoring approach as Plan 01: read spec algorithms, compute expected outputs, write TOML, diff against Rust crate.

Key note on `quota-basic.toml`: The CONTEXT.md identifies this as the most important vector — the original audit gap was that QuotaSlice conformance was optional. By placing it in `spec/conformance/required/slicing/`, it becomes required for all implementations.

Slicing vectors test selection (which items are chosen given a budget). Placing vectors test ordering (in what order selected items appear in output).
</execution_context>

<context>
@spec/src/slicers/greedy.md
@spec/src/slicers/knapsack.md
@spec/src/slicers/quota.md
@spec/src/placers/chronological.md
@spec/src/placers/u-shaped.md
@spec/src/conformance/format.md
@spec/src/conformance/levels.md
@.planning/phases/pending/15-conformance-hardening/15-CONTEXT.md
@.planning/phases/pending/15-conformance-hardening/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author slicing conformance vectors (6 files)</name>
  <files>
    spec/conformance/required/slicing/greedy-density.toml
    spec/conformance/required/slicing/greedy-exact-fit.toml
    spec/conformance/required/slicing/greedy-zero-tokens.toml
    spec/conformance/required/slicing/knapsack-basic.toml
    spec/conformance/required/slicing/knapsack-zero-tokens.toml
    spec/conformance/required/slicing/quota-basic.toml
  </files>
  <action>
For each of the 6 slicing vectors:
1. Read the spec algorithm chapter for the slicer type (greedy, knapsack, quota)
2. Read the conformance format spec
3. Construct inputs, compute expected selections from first principles
4. Write the TOML file to `spec/conformance/required/slicing/`
5. Diff against Rust crate copy

Slicer coverage:
- Greedy: 3 vectors (density ordering, exact budget fit, zero-token items)
- Knapsack: 2 vectors (basic DP selection, zero-token items)
- Quota: 1 vector (basic require/cap constraints) — THIS is the promoted vector

For quota-basic.toml specifically: verify the proportional distribution math step by step. This vector validates the most complex slicer in the system.

Avoid: Copy-pasting from the Rust crate first.
Why: Independent derivation validates the spec's algorithm descriptions are sufficient for implementation.
  </action>
  <verify>
```bash
for f in spec/conformance/required/slicing/*.toml; do
  base=$(basename "$f")
  cmp -s "$f" "/Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance/required/slicing/$base" \
    && echo "OK: $base" \
    || echo "MISMATCH: $base"
done
```
  </verify>
  <done>All 6 slicing TOML files exist and are byte-exact with Rust crate copies.</done>
</task>

<task type="auto">
  <name>Task 2: Author placing conformance vectors (4 files)</name>
  <files>
    spec/conformance/required/placing/chronological-basic.toml
    spec/conformance/required/placing/chronological-null-timestamps.toml
    spec/conformance/required/placing/u-shaped-basic.toml
    spec/conformance/required/placing/u-shaped-equal-scores.toml
  </files>
  <action>
For each of the 4 placing vectors:
1. Read the spec algorithm chapter for the placer type (chronological, u-shaped)
2. Read the conformance format spec
3. Construct inputs, compute expected orderings from first principles
4. Write the TOML file to `spec/conformance/required/placing/`
5. Diff against Rust crate copy

Placer coverage:
- Chronological: 2 vectors (basic timestamp ordering, null timestamp handling)
- U-Shaped: 2 vectors (basic score-based placement, equal-score tiebreaking)

These are simpler than scoring/slicing vectors — placing is purely about ordering.

Avoid: Copy-pasting from the Rust crate first.
  </action>
  <verify>
```bash
for f in spec/conformance/required/placing/*.toml; do
  base=$(basename "$f")
  cmp -s "$f" "/Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance/required/placing/$base" \
    && echo "OK: $base" \
    || echo "MISMATCH: $base"
done
```
  </verify>
  <done>All 4 placing TOML files exist and are byte-exact with Rust crate copies.</done>
</task>

</tasks>

<verification>
```bash
# Count slicing vectors
find spec/conformance/required/slicing -name "*.toml" | wc -l  # Expected: 6

# Count placing vectors
find spec/conformance/required/placing -name "*.toml" | wc -l  # Expected: 4

# Full byte-exact diff
diff -r spec/conformance/required/slicing/ /Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance/required/slicing/
diff -r spec/conformance/required/placing/ /Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance/required/placing/
# Expected: no output (identical)
```
</verification>

<success_criteria>
- 6 TOML files exist in `spec/conformance/required/slicing/`
- 4 TOML files exist in `spec/conformance/required/placing/`
- `diff -r` against Rust crate directories produces zero differences
- `quota-basic.toml` is in the `required/slicing/` directory (not optional)
</success_criteria>

<output>
After completion, create `.planning/phases/pending/15-conformance-hardening/15-02-SUMMARY.md`
</output>
