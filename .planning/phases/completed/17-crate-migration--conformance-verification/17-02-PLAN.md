---
phase: 17-crate-migration--conformance-verification
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/cupel/tests/conformance.rs
  - crates/cupel/tests/conformance/pipeline.rs
  - crates/cupel/tests/conformance/placing.rs
  - crates/cupel/tests/conformance/scoring.rs
  - crates/cupel/tests/conformance/slicing.rs
  - .githooks/pre-commit
autonomous: true
must_haves:
  truths:
    - 5 test .rs files exist at crates/cupel/tests/ with correct module structure
    - All imports changed from assay_cupel to cupel (8 replacements across 5 files)
    - Vector path in conformance.rs resolves to CARGO_MANIFEST_DIR/conformance/required/ (not tests/conformance/required/)
    - cargo test passes with 28 conformance vectors
    - Pre-commit hook blocks commits when conformance vectors diverge
    - cargo package --list includes all 28 .toml conformance vectors
    - Unpacked tarball passes cargo test
  artifacts:
    - crates/cupel/tests/conformance.rs (updated imports + path)
    - crates/cupel/tests/conformance/pipeline.rs (updated imports)
    - crates/cupel/tests/conformance/placing.rs (updated imports)
    - crates/cupel/tests/conformance/scoring.rs (updated imports)
    - crates/cupel/tests/conformance/slicing.rs (updated imports)
    - .githooks/pre-commit (conformance vector diff guard)
  key_links:
    - conformance.rs vector path must use .join("conformance").join("required") NOT .join("tests").join("conformance").join("required")
    - All 7 assay_cupel references in test files must become cupel
    - Pre-commit hook must compare conformance/required/ against crates/cupel/conformance/required/
    - cargo package tarball must include conformance vectors for tarball test verification
---

<objective>
Migrate the 5 conformance test files, update imports and vector paths, set up the pre-commit diff guard hook, and verify the complete crate passes all tests and packages correctly.

Purpose: Completes the migration by wiring up tests and verification infrastructure. After this plan, the crate is fully functional and ready for CI setup (Phase 18).
Output: A crate that passes cargo test (28 vectors), cargo package, and has a pre-commit hook preventing conformance vector drift.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/17-crate-migration--conformance-verification/17-CONTEXT.md
@.planning/phases/pending/17-crate-migration--conformance-verification/17-RESEARCH.md
@crates/cupel/Cargo.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Copy test files and update imports and vector path</name>
  <files>
    crates/cupel/tests/conformance.rs
    crates/cupel/tests/conformance/pipeline.rs
    crates/cupel/tests/conformance/placing.rs
    crates/cupel/tests/conformance/scoring.rs
    crates/cupel/tests/conformance/slicing.rs
  </files>
  <action>
    1. Copy test files from assay into the cupel crate:
       - `cp /Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance.rs crates/cupel/tests/conformance.rs`
       - `mkdir -p crates/cupel/tests/conformance`
       - `cp /Users/wollax/Git/personal/assay/crates/assay-cupel/tests/conformance/{pipeline,placing,scoring,slicing}.rs crates/cupel/tests/conformance/`

    2. Update imports in all 4 files that reference `assay_cupel` (7 total replacements):
       - conformance.rs line 13: `use assay_cupel::{...}` → `use cupel::{...}` (1 replacement)
       - pipeline.rs: `use assay_cupel::{...}` → `use cupel::{...}` AND 3 qualified paths `assay_cupel::Scorer` / `assay_cupel::CompositeScorer` → `cupel::Scorer` / `cupel::CompositeScorer` (4 replacements)
       - placing.rs: `use assay_cupel::{...}` → `use cupel::{...}` (1 replacement)
       - scoring.rs: `use assay_cupel::{...}` → `use cupel::{...}` (1 replacement)
       - slicing.rs: `use assay_cupel::{...}` → `use cupel::{...}` (1 replacement)

    3. Update vector path in conformance.rs (lines 22-25):
       BEFORE:
       ```rust
       let base = Path::new(env!("CARGO_MANIFEST_DIR"))
           .join("tests")
           .join("conformance")
           .join("required");
       ```
       AFTER:
       ```rust
       let base = Path::new(env!("CARGO_MANIFEST_DIR"))
           .join("conformance")
           .join("required");
       ```
       Remove the `.join("tests")` call.

    4. Verify no remaining references to `assay_cupel`:
       `grep -r 'assay_cupel' crates/cupel/tests/` should return nothing
  </action>
  <verify>
    - `find crates/cupel/tests -name '*.rs' | wc -l` returns 5
    - `grep -r 'assay_cupel' crates/cupel/tests/` returns nothing
    - `grep -c 'use cupel::' crates/cupel/tests/conformance.rs` returns 1
    - `grep 'join("tests")' crates/cupel/tests/conformance.rs` returns nothing
  </verify>
  <done>5 test files exist with all assay_cupel references replaced by cupel, and vector path updated to conformance/required/</done>
</task>

<task type="auto">
  <name>Task 2: Set up pre-commit hook for conformance vector diff guard</name>
  <files>
    .githooks/pre-commit
  </files>
  <action>
    1. Create the hooks directory: `mkdir -p .githooks`
    2. Create `.githooks/pre-commit` with the following content:
       ```bash
       #!/usr/bin/env bash
       set -euo pipefail

       # Guard: conformance vectors must stay in sync
       if ! diff -rq conformance/required/ crates/cupel/conformance/required/ >/dev/null 2>&1; then
           echo "ERROR: Conformance vectors are out of sync."
           echo "  canonical: conformance/required/"
           echo "  crate-local: crates/cupel/conformance/required/"
           echo ""
           diff -r conformance/required/ crates/cupel/conformance/required/ || true
           echo ""
           echo "Copy canonical vectors to crate-local before committing."
           exit 1
       fi
       ```
    3. Make it executable: `chmod +x .githooks/pre-commit`
    4. Configure git to use the hooks directory: `git config core.hooksPath .githooks`
    5. Verify the hook runs and passes (vectors should be in sync from Plan 01):
       - Temporarily modify a crate-local vector, verify hook blocks
       - Revert the change, verify hook passes
  </action>
  <verify>
    - `.githooks/pre-commit` exists and is executable
    - `git config core.hooksPath` returns `.githooks`
    - Hook passes when vectors are in sync
  </verify>
  <done>Pre-commit hook exists, is executable, is activated via git config, and correctly blocks commits when conformance vectors diverge</done>
</task>

<task type="auto">
  <name>Task 3: Full verification — tests, packaging, and tarball round-trip</name>
  <files>(no files modified — verification only, unless fixes are needed)</files>
  <action>
    1. Run `cargo test --manifest-path crates/cupel/Cargo.toml` — all 28 conformance tests must pass
    2. Run `cargo fmt --manifest-path crates/cupel/Cargo.toml --check` — must pass
    3. Run `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` — must pass
    4. Run `cargo package --list --manifest-path crates/cupel/Cargo.toml` and verify:
       - All 28 `.toml` conformance vectors appear in the listing
       - `grep -c '\.toml$'` on the output should return 29 (28 vectors + Cargo.toml.orig)
    5. Tarball round-trip verification:
       - `cd crates/cupel && cargo package --allow-dirty` (allow-dirty because files may not be committed yet)
       - Extract the tarball: `tar xf ../../target/package/cupel-1.0.0.crate -C /tmp/`
       - Run tests inside the unpacked crate: `cd /tmp/cupel-1.0.0 && cargo test`
       - Clean up: `rm -rf /tmp/cupel-1.0.0`
    6. If any step fails, diagnose and fix the actual issue (do NOT weaken tests or checks)
  </action>
  <verify>
    - `cargo test` exit 0 with 28 tests passing
    - `cargo fmt --check` exit 0
    - `cargo clippy -- -D warnings` exit 0
    - `cargo package --list | grep -c 'conformance/.*\.toml'` returns 28
    - Unpacked tarball `cargo test` exit 0
  </verify>
  <done>All conformance tests pass, all lint checks pass, cargo package includes all vectors, and unpacked tarball passes tests independently</done>
</task>
</tasks>

<verification>
1. `cargo test --manifest-path crates/cupel/Cargo.toml` — 28 tests pass
2. `cargo fmt --manifest-path crates/cupel/Cargo.toml --check` — exit 0
3. `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` — exit 0
4. `cargo package --list --manifest-path crates/cupel/Cargo.toml | grep -c 'conformance/.*\.toml'` — returns 28
5. Unpacked tarball `cargo test` — exit 0
6. `grep -r 'assay_cupel' crates/cupel/` — returns nothing
7. `.githooks/pre-commit` exists and is executable
8. `git config core.hooksPath` returns `.githooks`
</verification>

<success_criteria>
- All 28 conformance tests pass via cargo test
- cargo fmt --check and cargo clippy -- -D warnings both pass
- cargo package --list shows all 28 conformance vectors in the tarball
- Unpacked tarball passes cargo test independently
- Pre-commit hook blocks commits when conformance vectors diverge
- Zero references to assay_cupel remain anywhere in the crate
</success_criteria>

<output>
After completion, create `.planning/phases/17-crate-migration--conformance-verification/17-02-SUMMARY.md`
</output>
