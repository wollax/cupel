---
phase: 14-policy-type-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Wollax.Cupel/ScorerType.cs
  - src/Wollax.Cupel/SlicerType.cs
  - src/Wollax.Cupel/ScorerEntry.cs
  - src/Wollax.Cupel/CupelPolicy.cs
  - src/Wollax.Cupel/PipelineBuilder.cs
  - tests/Wollax.Cupel.Tests/**
autonomous: true
source_issue: ""

must_haves:
  truths:
    - ScorerType.Scaled enum value exists and serializes as "scaled"
    - SlicerType.Stream enum value exists and serializes as "stream"
    - ScorerEntry accepts InnerScorer property for Scaled type, validated at construction
    - CupelPolicy accepts StreamBatchSize property for Stream slicer, validated at construction
    - PipelineBuilder.WithPolicy creates ScaledScorer from nested ScorerEntry
    - PipelineBuilder.WithPolicy sets both async StreamSlice and sync GreedySlice fallback for Stream slicer
    - All existing tests continue to pass
  artifacts:
    - src/Wollax.Cupel/ScorerType.cs (Scaled member added)
    - src/Wollax.Cupel/SlicerType.cs (Stream member added)
    - src/Wollax.Cupel/ScorerEntry.cs (InnerScorer property, constructor validation)
    - src/Wollax.Cupel/CupelPolicy.cs (StreamBatchSize property, constructor validation)
    - src/Wollax.Cupel/PipelineBuilder.cs (CreateScorer + WithPolicy switch cases)
    - tests/Wollax.Cupel.Tests/ (new test files for enum, ScorerEntry, CupelPolicy, PipelineBuilder changes)
  key_links:
    - ScorerType.Scaled → ScorerEntry.InnerScorer → PipelineBuilder.CreateScorer recursive call → ScaledScorer
    - SlicerType.Stream → CupelPolicy.StreamBatchSize → PipelineBuilder.WithPolicy → WithAsyncSlicer(StreamSlice) + UseGreedySlice()
---

<objective>
Add `ScorerType.Scaled` and `SlicerType.Stream` enum values, extend `ScorerEntry` and `CupelPolicy` with the properties needed to configure them declaratively, and wire `PipelineBuilder` to construct the corresponding implementations from policy data.

Purpose: Close the integration gap where ScaledScorer and StreamSlice are only reachable via manual builder API, not from the declarative CupelPolicy path.
Output: Extended enums, model classes, and builder wiring with full test coverage.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/14-policy-type-completeness/14-CONTEXT.md
@.planning/phases/pending/14-policy-type-completeness/14-RESEARCH.md

@src/Wollax.Cupel/ScorerType.cs
@src/Wollax.Cupel/SlicerType.cs
@src/Wollax.Cupel/ScorerEntry.cs
@src/Wollax.Cupel/CupelPolicy.cs
@src/Wollax.Cupel/PipelineBuilder.cs
@src/Wollax.Cupel/Scoring/ScaledScorer.cs
@src/Wollax.Cupel/Slicing/StreamSlice.cs
@src/Wollax.Cupel/CupelPipeline.cs
@tests/Wollax.Cupel.Tests/ScorerEntryTests.cs
@tests/Wollax.Cupel.Tests/CupelPolicyTests.cs
@tests/Wollax.Cupel.Tests/PipelineBuilderTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum extensions and model class changes</name>
  <files>
    src/Wollax.Cupel/ScorerType.cs
    src/Wollax.Cupel/SlicerType.cs
    src/Wollax.Cupel/ScorerEntry.cs
    src/Wollax.Cupel/CupelPolicy.cs
  </files>
  <action>
1. Add `Scaled` to `ScorerType` enum at the end with explicit value and `[JsonStringEnumMemberName("scaled")]`:
   ```csharp
   /// <summary>Wraps another scorer and normalizes its output to [0, 1].</summary>
   [JsonStringEnumMemberName("scaled")]
   Scaled = 6
   ```

2. Add `Stream` to `SlicerType` enum at the end with explicit value and `[JsonStringEnumMemberName("stream")]`:
   ```csharp
   /// <summary>Online streaming selection via configurable micro-batches.</summary>
   [JsonStringEnumMemberName("stream")]
   Stream = 2
   ```

3. Extend `ScorerEntry`:
   - Add `ScorerEntry? InnerScorer` read-only property with `[JsonPropertyName("innerScorer")]`
   - Add `innerScorer` optional parameter (default null) to the `[JsonConstructor]` constructor
   - Add validation: when `Type == ScorerType.Scaled`, `innerScorer` must not be null (throw ArgumentException)
   - Add validation: when `Type != ScorerType.Scaled` and `innerScorer` is not null, throw ArgumentException
   - Store as-is (no defensive copy needed — ScorerEntry is sealed, not a collection)

4. Extend `CupelPolicy`:
   - Add `int? StreamBatchSize` read-only property with `[JsonPropertyName("streamBatchSize")]`
   - Add `streamBatchSize` optional parameter (default null) to the `[JsonConstructor]` constructor
   - Add validation: when `streamBatchSize` is not null and `slicerType != SlicerType.Stream`, throw ArgumentException (same pattern as knapsackBucketSize)
   - Add validation: when `streamBatchSize` is not null, ThrowIfNegativeOrZero (same pattern as knapsackBucketSize)
   - Add validation: when both quotas and slicerType == Stream, throw ArgumentException — QuotaSlice wraps ISlicer (sync) and cannot wrap StreamSlice (IAsyncSlicer)

Important: Do NOT change the existing enum values' integer assignments. Existing values keep their implicit ordinals. Only the new values get explicit assignments.
  </action>
  <verify>
    `rtk dotnet build src/Wollax.Cupel/` compiles with zero warnings. Existing tests still pass: `rtk dotnet test tests/Wollax.Cupel.Tests/ --treenode-filter "/**/ScorerEntryTests/**"` and `rtk dotnet test tests/Wollax.Cupel.Tests/ --treenode-filter "/**/CupelPolicyTests/**"`.
  </verify>
  <done>
    - ScorerType has 7 members (Recency through Scaled)
    - SlicerType has 3 members (Greedy, Knapsack, Stream)
    - ScorerEntry constructor accepts and validates InnerScorer
    - CupelPolicy constructor accepts and validates StreamBatchSize
    - Quota + Stream combination rejected at construction time
    - All existing tests pass (Enum.IsDefined handles new values automatically)
  </done>
</task>

<task type="auto">
  <name>Task 2: PipelineBuilder wiring and tests</name>
  <files>
    src/Wollax.Cupel/PipelineBuilder.cs
    tests/Wollax.Cupel.Tests/ (new or extended test files)
  </files>
  <action>
1. Extend `CreateScorer` switch in PipelineBuilder:
   ```csharp
   ScorerType.Scaled => new ScaledScorer(CreateScorer(entry.InnerScorer
       ?? throw new InvalidOperationException("InnerScorer is required for Scaled type."))),
   ```
   The null check is defensive — ScorerEntry constructor already validates this.

2. Extend `WithPolicy` slicer switch to handle `SlicerType.Stream`:
   ```csharp
   case SlicerType.Stream:
       UseGreedySlice(); // sync fallback for Execute()
       WithAsyncSlicer(new StreamSlice(policy.StreamBatchSize ?? 32));
       break;
   ```
   This sets both sync (GreedySlice) and async (StreamSlice) slicers. Sync Execute() uses GreedySlice. Async ExecuteStreamAsync() uses StreamSlice.

3. Write tests covering:
   - ScorerEntry with Type=Scaled and valid InnerScorer succeeds
   - ScorerEntry with Type=Scaled and null InnerScorer throws ArgumentException
   - ScorerEntry with Type!=Scaled and non-null InnerScorer throws ArgumentException
   - ScorerEntry with nested Scaled(Scaled(Recency)) validates (deep nesting allowed)
   - CupelPolicy with SlicerType.Stream and StreamBatchSize succeeds
   - CupelPolicy with SlicerType.Stream and null StreamBatchSize succeeds (uses default)
   - CupelPolicy with SlicerType.Stream and StreamBatchSize=0 throws
   - CupelPolicy with SlicerType.Greedy and StreamBatchSize=10 throws
   - CupelPolicy with SlicerType.Stream and quotas throws
   - PipelineBuilder.WithPolicy with Scaled scorer creates working pipeline (end-to-end: items scored, ScaledScorer normalization observable)
   - PipelineBuilder.WithPolicy with Stream slicer creates pipeline usable for both sync Execute and async ExecuteStreamAsync

Follow existing test patterns (TUnit, ordinal assertions, `await Assert.That(...)`).
  </action>
  <verify>
    `rtk dotnet test tests/Wollax.Cupel.Tests/` — all tests pass including new ones. `rtk dotnet build` — zero warnings across solution.
  </verify>
  <done>
    - CreateScorer handles ScorerType.Scaled with recursive construction
    - WithPolicy handles SlicerType.Stream with dual slicer setup
    - Comprehensive tests for all new model validations and builder wiring
    - All existing tests continue to pass
  </done>
</task>

</tasks>

<verification>
- `rtk dotnet build` — zero warnings across entire solution
- `rtk dotnet test tests/Wollax.Cupel.Tests/` — all tests pass
- Manual spot-check: `new ScorerEntry(ScorerType.Scaled, 1.0, innerScorer: new ScorerEntry(ScorerType.Recency, 1.0))` compiles and validates
- Manual spot-check: `new CupelPolicy([...], slicerType: SlicerType.Stream, streamBatchSize: 16)` compiles and validates
</verification>

<success_criteria>
- ScorerType.Scaled and SlicerType.Stream enum values exist with correct JSON serialization names
- ScorerEntry.InnerScorer property works for Scaled scorer nesting
- CupelPolicy.StreamBatchSize property works for Stream slicer configuration
- PipelineBuilder.WithPolicy correctly constructs ScaledScorer and StreamSlice from policy data
- QuotaSlice + StreamSlice combination is rejected at CupelPolicy construction time
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-policy-type-completeness/14-01-SUMMARY.md`
</output>
