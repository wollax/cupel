---
phase: 25
plan: "01"
title: "Fix misleading comments in conformance vector TOML files"
wave: 1
depends_on: []
files_modified:
  - spec/conformance/required/slicing/knapsack-basic.toml
  - crates/cupel/conformance/required/slicing/knapsack-basic.toml
  - spec/conformance/required/scoring/composite-weighted.toml
  - crates/cupel/conformance/required/scoring/composite-weighted.toml
  - spec/conformance/required/pipeline/pinned-items.toml
  - crates/cupel/conformance/required/pipeline/pinned-items.toml
  - spec/conformance/required/placing/u-shaped-basic.toml
  - crates/cupel/conformance/required/placing/u-shaped-basic.toml
  - spec/conformance/required/placing/u-shaped-equal-scores.toml
  - crates/cupel/conformance/required/placing/u-shaped-equal-scores.toml
autonomous: true
must_haves:
  truths:
    - "All five conformance vector TOML files have accurate, non-misleading comments"
    - "spec/ and crates/ copies are byte-identical after fixes"
  artifacts:
    - path: "spec/conformance/required/slicing/knapsack-basic.toml"
      provides: "Knapsack vector with abandoned first scenario deleted (lines 6-28 removed)"
      min_lines: 20
    - path: "spec/conformance/required/scoring/composite-weighted.toml"
      provides: "Composite weighted vector with scratchpad text removed (lines 24-28)"
      min_lines: 55
    - path: "spec/conformance/required/pipeline/pinned-items.toml"
      provides: "Pinned items vector with density-sort step added and score source clarified"
      min_lines: 35
    - path: "spec/conformance/required/placing/u-shaped-basic.toml"
      provides: "U-shaped basic vector with result[N] notation replacing left[N]/right[N]"
      min_lines: 12
    - path: "spec/conformance/required/placing/u-shaped-equal-scores.toml"
      provides: "U-shaped equal-scores vector with result[N] notation replacing left[N]/right[N]"
      min_lines: 12
  key_links:
    - from: "spec/conformance/required/"
      to: "crates/cupel/conformance/required/"
      via: "Byte-identical copies — every edit applied to both directories"
---

<objective>
Fix misleading and incorrect comments in five conformance vector TOML files. These files contain scratchpad thinking, abandoned scenarios, and ambiguous notation left over from initial authoring. Fixes are applied to both the spec/ canonical copy and the crates/ vendored copy simultaneously to maintain byte-identity. This satisfies CONF-01.
</objective>

<context>
@spec/conformance/required/slicing/knapsack-basic.toml
@spec/conformance/required/scoring/composite-weighted.toml
@spec/conformance/required/pipeline/pinned-items.toml
@spec/conformance/required/placing/u-shaped-basic.toml
@spec/conformance/required/placing/u-shaped-equal-scores.toml
@.planning/phases/pending/25-conformance-infrastructure-drift-guard/25-RESEARCH.md
</context>

<task type="auto">
  <name>Task 1: Fix knapsack-basic.toml and composite-weighted.toml comments</name>
  <files>
    spec/conformance/required/slicing/knapsack-basic.toml,
    crates/cupel/conformance/required/slicing/knapsack-basic.toml,
    spec/conformance/required/scoring/composite-weighted.toml,
    crates/cupel/conformance/required/scoring/composite-weighted.toml
  </files>
  <action>
    **knapsack-basic.toml:** Delete the abandoned first scenario entirely (lines 6–28). These lines contain the initial attempt that concluded "same as greedy in this case" followed by "Let me redesign for a genuine knapsack-vs-greedy difference." Keep only the redesigned scenario comment block (lines 30–53) which correctly documents the actual test data. The comment block should start immediately after line 5 (`slicer = "knapsack"`).

    **composite-weighted.toml:** Remove the scratchpad self-correction text at lines 24–28 (the paragraph starting with `# Wait — let me recompute priority.`). The priority computation above it (lines 19–23) already shows the correct values — the "Wait" paragraph is a leftover think-aloud that re-derives the same conclusion. Keep lines 19–23 as-is.

    Apply identical edits to both the `spec/conformance/required/` and `crates/cupel/conformance/required/` copies.
  </action>
  <verify>
    ```bash
    diff spec/conformance/required/slicing/knapsack-basic.toml crates/cupel/conformance/required/slicing/knapsack-basic.toml
    diff spec/conformance/required/scoring/composite-weighted.toml crates/cupel/conformance/required/scoring/composite-weighted.toml
    ```
    Both diffs should produce no output (byte-identical).
    Verify knapsack-basic.toml no longer contains "same as greedy in this case" or "Let me redesign".
    Verify composite-weighted.toml no longer contains "Wait —".
  </verify>
  <done>
    knapsack-basic.toml has only the redesigned scenario comment. composite-weighted.toml has no scratchpad self-correction text. Both spec/ and crates/ copies are identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix pinned-items.toml and u-shaped vector comments</name>
  <files>
    spec/conformance/required/pipeline/pinned-items.toml,
    crates/cupel/conformance/required/pipeline/pinned-items.toml,
    spec/conformance/required/placing/u-shaped-basic.toml,
    crates/cupel/conformance/required/placing/u-shaped-basic.toml,
    spec/conformance/required/placing/u-shaped-equal-scores.toml,
    crates/cupel/conformance/required/placing/u-shaped-equal-scores.toml
  </files>
  <action>
    **pinned-items.toml:** In the greedy slicing comment section (around lines 21–24), insert a density-sort step before the fill trace. The current comment jumps straight to selection order without showing density calculation. Add density values and sort order, matching the pattern used in `greedy-density.toml` and `greedy-exact-fit.toml`. Specifically, add before the fill trace:
    ```
    #   Density sort:
    #     new: 1.0/150 ≈ 0.00667
    #     old: 0.0/150 = 0.0
    #   Sorted: new, old
    ```
    Also clarify that the score values in the Slice section come from the Score step above (the "1.0" and "0.0" densities use the scored values, not raw).

    **u-shaped-basic.toml:** Replace `left[N]`/`right[N]` notation with `result[N]` notation:
    ```
    # Rank 0 (A) → result[0]
    # Rank 1 (B) → result[4]
    # Rank 2 (C) → result[1]
    # Rank 3 (D) → result[3]
    # Rank 4 (E) → result[2]
    ```

    **u-shaped-equal-scores.toml:** Replace `left[N]`/`right[N]` notation with `result[N]` notation:
    ```
    # Rank 0 (X, index 0) → result[0]
    # Rank 1 (Y, index 1) → result[3]
    # Rank 2 (Z, index 2) → result[1]
    # Rank 3 (W, index 3) → result[2]
    ```

    Apply identical edits to both the `spec/conformance/required/` and `crates/cupel/conformance/required/` copies.
  </action>
  <verify>
    ```bash
    diff spec/conformance/required/pipeline/pinned-items.toml crates/cupel/conformance/required/pipeline/pinned-items.toml
    diff spec/conformance/required/placing/u-shaped-basic.toml crates/cupel/conformance/required/placing/u-shaped-basic.toml
    diff spec/conformance/required/placing/u-shaped-equal-scores.toml crates/cupel/conformance/required/placing/u-shaped-equal-scores.toml
    ```
    All three diffs produce no output. Verify no `left[` or `right[` references remain in u-shaped files. Verify pinned-items.toml contains "Density sort" in the comment.
  </verify>
  <done>
    pinned-items.toml has density-sort step in comment. Both u-shaped vectors use result[N] notation. All spec/ and crates/ copies are byte-identical.
  </done>
</task>

<verification>
## Phase Contribution
Satisfies CONF-01: all five misleading/incorrect comment blocks are corrected in both canonical and vendored copies.

## Success Criteria
- [ ] knapsack-basic.toml: abandoned first scenario deleted (no "Let me redesign" text)
- [ ] composite-weighted.toml: scratchpad "Wait —" paragraph removed
- [ ] pinned-items.toml: density-sort step present in greedy section
- [ ] u-shaped-basic.toml: uses result[N] notation (no left[N]/right[N])
- [ ] u-shaped-equal-scores.toml: uses result[N] notation (no left[N]/right[N])
- [ ] All five files: spec/ and crates/ copies are byte-identical
- [ ] `cargo test --manifest-path crates/cupel/Cargo.toml` still passes (comments only — no data change)
</verification>
