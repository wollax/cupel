---
phase: 24-diagnostics-spec-chapter
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - spec/src/diagnostics/exclusion-reasons.md
  - spec/src/diagnostics/selection-report.md
  - spec/src/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "ExclusionReason has exactly 8 DATA-CARRYING variants with fields (not fieldless enums)"
    - "BudgetExceeded carries item_tokens and available_tokens fields"
    - "Reserved variants (ScoredTooLow, QuotaCapExceeded, QuotaRequireDisplaced, Filtered) are documented with 'reserved — not emitted by built-in stages' status"
    - "InclusionReason has exactly 3 variants (Scored, Pinned, ZeroToken)"
    - "SelectionReport has fields: events, included, excluded, total_candidates, total_tokens_considered"
    - "Excluded list sort invariant is documented: score descending, stable by insertion order on ties"
    - "IncludedItem and ExcludedItem types have field tables and JSON examples"
    - "ExcludedItem JSON example shows deduplicated_against present for Deduplicated and absent for other reasons"
    - "SUMMARY.md updated with Diagnostics chapter entry after Placers block, before # Conformance"
    - "All JSON examples use snake_case field names and omit absent fields (no nulls)"
    - "MUST keyword appears only in Conformance Notes sections"
    - "No C#, Rust, or language-specific syntax appears anywhere"
    - "mdBook can build the spec without errors after SUMMARY.md update"
  artifacts:
    - path: "spec/src/diagnostics/exclusion-reasons.md"
      provides: "ExclusionReason (8 data-carrying variants), InclusionReason (3 variants)"
      contains: "BudgetExceeded"
    - path: "spec/src/diagnostics/selection-report.md"
      provides: "SelectionReport, IncludedItem, ExcludedItem types with field tables and JSON examples"
      contains: "total_candidates"
    - path: "spec/src/SUMMARY.md"
      provides: "Diagnostics chapter registered in book navigation"
      contains: "[Diagnostics](diagnostics.md)"
  key_links:
    - "ExclusionReason variants MUST be data-carrying per user decision — this overrides the research recommendation for fieldless variants. If implemented as fieldless, the spec will be wrong."
    - "Excluded list sort invariant (score descending, stable insertion order) is conformance-testable — if omitted, implementations will diverge"
    - "SUMMARY.md entry placement must be after Placers and before # Conformance — wrong placement breaks the book's navigation hierarchy"
---

<objective>
Create the remaining two diagnostics sub-pages (Exclusion Reasons and SelectionReport) and register the complete chapter in SUMMARY.md.

Purpose: Complete the diagnostics spec chapter by defining the "consumer-side" types — the reason enums and the report structure that callers inspect after a pipeline run. Register the chapter in the book navigation so it's browsable.

Output: Two new markdown files and one updated file. The diagnostics spec chapter is complete and navigable.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/pending/24-diagnostics-spec-chapter/24-CONTEXT.md
@.planning/phases/pending/24-diagnostics-spec-chapter/24-RESEARCH.md

@spec/src/SUMMARY.md
@spec/src/diagnostics.md
@spec/src/diagnostics/trace-collector.md
@spec/src/diagnostics/events.md
@spec/src/data-model/context-item.md
@spec/src/data-model/enumerations.md
@spec/src/pipeline/classify.md
@spec/src/pipeline/slice.md
@spec/src/pipeline/place.md
@spec/src/pipeline/deduplicate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exclusion-reasons.md and selection-report.md</name>
  <files>
    spec/src/diagnostics/exclusion-reasons.md
    spec/src/diagnostics/selection-report.md
  </files>
  <action>
    **exclusion-reasons.md** — Structure:

    ```
    # Exclusion Reasons
    ## Overview
    ## ExclusionReason
    ## InclusionReason
    ## Conformance Notes
    ```

    Content:

    1. **Overview** — Every item considered by the pipeline receives either an inclusion or exclusion reason. These reasons form the explainability core of the diagnostics system — they answer "why was this item included/excluded?" Cross-reference: reasons are attached to items in the [SelectionReport](selection-report.md).

    2. **ExclusionReason** — **CRITICAL: Variants are DATA-CARRYING, not fieldless.** Each variant carries associated fields that provide context for the exclusion decision. This is a deliberate design choice — data-carrying variants enable callers to programmatically inspect exclusion details without parsing message strings.

       Rejected alternative: fieldless enum variants — simpler wire format but forces callers to reconstruct context from other diagnostic data, which is fragile and error-prone.

       Present as a table with columns: Variant, Description, Fields, Emitted by, Status.

       | Variant | Description | Fields | Emitted by | Status |
       |---------|-------------|--------|------------|--------|
       | `BudgetExceeded` | Item did not fit within the token budget | `item_tokens`: integer, `available_tokens`: integer | Slice stage, Place stage (Truncate) | Active |
       | `ScoredTooLow` | Item scored below the selection threshold | `score`: float64, `threshold`: float64 | — | Reserved |
       | `Deduplicated` | Byte-exact content duplicate removed | `deduplicated_against`: string (content identifier) | Deduplicate stage | Active |
       | `QuotaCapExceeded` | Kind exceeded its quota cap | `kind`: string, `cap`: integer, `actual`: integer | — | Reserved |
       | `QuotaRequireDisplaced` | Displaced to satisfy another kind's quota requirement | `displaced_by_kind`: string | — | Reserved |
       | `NegativeTokens` | Item has a negative token count | `tokens`: integer | Classify stage | Active |
       | `PinnedOverride` | Displaced by a pinned item during truncation overflow handling | `displaced_by`: string (content identifier) | Place stage (Truncate) | Active |
       | `Filtered` | Excluded by a user-defined filter predicate | `filter_name`: string | — | Reserved |

       Reserved variants are defined but not emitted by any built-in pipeline stage. They are reserved for future specification versions and custom stage implementations. Implementations must include these variants in their type definitions to ensure forward compatibility.

       Inline rationale: defining reserved variants now ensures that implementations allocate space in their type system for future extensions, avoiding breaking changes when these variants become active.

       **JSON example (BudgetExceeded):**

       ```json
       {
         "reason": "BudgetExceeded",
         "item_tokens": 2048,
         "available_tokens": 512
       }
       ```

       **JSON example (Deduplicated):**

       ```json
       {
         "reason": "Deduplicated",
         "deduplicated_against": "tool_output_abc123"
       }
       ```

       **JSON example (NegativeTokens):**

       ```json
       {
         "reason": "NegativeTokens",
         "tokens": -1
       }
       ```

       Wire format: the `reason` field is a string discriminator. Variant-specific fields appear as sibling fields alongside `reason`. Absent fields for inactive variant data are omitted.

    3. **InclusionReason** — Three variants (not data-carrying — inclusion reasons carry no additional context beyond the variant name):

       | Variant | Description | Emitted by |
       |---------|-------------|------------|
       | `Scored` | Included based on computed score within budget | Place stage |
       | `Pinned` | Bypassed scoring and slicing due to pinned status | Classify stage / Place stage |
       | `ZeroToken` | Included at no budget cost (zero-token item) | Place stage |

       **JSON example:**

       ```json
       {
         "reason": "Scored"
       }
       ```

       Inline rationale: inclusion reasons are simple status indicators — the score itself (carried on IncludedItem) provides the quantitative detail. Data-carrying variants would add complexity without diagnostic value.

    4. **Conformance Notes:**
       - Implementations MUST define all 8 `ExclusionReason` variants, including reserved variants, to ensure forward compatibility.
       - Implementations MUST handle unknown `ExclusionReason` variants gracefully when deserializing diagnostic data from other implementations or future specification versions.
       - The `reason` field in the JSON wire format MUST be the string name of the variant (e.g., `"BudgetExceeded"`, not an integer code).
       - Data-carrying fields MUST be present when the variant is emitted. For example, `BudgetExceeded` MUST include both `item_tokens` and `available_tokens`.
       - Reserved variants MUST NOT be emitted by built-in pipeline stages. Custom stage implementations may emit reserved variants.

    ---

    **selection-report.md** — Structure:

    ```
    # SelectionReport
    ## Overview
    ## How to Obtain
    ## SelectionReport Fields
    ## IncludedItem
    ## ExcludedItem
    ## Conformance Notes
    ```

    Content:

    1. **Overview** — The SelectionReport is the complete diagnostic output from a pipeline run. It contains the event log, lists of included and excluded items with their reasons, and aggregate statistics. It is the primary artifact callers use to understand pipeline decisions.

    2. **How to Obtain** — Three-step pseudocode:

       ```text
       OBTAIN-REPORT:
           collector <- CREATE DiagnosticTraceCollector(detail_level)
           result <- pipeline.RUN(candidates, budget, collector)
           report <- collector.BUILD-REPORT()
           // result contains the selected items
           // report contains the diagnostic data
       ```

       The report is extracted from the collector after the pipeline run completes. This keeps the pipeline's primary return type unchanged — the report is a side-channel, not part of the main output.

       Inline rationale: post-call extraction separates diagnostic concerns from the pipeline's functional return value. Callers who don't need diagnostics never interact with report types.
       Rejected alternative: returning the report as part of the pipeline result — couples diagnostics to every call site, even those that don't need it.

    3. **SelectionReport Fields:**

       | Field | Type | Required | Default | Description |
       |-------|------|----------|---------|-------------|
       | `events` | list of TraceEvent | Yes | — | All recorded events in insertion (stage) order. See [Events](events.md). |
       | `included` | list of IncludedItem | Yes | — | Items selected for the context window, in final placed order. |
       | `excluded` | list of ExcludedItem | Yes | — | Items not selected, sorted by score descending (stable by insertion order on ties). |
       | `total_candidates` | integer | Yes | — | Total number of items considered by the pipeline before any exclusion. |
       | `total_tokens_considered` | integer | Yes | — | Sum of tokens across all candidate items. |

       **Complete JSON example:**

       ```json
       {
         "events": [
           { "stage": "Classify", "duration_ms": 0.3, "item_count": 5 },
           { "stage": "Score", "duration_ms": 8.2, "item_count": 3 },
           { "stage": "Deduplicate", "duration_ms": 0.1, "item_count": 3 },
           { "stage": "Slice", "duration_ms": 1.4, "item_count": 3 },
           { "stage": "Place", "duration_ms": 0.2, "item_count": 2 }
         ],
         "included": [
           {
             "item": { "content": "Recent conversation turn", "tokens": 256, "kind": "message" },
             "score": 0.92,
             "reason": { "reason": "Scored" }
           },
           {
             "item": { "content": "System prompt", "tokens": 128, "kind": "system" },
             "score": 0.0,
             "reason": { "reason": "Pinned" }
           }
         ],
         "excluded": [
           {
             "item": { "content": "Old tool output", "tokens": 2048, "kind": "tool_output" },
             "score": 0.45,
             "reason": { "reason": "BudgetExceeded", "item_tokens": 2048, "available_tokens": 512 }
           },
           {
             "item": { "content": "Duplicate message", "tokens": 256, "kind": "message" },
             "score": 0.30,
             "reason": { "reason": "Deduplicated", "deduplicated_against": "Recent conversation turn" }
           }
         ],
         "total_candidates": 5,
         "total_tokens_considered": 4736
       }
       ```

    4. **IncludedItem:**

       | Field | Type | Required | Default | Description |
       |-------|------|----------|---------|-------------|
       | `item` | ContextItem | Yes | — | The included context item. See [ContextItem](../data-model/context-item.md). |
       | `score` | float64 | Yes | — | The computed relevance score at time of inclusion. `0.0` for pinned and zero-token items. |
       | `reason` | InclusionReason | Yes | — | Why this item was included. See [Exclusion Reasons](exclusion-reasons.md#inclusionreason). |

       **JSON example:**

       ```json
       {
         "item": { "content": "User's latest message", "tokens": 64, "kind": "message" },
         "score": 0.95,
         "reason": { "reason": "Scored" }
       }
       ```

    5. **ExcludedItem:**

       | Field | Type | Required | Default | Description |
       |-------|------|----------|---------|-------------|
       | `item` | ContextItem | Yes | — | The excluded context item. See [ContextItem](../data-model/context-item.md). |
       | `score` | float64 | Yes | — | The computed relevance score at time of exclusion. |
       | `reason` | ExclusionReason | Yes | — | Why this item was excluded. See [Exclusion Reasons](exclusion-reasons.md#exclusionreason). |

       **JSON example (BudgetExceeded):**

       ```json
       {
         "item": { "content": "Large tool output", "tokens": 4096, "kind": "tool_output" },
         "score": 0.60,
         "reason": { "reason": "BudgetExceeded", "item_tokens": 4096, "available_tokens": 1024 }
       }
       ```

       **JSON example (Deduplicated):**

       ```json
       {
         "item": { "content": "Duplicate content", "tokens": 256, "kind": "message" },
         "score": 0.40,
         "reason": { "reason": "Deduplicated", "deduplicated_against": "Original message" }
       }
       ```

       Note: `deduplicated_against` appears only when `reason` is `Deduplicated`. For all other reasons, variant-specific fields appear instead. Absent fields are omitted (no nulls).

       Inline rationale: `deduplicated_against` is modelled as a field on the `Deduplicated` reason variant (not as a nullable top-level field on ExcludedItem) because the user decision mandates data-carrying ExclusionReason variants. Each variant carries its own context fields — `Deduplicated` carries `deduplicated_against`, `BudgetExceeded` carries `item_tokens` and `available_tokens`, etc. This keeps ExcludedItem clean (always 3 fields: item, score, reason) and avoids nullable fields that only apply to one reason.
       Rejected alternative: nullable `deduplicated_against` on ExcludedItem — couples the ExcludedItem schema to a specific reason variant, contradicts the data-carrying variant design.

    6. **Conformance Notes:**
       - The `excluded` list MUST be sorted by score descending. When two items have equal scores, the item that was excluded earlier in the pipeline run appears first (stable sort).
         This ordering surfaces the highest-value rejected items first, which is the most useful presentation for debugging "why wasn't this included?" questions.
         *Rejected alternative: insertion order — less useful for diagnosis; the highest-scored excluded item is rarely the first item processed.*
       - The `included` list MUST be in final placed order (the order determined by the Placer), not score order or insertion order.
       - `total_candidates` MUST equal `len(included) + len(excluded)`.
       - `total_tokens_considered` MUST equal the sum of `tokens` across all items in both `included` and `excluded` lists.
       - The `score` field on `IncludedItem` and `ExcludedItem` MUST reflect the score at the time the inclusion/exclusion decision was made, not a recalculated value.

    **Style rules (both files):**
    - No C#, Rust, or language-specific syntax. Pseudocode only.
    - MUST keyword only in Conformance Notes.
    - snake_case for all JSON field names.
    - Absent fields omitted (no nulls).
    - Relative links for cross-references.
  </action>
  <verify>
    Confirm both files exist. Check exclusion-reasons.md has: 8 ExclusionReason data-carrying variants with fields columns, 3 InclusionReason variants, JSON examples, Conformance Notes.
    Check selection-report.md has: pseudocode for obtaining report, SelectionReport fields table, complete JSON example, IncludedItem and ExcludedItem field tables with JSON examples, sort invariant in Conformance Notes.
    Confirm no C#/Rust syntax.
  </verify>
  <done>
    Both sub-pages fully specify the consumer-side diagnostic types. ExclusionReason has 8 data-carrying variants. SelectionReport has complete field table and JSON example. Sort invariant documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SUMMARY.md and verify book builds</name>
  <files>spec/src/SUMMARY.md</files>
  <action>
    Update `spec/src/SUMMARY.md` to register the Diagnostics chapter.

    Add the following block **after** the Placers section (after `  - [UShapedPlacer](placers/u-shaped.md)`) and **before** the `# Conformance` heading:

    ```markdown
    - [Diagnostics](diagnostics.md)
      - [TraceCollector](diagnostics/trace-collector.md)
      - [Events](diagnostics/events.md)
      - [Exclusion Reasons](diagnostics/exclusion-reasons.md)
      - [SelectionReport](diagnostics/selection-report.md)
    ```

    Ensure the indentation matches the existing sub-page entries (2-space indent for child pages).

    After editing, verify the book builds:
    ```bash
    cd spec && mdbook build 2>&1
    ```

    If mdbook is not installed, verify by checking that all referenced files exist:
    ```bash
    ls spec/src/diagnostics.md spec/src/diagnostics/trace-collector.md spec/src/diagnostics/events.md spec/src/diagnostics/exclusion-reasons.md spec/src/diagnostics/selection-report.md
    ```
  </action>
  <verify>
    Confirm SUMMARY.md contains the Diagnostics entry with 4 sub-pages. Confirm the entry appears between Placers and Conformance. If mdbook is available, confirm it builds without errors.
  </verify>
  <done>
    SUMMARY.md updated. Diagnostics chapter appears in correct position in the book navigation. All 5 referenced files exist. Book builds (or file existence confirmed).
  </done>
</task>

</tasks>

<verification>
- `spec/src/diagnostics/exclusion-reasons.md` exists with 8 data-carrying ExclusionReason variants and 3 InclusionReason variants
- `spec/src/diagnostics/selection-report.md` exists with SelectionReport, IncludedItem, ExcludedItem field tables and JSON examples
- `spec/src/SUMMARY.md` contains Diagnostics entry with 4 sub-pages, positioned after Placers and before Conformance
- ExclusionReason variants carry data fields (BudgetExceeded has item_tokens + available_tokens)
- Excluded list sort invariant documented in Conformance Notes
- No C#, Rust, or language-specific syntax in any file
- All JSON examples use snake_case and omit absent fields
- MUST appears only within Conformance Notes sections
- All 5 spec files (1 top-level + 4 sub-pages) exist and are referenced from SUMMARY.md
</verification>

<success_criteria>
- Complete diagnostics spec chapter: 5 files (diagnostics.md + 4 sub-pages)
- SUMMARY.md updated and book navigable
- All diagnostic types fully specified with field tables and JSON examples
- Load-bearing decisions D2 (data-carrying ExclusionReason), D3 (post-call extraction), D4 (reserved variants), D6 (absent fields, no nulls) resolved
- ExclusionReason variants are DATA-CARRYING per user decision
- Excluded list sort invariant is a documented conformance requirement
- Language-agnostic throughout
</success_criteria>

<output>
After completion, create `.planning/phases/pending/24-diagnostics-spec-chapter/24-02-SUMMARY.md`
</output>
