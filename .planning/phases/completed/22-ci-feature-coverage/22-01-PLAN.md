---
phase: 22-ci-feature-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-rust.yml
  - .github/workflows/release-rust.yml
autonomous: true
source_issue: ""

must_haves:
  truths:
    - "CI runs cargo test with both default features and --all-features on every PR and push to main"
    - "Release workflow runs cargo test with both default features and --all-features before publishing"
    - "CI runs cargo clippy with both default features and --all-features"
    - "33 serde-gated tests and 15 cfg(feature = serde) code blocks are exercised in CI"
  artifacts:
    - path: ".github/workflows/ci-rust.yml"
      provides: "CI workflow with Clippy and Test steps for both default and all-features configurations"
    - path: ".github/workflows/release-rust.yml"
      provides: "Release workflow with Clippy and Test steps for both default and all-features configurations"
  key_links:
    - from: ".github/workflows/ci-rust.yml"
      to: "crates/cupel/Cargo.toml"
      via: "--manifest-path crates/cupel/Cargo.toml and --all-features flag"
    - from: ".github/workflows/release-rust.yml"
      to: "crates/cupel/Cargo.toml"
      via: "--manifest-path crates/cupel/Cargo.toml and --all-features flag"
---

<objective>
Add --all-features test and clippy steps to both CI and release workflows so that serde-gated tests and code are exercised on every CI run and before every publish.

Purpose: 33 serde tests and 15 feature-gated code blocks are silently skipped on every CI run today. This closes the integration gap identified in the v1.1 milestone audit.

Output: Updated ci-rust.yml and release-rust.yml, each with renamed existing steps (adding "default features" suffix) and new --all-features companion steps for both Clippy and Test.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/ci-rust.yml
@.github/workflows/release-rust.yml
@crates/cupel/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --all-features steps to ci-rust.yml</name>
  <files>.github/workflows/ci-rust.yml</files>
  <action>
  Make four changes to `.github/workflows/ci-rust.yml`:

  1. **Rename** the existing Clippy step (line 37) from `Clippy` to `Clippy (default features)` — the `run` command stays unchanged
  2. **Add** a new step immediately after it:
     ```yaml
      - name: Clippy (all features)
        run: cargo clippy --all-features --manifest-path crates/cupel/Cargo.toml -- -D warnings
     ```
  3. **Rename** the existing Test step (line 40) from `Test` to `Test (default features)` — the `run` command stays unchanged
  4. **Add** a new step immediately after it:
     ```yaml
      - name: Test (all features)
        run: cargo test --all-features --manifest-path crates/cupel/Cargo.toml
     ```

  The final step order should be: Format → Clippy (default features) → Clippy (all features) → Test (default features) → Test (all features) → Deny

  Do NOT modify the Format, Deny, Cache, Setup Rust, or Checkout steps.
  </action>
  <verify>
  Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-rust.yml'))"`
  Run both test configurations locally to confirm they pass:
  - `rtk cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings`
  - `rtk cargo clippy --all-features --manifest-path crates/cupel/Cargo.toml -- -D warnings`
  - `rtk cargo test --manifest-path crates/cupel/Cargo.toml`
  - `rtk cargo test --all-features --manifest-path crates/cupel/Cargo.toml`
  </verify>
  <done>ci-rust.yml has 4 quality gate steps: Clippy (default features), Clippy (all features), Test (default features), Test (all features). All pass locally.</done>
</task>

<task type="auto">
  <name>Task 2: Add --all-features steps to release-rust.yml</name>
  <files>.github/workflows/release-rust.yml</files>
  <action>
  Make the same four changes to `.github/workflows/release-rust.yml` in the `test` job:

  1. **Rename** the existing Clippy step (line 38) from `Clippy` to `Clippy (default features)` — the `run` command stays unchanged
  2. **Add** a new step immediately after it:
     ```yaml
      - name: Clippy (all features)
        run: cargo clippy --all-features --manifest-path crates/cupel/Cargo.toml -- -D warnings
     ```
  3. **Rename** the existing Test step (line 41) from `Test` to `Test (default features)` — the `run` command stays unchanged
  4. **Add** a new step immediately after it:
     ```yaml
      - name: Test (all features)
        run: cargo test --all-features --manifest-path crates/cupel/Cargo.toml
     ```

  The final step order in the `test` job should be: Format → Clippy (default features) → Clippy (all features) → Test (default features) → Test (all features) → Deny → Verify package contents

  Do NOT modify the Format, Deny, Verify package contents, Cache, Setup Rust, Checkout steps, or the `publish` job.
  </action>
  <verify>
  Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-rust.yml'))"`
  Diff the test job steps between ci-rust.yml and release-rust.yml to confirm the Clippy and Test steps are identical (only surrounding steps differ).
  </verify>
  <done>release-rust.yml test job has 4 quality gate steps matching ci-rust.yml: Clippy (default features), Clippy (all features), Test (default features), Test (all features). Serde regressions now block release.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-rust.yml'))"` — valid YAML
2. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-rust.yml'))"` — valid YAML
3. `rtk cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` — passes
4. `rtk cargo clippy --all-features --manifest-path crates/cupel/Cargo.toml -- -D warnings` — passes
5. `rtk cargo test --manifest-path crates/cupel/Cargo.toml` — passes (default features)
6. `rtk cargo test --all-features --manifest-path crates/cupel/Cargo.toml` — passes (serde tests included)
</verification>

<success_criteria>
- ci-rust.yml runs Clippy and Test for both default and all-features configurations
- release-rust.yml test job runs Clippy and Test for both default and all-features configurations
- Both workflow files are valid YAML
- All four local test/clippy runs pass
- No other workflow steps or jobs are modified
</success_criteria>

<output>
After completion, create `.planning/phases/22-ci-feature-coverage/22-01-SUMMARY.md`
</output>
