---
phase: 13-budget-contract-implementation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - spec/src/data-model/context-budget.md
  - spec/src/pipeline/slice.md
autonomous: true
must_haves:
  truths:
    - "Spec effective budget formula includes reservedTokens subtraction"
    - "Spec effective budget formula includes safety margin multiplication"
    - "Spec documents the order of operations: subtract reserved, then apply margin"
    - "Spec conformance note updated to reflect that reservedSlots and estimationSafetyMarginPercent ARE consumed by the pipeline (not just by callers/QuotaSlice)"
    - "Streaming path budget computation is documented in the spec"
  artifacts:
    - path: "spec/src/data-model/context-budget.md"
      provides: "Updated effective budget formula and semantics for reservedSlots and estimationSafetyMarginPercent"
    - path: "spec/src/pipeline/slice.md"
      provides: "Updated pseudocode and conformance notes reflecting new budget computation"
  key_links:
    - from: "spec/src/pipeline/slice.md"
      to: "spec/src/data-model/context-budget.md"
      via: "Cross-references effective budget formula"
source_issue: null
---

<objective>
Update the language-agnostic specification to reflect that `reservedSlots` and `estimationSafetyMarginPercent` are now consumed by the pipeline's effective budget computation.

Purpose: The spec currently says reservedSlots is only for QuotaSlice and safety margin is caller-only. After plan 01 wired them into the pipeline, the spec must match the implementation.

Output: Updated spec documents with corrected formulas, semantics, and conformance notes.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@spec/src/data-model/context-budget.md
@spec/src/pipeline/slice.md
@src/Wollax.Cupel/CupelPipeline.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update context-budget.md spec</name>
  <files>spec/src/data-model/context-budget.md</files>
  <action>
  Update `spec/src/data-model/context-budget.md`:

  1. **Effective Budget section** — replace the formula block with the expanded version:
     ```
     reservedTokens  <- sum of all values in budget.reservedSlots
     effectiveMax    <- max(0, maxTokens - outputReserve - pinnedTokens - reservedTokens)
     effectiveTarget <- max(0, targetTokens - pinnedTokens - reservedTokens)
     effectiveTarget <- min(effectiveTarget, effectiveMax)

     if estimationSafetyMarginPercent > 0:
         multiplier      <- 1.0 - estimationSafetyMarginPercent / 100.0
         effectiveMax    <- floor(effectiveMax * multiplier)
         effectiveTarget <- floor(effectiveTarget * multiplier)
         effectiveTarget <- min(effectiveTarget, effectiveMax)
     ```

  2. **Update the prose** below the formula to explain:
     - `reservedTokens` is the sum of all values in `reservedSlots`, subtracted alongside `outputReserve` and `pinnedTokens`
     - The safety margin is applied after all subtractions as a multiplicative reduction
     - Both use `floor` (integer truncation) when converting from floating-point

  3. **Semantics section** — update the two bullets:
     - `reservedSlots`: Change from "consumed by QuotaSlice; other slicers ignore it" to: "The sum of reserved slot values is subtracted from the effective budget during pipeline budget computation, reducing the token ceiling available for non-reserved items. Additionally, QuotaSlice uses reserved slots to guarantee minimum representation per kind."
     - `estimationSafetyMarginPercent`: Change from "available for caller use but not directly consumed by the core pipeline stages" to: "Applied as a multiplicative reduction to the effective budget after reserved slots and other subtractions. A value of 10.0 reduces the effective budget to 90% of its post-subtraction value."
  </action>
  <verify>
  Read the updated file and confirm:
  - Formula includes `reservedTokens` and `estimationSafetyMarginPercent`
  - Semantics bullets describe pipeline consumption (not caller-only)
  - No broken markdown formatting
  </verify>
  <done>context-budget.md spec reflects that both properties are consumed by the pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Update slice.md spec</name>
  <files>spec/src/pipeline/slice.md</files>
  <action>
  Update `spec/src/pipeline/slice.md`:

  1. **Effective Budget Computation section** — replace the pseudocode block:
     ```text
     COMPUTE-EFFECTIVE-BUDGET(budget, pinnedTokens):
         reservedTokens  <- sum(budget.reservedSlots.values)
         effectiveMax    <- max(0, budget.maxTokens - budget.outputReserve - pinnedTokens - reservedTokens)
         effectiveTarget <- max(0, budget.targetTokens - pinnedTokens - reservedTokens)
         effectiveTarget <- min(effectiveTarget, effectiveMax)

         if budget.estimationSafetyMarginPercent > 0:
             multiplier      <- 1.0 - budget.estimationSafetyMarginPercent / 100.0
             effectiveMax    <- floor(effectiveMax * multiplier)
             effectiveTarget <- floor(effectiveTarget * multiplier)
             effectiveTarget <- min(effectiveTarget, effectiveMax)

         return ContextBudget(
             maxTokens:    effectiveMax,
             targetTokens: effectiveTarget
         )
     ```

  2. **Update the "Where" list** below the pseudocode to add:
     - `reservedTokens` is the sum of all values in `budget.reservedSlots`, reserving token capacity for per-kind guarantees.
     - The safety margin reduces the effective budget by the configured percentage to account for token estimation error.

  3. **Conformance Notes section** — update the second bullet:
     - Change from: "The slicer receives a fresh ContextBudget with only `maxTokens` and `targetTokens` set. Other budget fields (`outputReserve`, `reservedSlots`, `estimationSafetyMarginPercent`) are not forwarded to the slicer in the adjusted budget."
     - To: "The slicer receives a fresh ContextBudget with only `maxTokens` and `targetTokens` set. The effects of `outputReserve`, `reservedSlots`, and `estimationSafetyMarginPercent` are incorporated into the `maxTokens` and `targetTokens` values of the adjusted budget; the original fields are not forwarded."

  4. **Edge Cases section** — add a new bullet:
     - "**Reserved slots exceed remaining budget.** If the sum of `reservedSlots` values exceeds `maxTokens - outputReserve - pinnedTokens`, then `effectiveMax = 0` and the slicer selects no items (or only zero-token items)."
  </action>
  <verify>
  Read the updated file and confirm:
  - Pseudocode includes reservedTokens and safety margin
  - Conformance note explains incorporation (not omission)
  - New edge case documented
  - No broken markdown formatting
  </verify>
  <done>slice.md spec reflects the updated budget computation with reserved slots and safety margin.</done>
</task>

</tasks>

<verification>
- Both spec files have updated formulas matching the implementation from plan 01
- Conformance notes are internally consistent (no contradictions between context-budget.md and slice.md)
- No markdown formatting issues
</verification>

<success_criteria>
- Spec formulas match the implementation in CupelPipeline.cs
- Semantics section accurately describes pipeline consumption of both properties
- Conformance notes updated to reflect that effects are incorporated into adjusted budget
- Edge case for reserved slots exceeding budget is documented
</success_criteria>

<output>
After completion, create `.planning/phases/active/13-budget-contract-implementation/13-02-SUMMARY.md`
</output>
