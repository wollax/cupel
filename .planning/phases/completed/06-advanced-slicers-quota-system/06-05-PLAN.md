---
phase: "06"
plan: "06-05"
title: "Slicer benchmarks — KnapsackSlice, QuotaSlice, StreamSlice"
wave: 3
depends_on: ["06-04"]
files_modified:
  - benchmarks/Wollax.Cupel.Benchmarks/SlicerBenchmark.cs
autonomous: true
must_haves:
  truths:
    - "Benchmark compares GreedySlice, KnapsackSlice, and QuotaSlice(GreedySlice) on identical datasets"
    - "Datasets: 100, 250, 500 items with mixed ContextKinds"
    - "KnapsackSlice bucket sizes 50/100/200 benchmarked to show performance/accuracy tradeoff"
    - "MemoryDiagnoser verifies allocation profiles"
    - "StreamSlice benchmarked separately with async enumerable source"
    - "Results document the performance characteristics for guidance threshold"
  artifacts:
    - path: "benchmarks/Wollax.Cupel.Benchmarks/SlicerBenchmark.cs"
      provides: "BenchmarkDotNet benchmark comparing all slicer implementations"
  key_links:
    - from: "SlicerBenchmark"
      to: "GreedySlice, KnapsackSlice, QuotaSlice, StreamSlice"
      via: "benchmarks all slicer implementations on shared test data"
---

<objective>
Create comprehensive benchmarks for all slicer implementations to document performance characteristics, allocation profiles, and the accuracy/performance tradeoff for KnapsackSlice bucket sizes.

Purpose: Benchmarks provide the data for guidance thresholds — when to use KnapsackSlice vs GreedySlice, what bucket size to choose, and the overhead of QuotaSlice decoration. They also verify zero-allocation discipline in hot paths.

Output: SlicerBenchmark.cs with BenchmarkDotNet benchmarks for all slicers.
</objective>

<context>
@benchmarks/Wollax.Cupel.Benchmarks/PipelineBenchmark.cs
@benchmarks/Wollax.Cupel.Benchmarks/Program.cs
@src/Wollax.Cupel/GreedySlice.cs
@src/Wollax.Cupel/KnapsackSlice.cs
@src/Wollax.Cupel/Slicing/QuotaSlice.cs
@src/Wollax.Cupel/Slicing/StreamSlice.cs
@src/Wollax.Cupel/Slicing/QuotaBuilder.cs
@src/Wollax.Cupel/IAsyncSlicer.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlicerBenchmark with all slicer implementations</name>
  <files>
    benchmarks/Wollax.Cupel.Benchmarks/SlicerBenchmark.cs
  </files>
  <action>
    Create SlicerBenchmark.cs in the benchmarks project. Use [MemoryDiagnoser] attribute.

    Setup:
    - [Params(100, 250, 500)] public int ItemCount
    - Generate test data in [GlobalSetup]: create ItemCount items with mixed ContextKinds (Message, Document, ToolOutput, Memory), varied token counts (10-200), and randomized scores (0.1-1.0). Use a fixed seed Random for reproducibility.
    - Pre-create ScoredItem[] from the items
    - Pre-create ContextBudget with targetTokens = sum_of_all_tokens * 0.4 (select ~40% of content)
    - Pre-create slicer instances:
      - GreedySlice
      - KnapsackSlice(bucketSize: 50)
      - KnapsackSlice(bucketSize: 100)
      - KnapsackSlice(bucketSize: 200)
      - QuotaSlice wrapping GreedySlice with: Require(Message, 30%), Cap(Document, 40%)
      - QuotaSlice wrapping KnapsackSlice(100) with same quotas

    Synchronous benchmarks:
    - [Benchmark(Baseline = true)] Greedy() — GreedySlice.Slice(...)
    - [Benchmark] Knapsack_Bucket50() — KnapsackSlice(50).Slice(...)
    - [Benchmark] Knapsack_Bucket100() — KnapsackSlice(100).Slice(...)
    - [Benchmark] Knapsack_Bucket200() — KnapsackSlice(200).Slice(...)
    - [Benchmark] QuotaGreedy() — QuotaSlice(GreedySlice).Slice(...)
    - [Benchmark] QuotaKnapsack100() — QuotaSlice(KnapsackSlice(100)).Slice(...)

    Async benchmark (separate benchmark class or method):
    - StreamSlice_Batch32() — StreamSlice(32).SliceAsync(asyncEnumerable, ...)
    - Use a pre-materialized list converted to IAsyncEnumerable for fair comparison

    All benchmarks use NullTraceCollector.Instance.

    After creating the benchmark, verify it compiles and runs a quick dry-run:
    ```bash
    rtk dotnet build /Users/wollax/Git/personal/cupel/Cupel.slnx -c Release
    ```

    Do NOT run the full benchmark (takes too long). Just verify it compiles.
  </action>
  <verify>
    rtk dotnet build /Users/wollax/Git/personal/cupel/Cupel.slnx -c Release
    rtk dotnet test /Users/wollax/Git/personal/cupel/tests/Wollax.Cupel.Tests
  </verify>
  <done>
    - SlicerBenchmark.cs exists in benchmarks/Wollax.Cupel.Benchmarks/
    - Benchmarks GreedySlice, KnapsackSlice (3 bucket sizes), QuotaSlice (2 variants), StreamSlice
    - Uses [MemoryDiagnoser] for allocation tracking
    - Parameterized with 100/250/500 item counts
    - Uses NullTraceCollector and fixed-seed data for reproducibility
    - Release build compiles with zero warnings
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
```bash
rtk dotnet build /Users/wollax/Git/personal/cupel/Cupel.slnx -c Release
rtk dotnet test /Users/wollax/Git/personal/cupel/tests/Wollax.Cupel.Tests
```
Verify the benchmark project compiles in Release mode (BenchmarkDotNet requires Release).
</verification>

<success_criteria>
- [ ] SlicerBenchmark.cs compiles in Release configuration
- [ ] Benchmarks all 4 slicer implementations (Greedy, Knapsack, Quota, Stream)
- [ ] KnapsackSlice benchmarked at 3 bucket sizes (50, 100, 200)
- [ ] QuotaSlice benchmarked wrapping both GreedySlice and KnapsackSlice
- [ ] [MemoryDiagnoser] enabled for allocation profiling
- [ ] Parameterized for 100/250/500 items
- [ ] All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/pending/06-advanced-slicers-quota-system/06-05-SUMMARY.md`
</output>
