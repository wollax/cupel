---
phase: "03"
plan: "03-03"
title: "Scorer zero-allocation benchmark"
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs
autonomous: true
must_haves:
  truths:
    - "All six scorers show zero Gen0 collections in MemoryDiagnoser output"
    - "Benchmark covers all six scorers with realistic item sets (100, 500 items)"
    - "Each benchmark method returns a value to prevent dead code elimination"
  artifacts:
    - path: "benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs"
      provides: "MemoryDiagnoser benchmark verifying zero-allocation for all six scorers"
  key_links:
    - from: "ScorerBenchmark"
      to: "RecencyScorer, PriorityScorer, ReflexiveScorer, KindScorer, TagScorer, FrequencyScorer"
      via: "instantiates each scorer, calls Score() in for-loop over item array"
---

<objective>
Create a single BenchmarkDotNet benchmark class that exercises all six scorers with [MemoryDiagnoser], verifying the zero heap allocation constraint from the phase success criteria. This is the final verification gate for Phase 3.
</objective>

<context>
@benchmarks/Wollax.Cupel.Benchmarks/TraceGatingBenchmark.cs
@benchmarks/Wollax.Cupel.Benchmarks/Program.cs
@.planning/phases/pending/03-individual-scorers/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScorerBenchmark class</name>
  <files>
    benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs
  </files>
  <action>
    Create ScorerBenchmark.cs following the existing TraceGatingBenchmark pattern:

    1. [MemoryDiagnoser] attribute on class
    2. [Params(100, 500)] for ItemCount
    3. [GlobalSetup] creates an array of ContextItems with varied data:
       - Each item gets: Content, Tokens, Timestamp (now minus i minutes), Priority (i), Kind (cycle through the 5 kinds), Tags (["tag-{i%5}"]), FutureRelevanceHint (i / (double)ItemCount)
       - Use Enumerable.Range + Select + ToArray (allocation in setup is fine)
    4. Initialize all six scorers in Setup:
       - _recencyScorer = new RecencyScorer()
       - _priorityScorer = new PriorityScorer()
       - _reflexiveScorer = new ReflexiveScorer()
       - _kindScorer = new KindScorer()
       - _tagScorer = new TagScorer(new Dictionary<string, double> { ["tag-0"] = 1.0, ["tag-1"] = 0.8, ["tag-2"] = 0.6, ["tag-3"] = 0.4, ["tag-4"] = 0.2 })
       - _frequencyScorer = new FrequencyScorer()
    5. One [Benchmark] method per scorer, each returning double (sum of scores) to prevent dead code elimination
    6. Each benchmark method: for-loop over _items, accumulate sum += scorer.Score(_items[i], _items), return sum

    Add `using Wollax.Cupel.Scoring;` import.

    See RESEARCH.md "BenchmarkDotNet Scorer Benchmark Pattern" for reference code.
  </action>
  <verify>
    rtk dotnet build /Users/wollax/Git/personal/cupel/benchmarks/Wollax.Cupel.Benchmarks
  </verify>
  <done>
    - ScorerBenchmark.cs compiles
    - Contains 6 [Benchmark] methods (one per scorer)
    - Uses [MemoryDiagnoser] and [Params(100, 500)]
    - All benchmark methods return a value (not void)
  </done>
</task>

</tasks>

<verification>
Build the benchmark project:
```bash
rtk dotnet build /Users/wollax/Git/personal/cupel/benchmarks/Wollax.Cupel.Benchmarks
```

Optionally run a quick smoke test (short run, not full benchmark):
```bash
cd /Users/wollax/Git/personal/cupel && rtk dotnet run --project benchmarks/Wollax.Cupel.Benchmarks -c Release -- --filter "ScorerBenchmark" --job short
```

The full benchmark run (for actual zero-allocation verification) can be done manually by the user.
</verification>

<success_criteria>
- [ ] ScorerBenchmark.cs exists in benchmarks/Wollax.Cupel.Benchmarks/
- [ ] Benchmark project builds successfully
- [ ] All six scorers have dedicated [Benchmark] methods
- [ ] [MemoryDiagnoser] is applied to the class
- [ ] Setup creates realistic test data with all relevant ContextItem fields populated
</success_criteria>
