---
phase: 07-explainability-overflow-handling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/Wollax.Cupel/Wollax.Cupel.csproj
  - src/Wollax.Cupel/Diagnostics/InclusionReason.cs
  - src/Wollax.Cupel/Diagnostics/ExclusionReason.cs
  - src/Wollax.Cupel/Diagnostics/IncludedItem.cs
  - src/Wollax.Cupel/Diagnostics/ExcludedItem.cs
  - src/Wollax.Cupel/Diagnostics/OverflowStrategy.cs
  - src/Wollax.Cupel/Diagnostics/OverflowEvent.cs
  - src/Wollax.Cupel/Diagnostics/SelectionReport.cs
  - src/Wollax.Cupel/Diagnostics/ReportBuilder.cs
  - src/Wollax.Cupel/CupelPipeline.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/IncludedItemTests.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/ExcludedItemTests.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/OverflowEventTests.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/SelectionReportTests.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/ReportBuilderTests.cs
  - tests/Wollax.Cupel.Tests/Diagnostics/TraceEventTests.cs
autonomous: true
must_haves:
  truths:
    - InclusionReason enum has values Scored, Pinned, ZeroToken
    - ExclusionReason enum has 8 values replacing the existing 4-value stub
    - IncludedItem is a sealed record with Item, Score, Reason
    - ExcludedItem is a sealed record with Item, Score, Reason, optional DeduplicatedAgainst
    - SelectionReport has Included, Excluded, TotalCandidates, TotalTokensConsidered, Events
    - OverflowStrategy enum has Throw, Truncate, Proceed
    - OverflowEvent is a sealed record with TokensOverBudget, OverflowingItems, Budget
    - ReportBuilder is internal and accumulates items through pipeline stages
  artifacts:
    - src/Wollax.Cupel/Diagnostics/InclusionReason.cs
    - src/Wollax.Cupel/Diagnostics/ExclusionReason.cs (evolved from 4 to 8 values)
    - src/Wollax.Cupel/Diagnostics/IncludedItem.cs
    - src/Wollax.Cupel/Diagnostics/ExcludedItem.cs
    - src/Wollax.Cupel/Diagnostics/OverflowStrategy.cs
    - src/Wollax.Cupel/Diagnostics/OverflowEvent.cs
    - src/Wollax.Cupel/Diagnostics/SelectionReport.cs (evolved)
    - src/Wollax.Cupel/Diagnostics/ReportBuilder.cs
  key_links:
    - ExclusionReason enum change is backward-incompatible but nothing is shipped (PublicAPI.Shipped.txt is empty)
    - SelectionReport evolution must retain Events property for existing trace consumers
    - ReportBuilder must use the same gating pattern as existing trace code (if sw is not null)
---

<objective>
Create the explainability and overflow data types that Phase 7 depends on, plus the internal ReportBuilder that accumulates inclusion/exclusion data during pipeline execution.

This plan establishes all new types via TDD: enums (InclusionReason, ExclusionReason, OverflowStrategy), records (IncludedItem, ExcludedItem, OverflowEvent), the evolved SelectionReport, and the internal ReportBuilder. Plan 02 will wire these into the pipeline.
</objective>

<execution_context>
This is the first plan in Phase 7. No prior Phase 7 work exists. The existing ExclusionReason enum (4 values) and SelectionReport (Events only) are stubs from Phase 2 that this plan replaces/evolves.
</execution_context>

<context>
@src/Wollax.Cupel/Diagnostics/ExclusionReason.cs — existing 4-value stub to replace
@src/Wollax.Cupel/Diagnostics/SelectionReport.cs — existing stub to evolve
@src/Wollax.Cupel/Diagnostics/TraceEvent.cs — pattern reference for readonly record struct
@src/Wollax.Cupel/ScoredItem.cs — pattern reference for sealed record
@src/Wollax.Cupel/ContextItem.cs — referenced by IncludedItem/ExcludedItem
@src/Wollax.Cupel/ContextBudget.cs — referenced by OverflowEvent
@src/Wollax.Cupel/CupelPipeline.cs — will consume ReportBuilder in plan 02 (reference for gating pattern)
@.planning/phases/pending/07-explainability-overflow-handling/07-CONTEXT.md — LOCKED decisions
@.planning/phases/pending/07-explainability-overflow-handling/07-RESEARCH.md — architecture findings
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Enums and value types (InclusionReason, ExclusionReason, OverflowStrategy, OverflowEvent)</name>
  <files>
    src/Wollax.Cupel/Diagnostics/InclusionReason.cs
    src/Wollax.Cupel/Diagnostics/ExclusionReason.cs
    src/Wollax.Cupel/Diagnostics/OverflowStrategy.cs
    src/Wollax.Cupel/Diagnostics/OverflowEvent.cs
    tests/Wollax.Cupel.Tests/Diagnostics/OverflowEventTests.cs
    tests/Wollax.Cupel.Tests/Diagnostics/TraceEventTests.cs
  </files>
  <action>
  RED: Write tests first.

  1. **ExclusionReason** — Replace the existing 4-value enum with 8 values:
     `BudgetExceeded`, `ScoredTooLow`, `Deduplicated`, `QuotaCapExceeded`, `QuotaRequireDisplaced`, `NegativeTokens`, `PinnedOverride`, `Filtered`
     - Remove old values: `LowScore` → `ScoredTooLow`, `Duplicate` → `Deduplicated`, `QuotaExceeded` → `QuotaCapExceeded`
     - Note: `ScoredTooLow` and `Filtered` are reserved for future use per RESEARCH.md

  2. **InclusionReason** — New enum in `Wollax.Cupel.Diagnostics`:
     Values: `Scored`, `Pinned`, `ZeroToken`

  3. **OverflowStrategy** — New enum in `Wollax.Cupel.Diagnostics`:
     Values: `Throw`, `Truncate`, `Proceed`

  4. **OverflowEvent** — New sealed record in `Wollax.Cupel.Diagnostics`:
     ```csharp
     public sealed record OverflowEvent
     {
         public required int TokensOverBudget { get; init; }
         public required IReadOnlyList<ContextItem> OverflowingItems { get; init; }
         public required ContextBudget Budget { get; init; }
     }
     ```

  GREEN: Implement.
  - ExclusionReason: overwrite existing file with new 8-value enum
  - InclusionReason: new file
  - OverflowStrategy: new file
  - OverflowEvent: new sealed record file with tests for construction, value equality, required properties

  REFACTOR: Ensure XML doc comments on all enum values and properties.
  </action>
  <verify>
  `rtk dotnet build src/Wollax.Cupel/` compiles cleanly.
  `rtk dotnet test tests/Wollax.Cupel.Tests/ --filter "OverflowEvent"` passes.
  Update `tests/Wollax.Cupel.Tests/Diagnostics/TraceEventTests.cs` — the `ExclusionReason_HasExpectedValues` test (lines 33-42) references `LowScore`, `Duplicate`, `QuotaExceeded` and asserts `Length == 4`. Replace with the new 8-value assertions: `BudgetExceeded`, `ScoredTooLow`, `Deduplicated`, `QuotaCapExceeded`, `QuotaRequireDisplaced`, `NegativeTokens`, `PinnedOverride`, `Filtered`, and assert `Length == 8`.
  </verify>
  <done>
  - InclusionReason enum exists with 3 values (Scored, Pinned, ZeroToken)
  - ExclusionReason enum exists with 8 values replacing the old 4-value stub
  - OverflowStrategy enum exists with 3 values (Throw, Truncate, Proceed)
  - OverflowEvent sealed record exists with required properties and tests
  - All existing tests still pass after ExclusionReason changes
  </done>
</task>

<task type="tdd">
  <name>Task 2: IncludedItem, ExcludedItem records, evolved SelectionReport, and internal ReportBuilder</name>
  <files>
    src/Wollax.Cupel/Wollax.Cupel.csproj
    src/Wollax.Cupel/Diagnostics/IncludedItem.cs
    src/Wollax.Cupel/Diagnostics/ExcludedItem.cs
    src/Wollax.Cupel/Diagnostics/SelectionReport.cs
    src/Wollax.Cupel/Diagnostics/ReportBuilder.cs
    src/Wollax.Cupel/CupelPipeline.cs
    tests/Wollax.Cupel.Tests/Diagnostics/IncludedItemTests.cs
    tests/Wollax.Cupel.Tests/Diagnostics/ExcludedItemTests.cs
    tests/Wollax.Cupel.Tests/Diagnostics/SelectionReportTests.cs
    tests/Wollax.Cupel.Tests/Diagnostics/ReportBuilderTests.cs
  </files>
  <action>
  RED: Write tests first.

  1. **IncludedItem** — sealed record in `Wollax.Cupel.Diagnostics`:
     ```csharp
     public sealed record IncludedItem
     {
         public required ContextItem Item { get; init; }
         public required double Score { get; init; }
         public required InclusionReason Reason { get; init; }
     }
     ```
     Tests: construction, value equality, required properties.

  2. **ExcludedItem** — sealed record in `Wollax.Cupel.Diagnostics`:
     ```csharp
     public sealed record ExcludedItem
     {
         public required ContextItem Item { get; init; }
         public required double Score { get; init; }
         public required ExclusionReason Reason { get; init; }
         public ContextItem? DeduplicatedAgainst { get; init; }
     }
     ```
     Tests: construction, value equality, DeduplicatedAgainst is null by default, non-null when set.

  3. **SelectionReport** — Evolve existing stub. Keep `Events`, add new properties:
     ```csharp
     public sealed record SelectionReport
     {
         public required IReadOnlyList<TraceEvent> Events { get; init; }
         public required IReadOnlyList<IncludedItem> Included { get; init; }
         public required IReadOnlyList<ExcludedItem> Excluded { get; init; }
         public required int TotalCandidates { get; init; }
         public required int TotalTokensConsidered { get; init; }
     }
     ```
     Tests: construction, Excluded ordered by score descending (convention, enforced by ReportBuilder).
     **Breaking change:** Adding `required` properties to SelectionReport means existing code that constructs it (in CupelPipeline.cs) will fail to compile. That's expected — Plan 02 fixes the pipeline. For now, update the existing construction sites with placeholder empty collections to keep the build green.

  4. **ReportBuilder** — `internal` class in `Wollax.Cupel.Diagnostics`:
     Methods:
     - `AddIncluded(ContextItem item, double score, InclusionReason reason)`
     - `AddExcluded(ContextItem item, double score, ExclusionReason reason, ContextItem? deduplicatedAgainst = null)`
     - `SetTotalCandidates(int count)`
     - `SetTotalTokensConsidered(int tokens)`
     - `Build(IReadOnlyList<TraceEvent> events) → SelectionReport`
     The `Build` method sorts excluded items by score descending (stable sort with index tiebreaking).
     Tests: verify accumulation, ordering of excluded items, summary stats.
     **InternalsVisibleTo** — The project does NOT have `InternalsVisibleTo` set up. Add the following to `src/Wollax.Cupel/Wollax.Cupel.csproj` inside an `<ItemGroup>`:
     ```xml
     <InternalsVisibleTo Include="Wollax.Cupel.Tests" />
     ```
     This is required for the test project to access the `internal` ReportBuilder class.

  GREEN: Implement all types.

  REFACTOR: XML doc comments on all public types and properties. Ensure SelectionReport construction in CupelPipeline.cs (lines ~287-289) and ExecuteStreamAsync (~367-370) compiles by providing the new required properties with empty defaults temporarily.
  </action>
  <verify>
  `rtk dotnet build` (full solution) compiles cleanly.
  `rtk dotnet test tests/Wollax.Cupel.Tests/` — ALL tests pass (including existing pipeline tests).
  </verify>
  <done>
  - IncludedItem sealed record exists with Item, Score, Reason and passes tests
  - ExcludedItem sealed record exists with Item, Score, Reason, optional DeduplicatedAgainst and passes tests
  - SelectionReport has been evolved with Included, Excluded, TotalCandidates, TotalTokensConsidered while retaining Events
  - ReportBuilder is internal, accumulates items, sorts excluded by score descending, and passes tests
  - Full solution builds and all tests pass
  </done>
</task>
</tasks>

<verification>
```bash
rtk dotnet build
rtk dotnet test tests/Wollax.Cupel.Tests/
```
- Zero build warnings
- All existing tests pass
- New tests for OverflowEvent, IncludedItem, ExcludedItem, SelectionReport, ReportBuilder pass
</verification>

<success_criteria>
1. All 4 enums exist: InclusionReason (3 values), ExclusionReason (8 values), OverflowStrategy (3 values), plus PipelineStage unchanged
2. All 3 new records exist: IncludedItem, ExcludedItem, OverflowEvent — all sealed with required init props
3. SelectionReport evolved with Included, Excluded, TotalCandidates, TotalTokensConsidered, retaining Events
4. ReportBuilder is internal, accumulates items, sorts excluded by score descending
5. Full solution compiles and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-explainability-overflow-handling/07-01-SUMMARY.md`
</output>
