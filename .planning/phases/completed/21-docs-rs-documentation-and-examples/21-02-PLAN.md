---
phase: 21-docs-rs-documentation-and-examples
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cupel/src/model/mod.rs
  - crates/cupel/src/model/context_item.rs
  - crates/cupel/src/model/context_budget.rs
  - crates/cupel/src/model/context_kind.rs
  - crates/cupel/src/model/context_source.rs
  - crates/cupel/src/model/overflow_strategy.rs
  - crates/cupel/src/model/scored_item.rs
  - crates/cupel/src/scorer/mod.rs
  - crates/cupel/src/scorer/composite.rs
  - crates/cupel/src/scorer/frequency.rs
  - crates/cupel/src/scorer/kind.rs
  - crates/cupel/src/scorer/priority.rs
  - crates/cupel/src/scorer/recency.rs
  - crates/cupel/src/scorer/reflexive.rs
  - crates/cupel/src/scorer/scaled.rs
  - crates/cupel/src/scorer/tag.rs
  - crates/cupel/src/slicer/mod.rs
  - crates/cupel/src/slicer/greedy.rs
  - crates/cupel/src/slicer/knapsack.rs
  - crates/cupel/src/slicer/quota.rs
  - crates/cupel/src/placer/mod.rs
  - crates/cupel/src/placer/chronological.rs
  - crates/cupel/src/placer/u_shaped.rs
  - crates/cupel/src/pipeline/mod.rs
  - crates/cupel/src/error.rs
autonomous: true
source_issue: ""

must_haves:
  truths:
    - "Every public module has a //! doc comment explaining its purpose"
    - "Every public struct and trait has at least one compilable doctest"
    - "Scorer module includes a use-case comparison table in its module docs"
    - "Pipeline module docs explain the 6-stage execution flow"
    - "cargo test --doc --all-features passes with all doctests green"
  artifacts:
    - path: "crates/cupel/src/model/mod.rs"
      provides: "Module-level docs for model types with type inventory"
    - path: "crates/cupel/src/scorer/mod.rs"
      provides: "Module-level docs with scorer comparison table and usage guidance"
    - path: "crates/cupel/src/slicer/mod.rs"
      provides: "Module-level docs explaining slicing strategies"
    - path: "crates/cupel/src/placer/mod.rs"
      provides: "Module-level docs explaining placement strategies"
    - path: "crates/cupel/src/pipeline/mod.rs"
      provides: "Module-level docs with stage flow and builder example"
    - path: "crates/cupel/src/error.rs"
      provides: "Module-level docs for error types"
  key_links:
    - from: "crates/cupel/src/scorer/mod.rs"
      to: "crates/cupel/src/scorer/*.rs"
      via: "Module docs reference individual scorer types"
    - from: "crates/cupel/src/pipeline/mod.rs"
      to: "crates/cupel/src/scorer/mod.rs"
      via: "Pipeline docs reference Scorer trait"
---

<objective>
Add module-level `//!` doc comments to all 6 public modules and compilable doctests to all public structs and traits. The scorer module gets deeper treatment with a comparison table. The pipeline module gets a stage-flow explanation.

Purpose: Module docs are the second-most-visited page after the crate root on docs.rs. Every module needs to explain its role in the pipeline and list its key types. Doctests serve as both documentation and regression tests.

Output: All 25+ source files updated with doc comments; all doctests compile and pass.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@crates/cupel/src/lib.rs
@crates/cupel/src/model/mod.rs
@crates/cupel/src/model/context_item.rs
@crates/cupel/src/model/context_budget.rs
@crates/cupel/src/model/context_kind.rs
@crates/cupel/src/model/context_source.rs
@crates/cupel/src/model/overflow_strategy.rs
@crates/cupel/src/model/scored_item.rs
@crates/cupel/src/scorer/mod.rs
@crates/cupel/src/scorer/composite.rs
@crates/cupel/src/scorer/frequency.rs
@crates/cupel/src/scorer/kind.rs
@crates/cupel/src/scorer/priority.rs
@crates/cupel/src/scorer/recency.rs
@crates/cupel/src/scorer/reflexive.rs
@crates/cupel/src/scorer/scaled.rs
@crates/cupel/src/scorer/tag.rs
@crates/cupel/src/slicer/mod.rs
@crates/cupel/src/slicer/greedy.rs
@crates/cupel/src/slicer/knapsack.rs
@crates/cupel/src/slicer/quota.rs
@crates/cupel/src/placer/mod.rs
@crates/cupel/src/placer/chronological.rs
@crates/cupel/src/placer/u_shaped.rs
@crates/cupel/src/pipeline/mod.rs
@crates/cupel/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module-level docs to all 6 public modules</name>
  <files>
  crates/cupel/src/model/mod.rs
  crates/cupel/src/scorer/mod.rs
  crates/cupel/src/slicer/mod.rs
  crates/cupel/src/placer/mod.rs
  crates/cupel/src/pipeline/mod.rs
  crates/cupel/src/error.rs
  </files>
  <action>
  Add `//!` module-level doc comments to the top of each module file. Each module doc should contain:
  1. A one-sentence purpose statement
  2. A brief explanation of the module's role in the pipeline
  3. A list of key types provided
  4. A mini code example (compilable doctest)

  **Specific depth per module:**

  **model/mod.rs** — "Data types that flow through the cupel pipeline." List all 7 types. Mini example showing ContextItemBuilder and ContextBudget construction. Mention chrono dependency for timestamps.

  **scorer/mod.rs** — "Scoring strategies that compute relevance for context items." This module gets DEEPER treatment:
  - Explain the Scorer trait contract (pure function, ordinal-only, [0,1] convention)
  - Include the scorer comparison table (same content as README but in rustdoc format):
    | Scorer | Strategy | Input | Use Case |
    Each row: scorer name (as link), type (rank/absolute/relative), required field, one-line use case
  - Mini example: CompositeScorer with RecencyScorer + KindScorer
  - Note about ScaledScorer for normalization

  **slicer/mod.rs** — "Slicing strategies that select items to fit within a token budget." Explain the Slicer trait (receives sorted items, returns subset). List 3 slicers with one-line descriptions. Mini example with GreedySlice.

  **placer/mod.rs** — "Placement strategies that determine final item ordering." Explain the Placer trait (receives merged items, returns ordered list). Describe U-shaped attention and chronological strategies. Mini example.

  **pipeline/mod.rs** — "The fixed 6-stage context selection pipeline." Explain the stage flow: Classify → Score → Deduplicate → Sort → Slice → Place. Describe what each stage does in 1-2 sentences. Builder pattern example.

  **error.rs** — "Error types for pipeline construction and execution." List the error variants with one-line descriptions. No code example needed (the existing doc comments on CupelError are sufficient).

  IMPORTANT:
  - All doctests must compile. Use `# fn main() -> Result<(), cupel::CupelError> {` hidden wrappers where needed.
  - Use `# ` prefix to hide boilerplate lines in doctests.
  - Do NOT modify existing code, only ADD doc comments.
  - Do NOT modify existing doc comments on structs/traits/methods — only add `//!` module docs.
  </action>
  <verify>
  Run: `rtk cargo test --doc --manifest-path crates/cupel/Cargo.toml`
  Run: `rtk cargo test --doc --all-features --manifest-path crates/cupel/Cargo.toml`
  Both must pass.
  </verify>
  <done>All 6 public modules have //! doc comments with purpose, type list, and compilable examples. Scorer module has comparison table. Pipeline module has stage flow description.</done>
</task>

<task type="auto">
  <name>Task 2: Add doctests to all public structs and traits</name>
  <files>
  crates/cupel/src/model/context_item.rs
  crates/cupel/src/model/context_budget.rs
  crates/cupel/src/model/context_kind.rs
  crates/cupel/src/model/context_source.rs
  crates/cupel/src/model/overflow_strategy.rs
  crates/cupel/src/model/scored_item.rs
  crates/cupel/src/scorer/composite.rs
  crates/cupel/src/scorer/frequency.rs
  crates/cupel/src/scorer/kind.rs
  crates/cupel/src/scorer/priority.rs
  crates/cupel/src/scorer/recency.rs
  crates/cupel/src/scorer/reflexive.rs
  crates/cupel/src/scorer/scaled.rs
  crates/cupel/src/scorer/tag.rs
  crates/cupel/src/slicer/greedy.rs
  crates/cupel/src/slicer/knapsack.rs
  crates/cupel/src/slicer/quota.rs
  crates/cupel/src/placer/chronological.rs
  crates/cupel/src/placer/u_shaped.rs
  </files>
  <action>
  Add a compilable doctest (`/// # Examples` section) to every public struct and trait that does not already have one. Each doctest should demonstrate the most common usage pattern for that type.

  **Guidelines for doctests:**
  - Use `# fn main() -> Result<(), cupel::CupelError> {` and `# Ok(()) }` hidden wrappers for `?` operator
  - Keep examples minimal — show construction and one key operation
  - Use realistic LLM-domain values (e.g., "system prompt", "tool output", "user message")
  - For scorers: show construction + scoring a single item
  - For slicers: show construction + slicing a small set
  - For placers: show construction + placing a small set
  - For model types: show construction via builder or constructor

  **Types that need doctests (check if they already have one first):**
  - ContextItem (via ContextItemBuilder) — already has some docs, add example if missing
  - ContextItemBuilder — add example showing full builder chain
  - ContextBudget — add example showing new() with validation
  - ContextKind — add example showing from_static and custom kinds
  - ContextSource — add example showing from_static and custom sources
  - OverflowStrategy — add example showing variants
  - ScoredItem — add example showing construction
  - All 8 scorers (RecencyScorer, PriorityScorer, KindScorer, TagScorer, FrequencyScorer, ReflexiveScorer, CompositeScorer, ScaledScorer)
  - All 3 slicers (GreedySlice, KnapsackSlice, QuotaSlice)
  - Both placers (ChronologicalPlacer, UShapedPlacer)
  - Pipeline and PipelineBuilder
  - Scorer trait, Slicer trait, Placer trait

  IMPORTANT:
  - ONLY add doc comments with examples. Do NOT modify existing code or doc comments.
  - Preserve existing doc comments and add the example section below them.
  - For ScoredItem doctests, note that `score` and `item` fields are pub (check source first).
  - For serde-gated types: do NOT add serde-specific doctests (those go in the serde_roundtrip example).
  </action>
  <verify>
  Run: `rtk cargo test --doc --manifest-path crates/cupel/Cargo.toml`
  Run: `rtk cargo test --doc --all-features --manifest-path crates/cupel/Cargo.toml`
  Run: `rtk cargo doc --no-deps --all-features --manifest-path crates/cupel/Cargo.toml 2>&1` — zero warnings
  All must pass.
  </verify>
  <done>Every public struct and trait has at least one compilable doctest. cargo test --doc passes with and without features. cargo doc builds with zero warnings.</done>
</task>

</tasks>

<verification>
1. `cargo test --doc --manifest-path crates/cupel/Cargo.toml` — all doctests pass (no features)
2. `cargo test --doc --all-features --manifest-path crates/cupel/Cargo.toml` — all doctests pass (with serde)
3. `cargo doc --no-deps --all-features --manifest-path crates/cupel/Cargo.toml` — zero warnings
4. Manual review: every `mod.rs` has `//!` comments; every public type has `# Examples`
</verification>

<success_criteria>
- All 6 public modules have //! doc comments with purpose, type list, and examples
- Scorer module has comparison table in rustdoc format
- Pipeline module has 6-stage flow description
- Every public struct and trait has at least one compilable doctest
- All doctests pass with and without features
- cargo doc builds with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/21-docs-rs-documentation-and-examples/21-02-SUMMARY.md`
</output>
