---
phase: 18-dual-language-ci
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .github/workflows/release-rust.yml
autonomous: true
must_haves:
  truths:
    - Release workflow has workflow_dispatch trigger with dry-run boolean input
    - Release workflow runs full test suite (fmt, clippy, test, cargo-deny) before any publish step
    - Dry-run completes successfully without publishing
    - Publish step is gated behind release environment and dry-run=false condition
    - Successful publish creates a GitHub Release with rust-v{version} tag
  artifacts:
    - path: .github/workflows/release-rust.yml
      provides: Rust release workflow with dry-run support, environment gating, and GitHub Release creation
      min_lines: 60
  key_links:
    - from: .github/workflows/release-rust.yml
      to: .github/workflows/release.yml
      via: "mirrors .NET release workflow structure (workflow_dispatch, dry-run, environment, gh-release)"
    - from: .github/workflows/release-rust.yml
      to: crates/cupel/Cargo.toml
      via: "--manifest-path and cargo metadata for version extraction"
    - from: .github/workflows/release-rust.yml
      to: crates/cupel/deny.toml
      via: "cargo-deny check in pre-publish validation"
---

<objective>
Create the Rust release workflow with dry-run support and verify it completes without error.
Purpose: Enable controlled crate publishing with full test gating, environment protection, and dry-run validation before Phase 19 enables actual crates.io publishing.
Output: release-rust.yml
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/18-dual-language-ci/18-CONTEXT.md
@.planning/phases/pending/18-dual-language-ci/18-RESEARCH.md
@.planning/phases/pending/18-dual-language-ci/18-01-SUMMARY.md
@.github/workflows/release.yml
@.github/workflows/ci-rust.yml
@crates/cupel/Cargo.toml
@rust-toolchain.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Rust release workflow</name>
  <files>.github/workflows/release-rust.yml</files>
  <action>
Create `.github/workflows/release-rust.yml` mirroring the structure of `.github/workflows/release.yml` (the .NET release workflow).

**Workflow configuration:**
- Name: `Publish to crates.io`
- Trigger: `workflow_dispatch` with `dry-run` boolean input (default: false), same pattern as .NET release.yml
- Permissions: `contents: write` (for GitHub Release creation)

**Job 1: `test`** (runs on `ubuntu-latest`)
Full pre-publish validation:
1. `actions/checkout@v4` with `fetch-depth: 0`
2. `dtolnay/rust-toolchain@1.85.0` with components: `rustfmt, clippy`
3. `Swatinem/rust-cache@v2` with `workspaces: "crates/cupel -> target"`
4. `cargo fmt --check --manifest-path crates/cupel/Cargo.toml`
5. `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings`
6. `cargo test --manifest-path crates/cupel/Cargo.toml`
7. `EmbarkStudios/cargo-deny-action@v2` with `manifest-path: crates/cupel/Cargo.toml`
8. `cargo package --manifest-path crates/cupel/Cargo.toml --list` (verify package contents)

**Job 2: `publish`** (runs on `ubuntu-latest`)
- `needs: test`
- `if: ${{ inputs.dry-run != true }}`
- `environment: release` (GitHub environment with approval gate)
- Steps:
  1. Verify running from main branch (same pattern as .NET release.yml)
  2. `actions/checkout@v4` with `fetch-depth: 0`
  3. `dtolnay/rust-toolchain@1.85.0`
  4. Extract version: `cargo metadata --manifest-path crates/cupel/Cargo.toml --format-version 1 --no-deps | jq -r '.packages[0].version'` → output as `version`
  5. `cargo publish --manifest-path crates/cupel/Cargo.toml` (uses `CARGO_REGISTRY_TOKEN` secret — Phase 19 will configure this)
  6. `softprops/action-gh-release@v2` with `tag_name: rust-v${{ steps.version.outputs.version }}`, `name: cupel rust-v${{ steps.version.outputs.version }}`, `generate_release_notes: true`. Use `rust-v` prefix to avoid tag collision with .NET `v` tags.

Match the conventions of the .NET `release.yml` exactly: step naming style, error message format for branch check, workflow_dispatch input structure.
  </action>
  <verify>
Validate YAML syntax of release-rust.yml.
Confirm the workflow has:
- workflow_dispatch trigger with dry-run boolean input
- test job with all 4 cargo checks + package --list
- publish job with needs: test, environment: release, and dry-run gate
- rust-v tag prefix in GitHub Release step
- main branch verification in publish job
  </verify>
  <done>
- `release-rust.yml` exists with valid YAML
- Has workflow_dispatch trigger with dry-run input matching .NET release pattern
- Test job runs fmt, clippy, test, cargo-deny, and package verification
- Publish job is gated behind `release` environment and `dry-run != true` condition
- GitHub Release uses `rust-v{version}` tag prefix
- Main branch check prevents publishing from feature branches
  </done>
</task>
</tasks>

<verification>
1. `release-rust.yml` is valid YAML
2. Workflow structure mirrors `.NET` release.yml conventions
3. Test job covers all 4 cargo checks (fmt, clippy, test, deny) plus package verification
4. Publish job has correct gating: `needs: test`, `if: dry-run != true`, `environment: release`
5. Version extraction uses `cargo metadata` (not manual parsing)
6. Tag prefix is `rust-v` (not `v`) to avoid collision with .NET tags
7. Branch verification step prevents non-main publishes
</verification>

<success_criteria>
- Release workflow ready for dry-run validation (Phase 19 will add CARGO_REGISTRY_TOKEN and test actual publish)
- Workflow follows same conventions as .NET release.yml
- No overlap or collision with .NET release tags
</success_criteria>

<output>
After completion, create `.planning/phases/pending/18-dual-language-ci/18-02-SUMMARY.md`
</output>
