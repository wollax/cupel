---
phase: 16-pre-flight--crate-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - crates/cupel/Cargo.toml
  - crates/cupel/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Standalone Cargo.toml has all required crates.io publishing fields with no workspace-inherited values"
    - "cargo check passes on the empty lib.rs placeholder"
    - "Cargo.toml version is 1.0.0 (spec-aligned versioning)"
    - "include field is present to control what ships in the crate tarball"
  artifacts:
    - path: "crates/cupel/Cargo.toml"
      provides: "Complete standalone crate manifest for crates.io publishing"
      contains: "name = \"cupel\""
    - path: "crates/cupel/src/lib.rs"
      provides: "Empty placeholder for cargo check to pass"
      min_lines: 1
  key_links:
    - from: "crates/cupel/Cargo.toml"
      to: "rust-toolchain.toml"
      via: "MSRV alignment — rust-version field matches toolchain MSRV"
      pattern: "rust-version = \"1\\.85\""
    - from: "crates/cupel/Cargo.toml"
      to: "crates/cupel/src/lib.rs"
      via: "Default lib target — Cargo convention"
      pattern: "lib.rs"
---

<objective>
Create the standalone Cargo.toml and empty lib.rs placeholder at `crates/cupel/`, then verify with `cargo check`.

Purpose: This is the crate scaffold that all subsequent phases build on. The Cargo.toml must have every field required for crates.io publishing with zero workspace inheritance. Getting this right now prevents hard-to-diagnose issues during `cargo publish` in Phase 19.

Output: A `crates/cupel/` directory containing a complete `Cargo.toml` and minimal `src/lib.rs` that passes `cargo check`.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/16-pre-flight--crate-scaffold/16-RESEARCH.md
@.planning/phases/pending/16-pre-flight--crate-scaffold/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo.toml and lib.rs placeholder</name>
  <files>crates/cupel/Cargo.toml, crates/cupel/src/lib.rs</files>
  <action>
  1. Create the directory structure: `crates/cupel/src/`

  2. Create `crates/cupel/Cargo.toml` with ALL fields fully specified (no workspace inheritance). Use the crate name confirmed in Plan 01's summary (expected: `cupel`; if unavailable, use the fallback name from the summary).

     ```toml
     [package]
     name = "cupel"
     version = "1.0.0"
     edition = "2024"
     rust-version = "1.85"
     license = "MIT"
     repository = "https://github.com/wollax/cupel"
     description = "Context window management pipeline for LLM applications"
     readme = "README.md"
     categories = ["algorithms", "text-processing"]
     keywords = ["llm", "context-window", "pipeline", "token-budget"]
     include = [
         "src/**/*.rs",
         "Cargo.toml",
         "LICENSE",
         "README.md",
     ]

     [dependencies]
     chrono = "0.4"
     thiserror = "2"

     [dev-dependencies]
     toml = "0.8"
     serde = { version = "1", features = ["derive"] }
     serde_json = "1"
     ```

     Key decisions baked in:
     - `version = "1.0.0"` — spec-aligned versioning (implements Spec 1.0), independent of .NET package version
     - `edition = "2024"` — matches existing assay-cupel crate, requires MSRV 1.85
     - `rust-version = "1.85"` — consumer-facing MSRV declaration (Rust 2024 edition minimum)
     - `license = "MIT"` — matches existing assay workspace license
     - `chrono = "0.4"` — use default features for now; exact feature refinement happens in Phase 17 when source files are analyzed
     - `include` — controls what ships in the crate tarball. Phase 17 will add `conformance/**/*.toml` when vectors are vendored
     - NO `[lints]` section with workspace reference — the existing assay-cupel has `[lints] workspace = true` which is NOT valid without a workspace root

  3. Create `crates/cupel/src/lib.rs` with a minimal doc comment placeholder:

     ```rust
     //! Context window management pipeline for LLM applications.
     ```

     This single-line file is sufficient for `cargo check` to pass.
  </action>
  <verify>
  - `cat crates/cupel/Cargo.toml` shows all required fields with no `.workspace = true` entries
  - `cat crates/cupel/src/lib.rs` shows the doc comment
  - Verify no workspace inheritance: `grep -c "workspace" crates/cupel/Cargo.toml` returns 0
  </verify>
  <done>Standalone `Cargo.toml` exists at `crates/cupel/` with all crates.io publishing fields. `lib.rs` placeholder exists at `crates/cupel/src/`. No workspace-inherited fields present.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test with cargo check</name>
  <files></files>
  <action>
  Run `cargo check --manifest-path crates/cupel/Cargo.toml` to verify the scaffold compiles.

  This validates:
  - All `Cargo.toml` fields parse correctly
  - Dependencies resolve from crates.io
  - `edition = "2024"` is accepted by the toolchain
  - `lib.rs` compiles (trivially, as it's just a doc comment)

  If `cargo check` fails:
  - "failed to parse manifest" → check for `.workspace = true` leakage
  - "failed to select a version" → dependency version mismatch, check crates.io for latest compatible versions
  - "unknown edition" → toolchain too old for edition 2024, verify `rust-toolchain.toml` from Plan 01

  Also run `cargo fmt --check --manifest-path crates/cupel/Cargo.toml` and `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` to confirm lint tooling works on the scaffold.
  </action>
  <verify>
  - `cargo check --manifest-path crates/cupel/Cargo.toml` exits with code 0
  - `cargo fmt --check --manifest-path crates/cupel/Cargo.toml` exits with code 0
  - `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` exits with code 0
  - `crates/cupel/target/` is NOT shown in `git status` (covered by `.gitignore` from Plan 01)
  </verify>
  <done>`cargo check`, `cargo fmt --check`, and `cargo clippy` all pass on the scaffold. Build artifacts are gitignored.</done>
</task>

</tasks>

<verification>
- `cargo check --manifest-path crates/cupel/Cargo.toml` passes (exit code 0)
- `cargo fmt --check --manifest-path crates/cupel/Cargo.toml` passes
- `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` passes
- `grep -c "workspace" crates/cupel/Cargo.toml` returns 0 (no workspace inheritance)
- `git status` does not show `target/` directories (gitignore working)
- `crates/cupel/Cargo.toml` contains all required publishing fields: name, version, edition, rust-version, license, repository, description, categories, keywords, include
</verification>

<success_criteria>
1. `crates/cupel/Cargo.toml` is a complete standalone manifest with all crates.io required fields and zero workspace-inherited values
2. `crates/cupel/src/lib.rs` exists as a minimal placeholder
3. `cargo check --manifest-path crates/cupel/Cargo.toml` passes
4. `cargo fmt --check` and `cargo clippy -- -D warnings` pass on the scaffold
5. Build artifacts (`target/`) are not visible in `git status`
</success_criteria>

<output>
After completion, create `.planning/phases/pending/16-pre-flight--crate-scaffold/16-02-SUMMARY.md`

Include in the summary:
- Final Cargo.toml contents (all fields)
- Cargo check output confirming success
- Any dependency version notes (if versions were adjusted from research recommendations)
</output>
