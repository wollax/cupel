---
phase: "04"
plan: "04-03"
title: "Stable sort tiebreaking test and composite scorer benchmark"
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - tests/Wollax.Cupel.Tests/Scoring/CompositeScorerTests.cs
  - tests/Wollax.Cupel.Tests/Scoring/ScaledScorerTests.cs
  - benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs
autonomous: true
must_haves:
  truths:
    - "Stable sort with index-augmented comparison preserves insertion order for identical composite scores"
    - "CompositeScorer containing ScaledScorer triggers cycle detection correctly (no false positives on valid trees)"
    - "CompositeScorer and ScaledScorer show zero Gen0 collections in MemoryDiagnoser benchmark"
  artifacts:
    - path: "tests/Wollax.Cupel.Tests/Scoring/CompositeScorerTests.cs"
      provides: "Stable sort tiebreaking test and CompositeScorer+ScaledScorer integration tests"
    - path: "benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs"
      provides: "Updated benchmark with CompositeScorer and ScaledScorer methods"
  key_links:
    - from: "stable sort test"
      to: "Array.Sort with index-augmented comparer"
      via: "demonstrates the pattern Phase 5 pipeline will adopt"
    - from: "CompositeScorer"
      to: "ScaledScorer"
      via: "cycle detection traverses ScaledScorer.Inner — verified by integration tests"
    - from: "ScorerBenchmark"
      to: "CompositeScorer, ScaledScorer"
      via: "benchmark methods verifying zero-allocation"
---

<objective>
Add integration tests that exercise CompositeScorer + ScaledScorer together, verify stable sort tiebreaking with identical composite scores, and extend the benchmark to cover both new scorers. This plan validates the full Phase 4 success criteria including cross-component composition.
</objective>

<context>
@src/Wollax.Cupel/Scoring/CompositeScorer.cs
@src/Wollax.Cupel/Scoring/ScaledScorer.cs
@tests/Wollax.Cupel.Tests/Scoring/CompositeScorerTests.cs
@tests/Wollax.Cupel.Tests/Scoring/ScaledScorerTests.cs
@benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs
@.planning/phases/pending/04-composite-scoring/04-RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Cross-component integration tests and stable sort tiebreaking</name>
  <files>
    tests/Wollax.Cupel.Tests/Scoring/CompositeScorerTests.cs
    tests/Wollax.Cupel.Tests/Scoring/ScaledScorerTests.cs
  </files>
  <behavior>
    Add to CompositeScorerTests:

    CompositeScorer + ScaledScorer integration:
    - CompositeWithScaledScorer_ProducesValidScores: CompositeScorer containing a ScaledScorer(RecencyScorer) and a PriorityScorer — produces valid scores
    - CompositeWithScaledScorer_OrdinalRelationships: verify that weighting between a scaled and unscaled scorer produces expected ordinal outcomes
    - ScaledScorerWrappingComposite_Succeeds: ScaledScorer wrapping a CompositeScorer — construction succeeds, produces [0, 1] output
    - CompositeWithScaledComposite_NoCycleDetectionFalsePositive: CompositeScorer containing ScaledScorer(anotherComposite) — no false positive cycle, valid DAG

    Stable sort tiebreaking test (Phase 4 success criteria #4):
    - IdenticalCompositeScores_PreserveInsertionOrder: create 5 items that all produce identical composite scores (e.g., all FutureRelevanceHint=null with ReflexiveScorer). Build (score, originalIndex) array, sort with index-augmented comparer (score DESC, index ASC). Verify that after sort, indices are 0, 1, 2, 3, 4 (insertion order preserved).
    - TiedScoresWithDifferentInsertionOrder_StableSort: create items where some have identical scores but different insertion indices. Verify stable ordering within tied groups.

    Add to ScaledScorerTests:
    - ScaledComposite_OutputInZeroToOne: ScaledScorer wrapping a CompositeScorer — all outputs in [0, 1]

    Follow TUnit async Task pattern. The stable sort tests demonstrate the index-augmented Array.Sort pattern from RESEARCH.md pattern 4 — this establishes the technique that Phase 5 pipeline will adopt.
  </behavior>
  <implementation>
    The stable sort pattern to use in tests:
    ```csharp
    var scored = new (double Score, int Index)[items.Count];
    for (var i = 0; i < items.Count; i++)
        scored[i] = (scorer.Score(items[i], items), i);

    Array.Sort(scored, static (a, b) =>
    {
        var cmp = b.Score.CompareTo(a.Score); // descending
        return cmp != 0 ? cmp : a.Index.CompareTo(b.Index); // ascending index
    });
    ```

    This is test-only code — the sort helper does not need to be in the production library (Phase 5 will add it to the pipeline). The tests prove the technique works with CompositeScorer output.
  </implementation>
  <verify>
    rtk dotnet test /Users/wollax/Git/personal/cupel/tests/Wollax.Cupel.Tests --filter "CompositeScorer|ScaledScorer"
    rtk dotnet test /Users/wollax/Git/personal/cupel/tests/Wollax.Cupel.Tests
  </verify>
  <done>
    - All CompositeScorer + ScaledScorer integration tests pass
    - Stable sort tiebreaking test passes (identical scores preserve insertion order)
    - No cycle detection false positives on valid CompositeScorer+ScaledScorer DAGs
    - Full test suite passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ScorerBenchmark with CompositeScorer and ScaledScorer</name>
  <files>
    benchmarks/Wollax.Cupel.Benchmarks/ScorerBenchmark.cs
  </files>
  <action>
    Add to the existing ScorerBenchmark class:

    1. In [GlobalSetup], add:
       - _compositeScorer = new CompositeScorer([(new RecencyScorer(), 2.0), (new PriorityScorer(), 1.0), (new ReflexiveScorer(), 1.0)])
       - _scaledScorer = new ScaledScorer(new RecencyScorer())
       - _scaledCompositeScorer = new ScaledScorer(_compositeScorer)  // ScaledScorer wrapping CompositeScorer

    2. Add three new [Benchmark] methods following existing pattern:
       - CompositeScorer_Score: for-loop scoring all items with _compositeScorer, return sum
       - ScaledScorer_Score: for-loop scoring all items with _scaledScorer, return sum
       - ScaledCompositeScorer_Score: for-loop scoring all items with _scaledCompositeScorer, return sum

    3. Add `using Wollax.Cupel.Scoring;` if not already present (it should be from existing benchmark methods).

    Each benchmark method returns double (sum) to prevent dead code elimination.
  </action>
  <verify>
    rtk dotnet build /Users/wollax/Git/personal/cupel/benchmarks/Wollax.Cupel.Benchmarks
  </verify>
  <done>
    - ScorerBenchmark.cs builds with new benchmark methods
    - CompositeScorer, ScaledScorer, and ScaledComposite benchmarks exist
    - All benchmark methods return a value (not void)
  </done>
</task>

</tasks>

<verification>
Build everything and run full test suite:
```bash
rtk dotnet build /Users/wollax/Git/personal/cupel/Cupel.slnx
rtk dotnet test /Users/wollax/Git/personal/cupel/tests/Wollax.Cupel.Tests
rtk dotnet build /Users/wollax/Git/personal/cupel/benchmarks/Wollax.Cupel.Benchmarks
```

Optionally run a quick benchmark smoke test:
```bash
cd /Users/wollax/Git/personal/cupel && rtk dotnet run --project benchmarks/Wollax.Cupel.Benchmarks -c Release -- --filter "ScorerBenchmark" --job short
```
</verification>

<success_criteria>
- [ ] CompositeScorer + ScaledScorer integration tests pass (valid DAGs, ordinal relationships)
- [ ] Stable sort tiebreaking test passes (identical scores preserve insertion order)
- [ ] ScorerBenchmark has CompositeScorer, ScaledScorer, and ScaledComposite benchmark methods
- [ ] Benchmark project builds successfully
- [ ] Full solution builds with zero warnings
- [ ] Full test suite passes (no regressions)
</success_criteria>
