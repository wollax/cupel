---
phase: 17-crate-migration--conformance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cupel/src/lib.rs
  - crates/cupel/src/error.rs
  - crates/cupel/src/model/*.rs
  - crates/cupel/src/pipeline/*.rs
  - crates/cupel/src/placer/*.rs
  - crates/cupel/src/scorer/*.rs
  - crates/cupel/src/slicer/*.rs
  - crates/cupel/conformance/required/**/*.toml
  - crates/cupel/Cargo.toml
autonomous: true
must_haves:
  truths:
    - 26 .rs source files exist at crates/cupel/src/ with correct module hierarchy
    - 28 conformance vectors exist at crates/cupel/conformance/required/ (mirroring repo-root conformance/required/)
    - Cargo.toml include array contains "conformance/**/*.toml"
    - cargo build compiles without errors
    - cargo clippy -- -D warnings passes
    - cargo fmt --check passes
  artifacts:
    - crates/cupel/src/lib.rs (real module declarations, not placeholder)
    - crates/cupel/src/error.rs
    - crates/cupel/src/model/ (7 files)
    - crates/cupel/src/pipeline/ (7 files)
    - crates/cupel/src/placer/ (3 files)
    - crates/cupel/src/scorer/ (9 files)
    - crates/cupel/src/slicer/ (4 files)
    - crates/cupel/conformance/required/ (28 .toml vectors in 4 subdirs)
  key_links:
    - Cargo.toml dependencies must match what source files import (chrono, thiserror)
    - Source files contain zero references to assay_cupel — no import changes needed in src/
    - Conformance vectors at crate-local path must be byte-identical to repo-root canonical copies
---

<objective>
Migrate all 26 Rust source files from assay/crates/assay-cupel/src/ into cupel/crates/cupel/src/, copy 28 conformance test vectors into the crate, and update Cargo.toml to include vectors in the package tarball.

Purpose: Establishes the compilable crate with all production source code and test data in place.
Output: A crate that builds, passes clippy, and passes fmt — ready for test migration in the next plan.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/17-crate-migration--conformance-verification/17-CONTEXT.md
@.planning/phases/pending/17-crate-migration--conformance-verification/17-RESEARCH.md
@crates/cupel/Cargo.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Copy source files from assay into cupel crate</name>
  <files>
    crates/cupel/src/ (26 files across 5 subdirectories)
  </files>
  <action>
    1. Remove the placeholder `crates/cupel/src/lib.rs` (empty file from Phase 16)
    2. Copy the entire `src/` directory from `/Users/wollax/Git/personal/assay/crates/assay-cupel/src/` to `/Users/wollax/Git/personal/cupel/crates/cupel/src/`
       - Use `cp -R` to preserve directory structure
       - This includes: lib.rs, error.rs, model/ (7 files), pipeline/ (7 files), placer/ (3 files), scorer/ (9 files), slicer/ (4 files)
    3. Verify NO source files reference `assay_cupel` — they should not, but confirm with `grep -r 'assay_cupel' crates/cupel/src/`
    4. Count files: `find crates/cupel/src -name '*.rs' | wc -l` should return 32 (26 source files + 6 mod.rs files... verify actual count matches source)

    IMPORTANT: Source files are in a DIFFERENT repo at /Users/wollax/Git/personal/assay/crates/assay-cupel/. This is a cross-repo copy, not a git mv.
  </action>
  <verify>
    - `find crates/cupel/src -name '*.rs' | wc -l` matches the count from the source repo
    - `grep -r 'assay_cupel' crates/cupel/src/` returns no results
    - `diff -r /Users/wollax/Git/personal/assay/crates/assay-cupel/src/ crates/cupel/src/` shows no differences
  </verify>
  <done>All 26 source files exist at crates/cupel/src/ with identical content to the assay source, and no references to assay_cupel</done>
</task>

<task type="auto">
  <name>Task 2: Copy conformance vectors and update Cargo.toml include</name>
  <files>
    crates/cupel/conformance/required/ (28 .toml files in 4 subdirs)
    crates/cupel/Cargo.toml
  </files>
  <action>
    1. Copy conformance vectors from repo-root canonical location into the crate:
       `cp -R conformance/required/ crates/cupel/conformance/required/`
       - This creates: crates/cupel/conformance/required/{pipeline,placing,scoring,slicing}/ with 28 total .toml files
    2. Verify the crate-local copy is identical to canonical:
       `diff -r conformance/required/ crates/cupel/conformance/required/`
    3. Update `crates/cupel/Cargo.toml` — add `"conformance/**/*.toml"` to the `include` array:
       ```toml
       include = [
           "src/**/*.rs",
           "conformance/**/*.toml",
           "Cargo.toml",
           "LICENSE",
           "README.md",
       ]
       ```
  </action>
  <verify>
    - `find crates/cupel/conformance/required -name '*.toml' | wc -l` returns 28
    - `diff -r conformance/required/ crates/cupel/conformance/required/` shows no differences
    - `grep 'conformance' crates/cupel/Cargo.toml` shows the include entry
  </verify>
  <done>28 conformance vectors exist at crates/cupel/conformance/required/, are byte-identical to repo-root canonical copies, and Cargo.toml include array lists them</done>
</task>

<task type="auto">
  <name>Task 3: Verify crate builds cleanly</name>
  <files>(no files modified — verification only)</files>
  <action>
    1. Run `cargo build --manifest-path crates/cupel/Cargo.toml` — must succeed
    2. Run `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` — must pass with no warnings
    3. Run `cargo fmt --manifest-path crates/cupel/Cargo.toml --check` — must pass
    4. If any step fails, diagnose and fix the actual issue (do NOT weaken checks)
  </action>
  <verify>
    - All three commands exit 0
  </verify>
  <done>Crate compiles, passes clippy with -D warnings, and passes fmt check</done>
</task>
</tasks>

<verification>
1. `find crates/cupel/src -name '*.rs' | wc -l` — matches source repo count
2. `grep -r 'assay_cupel' crates/cupel/src/` — returns nothing
3. `diff -r conformance/required/ crates/cupel/conformance/required/` — no differences
4. `cargo build --manifest-path crates/cupel/Cargo.toml` — exit 0
5. `cargo clippy --manifest-path crates/cupel/Cargo.toml -- -D warnings` — exit 0
6. `cargo fmt --manifest-path crates/cupel/Cargo.toml --check` — exit 0
</verification>

<success_criteria>
- 26 Rust source files from assay live at crates/cupel/src/ and compile cleanly
- 28 conformance vectors vendored into crates/cupel/conformance/required/
- Cargo.toml includes conformance vectors in the package tarball
- cargo build, clippy, and fmt all pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-crate-migration--conformance-verification/17-01-SUMMARY.md`
</output>
