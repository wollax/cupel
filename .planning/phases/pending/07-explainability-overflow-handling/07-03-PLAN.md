---
phase: 07-explainability-overflow-handling
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/Wollax.Cupel/PublicAPI.Unshipped.txt
  - tests/Wollax.Cupel.Tests/Pipeline/ExplainabilityIntegrationTests.cs
autonomous: true
must_haves:
  truths:
    - PublicAPI.Unshipped.txt contains entries for all new public types and members
    - Old ExclusionReason entries removed from PublicAPI.Unshipped.txt
    - Integration tests verify end-to-end explainability across realistic scenarios
    - All phase success criteria are met
  artifacts:
    - src/Wollax.Cupel/PublicAPI.Unshipped.txt — updated with all Phase 7 API surface
    - tests/Wollax.Cupel.Tests/Pipeline/ExplainabilityIntegrationTests.cs
  key_links:
    - PublicAPI entries must exactly match the analyzer expectations (run build to verify)
    - Integration tests must cover the 4 success criteria from the phase definition
---

<objective>
Finalize Phase 7 with PublicAPI surface updates and end-to-end integration tests that validate all four phase success criteria. This plan ensures the API surface is tracked and the explainability features work together in realistic scenarios.
</objective>

<execution_context>
Plans 01 and 02 have delivered all types and pipeline integration. This plan validates the complete feature set and locks the API surface.
</execution_context>

<context>
@src/Wollax.Cupel/PublicAPI.Unshipped.txt — needs new entries, old ExclusionReason entries updated
@src/Wollax.Cupel/CupelPipeline.cs — DryRun() and overflow handling from Plan 02
@src/Wollax.Cupel/PipelineBuilder.cs — WithOverflowStrategy() from Plan 02
@src/Wollax.Cupel/Diagnostics/SelectionReport.cs — evolved from Plan 01
@src/Wollax.Cupel/Diagnostics/IncludedItem.cs — from Plan 01
@src/Wollax.Cupel/Diagnostics/ExcludedItem.cs — from Plan 01
@src/Wollax.Cupel/Diagnostics/OverflowEvent.cs — from Plan 01
@src/Wollax.Cupel/Diagnostics/OverflowStrategy.cs — from Plan 01
@src/Wollax.Cupel/Diagnostics/InclusionReason.cs — from Plan 01
@src/Wollax.Cupel/Diagnostics/ExclusionReason.cs — evolved from Plan 01
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update PublicAPI.Unshipped.txt</name>
  <files>
    src/Wollax.Cupel/PublicAPI.Unshipped.txt
  </files>
  <action>
  1. Remove old ExclusionReason entries that no longer exist:
     - `Wollax.Cupel.Diagnostics.ExclusionReason.LowScore`
     - `Wollax.Cupel.Diagnostics.ExclusionReason.Duplicate`
     - `Wollax.Cupel.Diagnostics.ExclusionReason.QuotaExceeded`

  2. Add new ExclusionReason values:
     - `ScoredTooLow`, `Deduplicated`, `QuotaCapExceeded`, `QuotaRequireDisplaced`, `NegativeTokens`, `PinnedOverride`, `Filtered`

  3. Add all new types and members. Use `rtk dotnet build src/Wollax.Cupel/` and fix any RS0016 (missing API entry) warnings. The analyzer will tell you exactly which entries are missing.

  Expected new entries include:
  - InclusionReason enum and its values
  - OverflowStrategy enum and its values
  - IncludedItem record and its properties
  - ExcludedItem record and its properties
  - OverflowEvent record and its properties
  - SelectionReport new properties (Included, Excluded, TotalCandidates, TotalTokensConsidered)
  - CupelPipeline.DryRun() method
  - PipelineBuilder.WithOverflowStrategy() method
  </action>
  <verify>
  `rtk dotnet build src/Wollax.Cupel/` produces zero RS0016 or RS0017 warnings.
  </verify>
  <done>
  - PublicAPI.Unshipped.txt is complete with all Phase 7 API surface
  - No RS0016/RS0017 warnings from the public API analyzer
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration tests for Phase 7 success criteria</name>
  <files>
    tests/Wollax.Cupel.Tests/Pipeline/ExplainabilityIntegrationTests.cs
  </files>
  <action>
  Create integration test class that validates all 4 phase success criteria in realistic scenarios.

  **Test scenarios:**

  1. **SC1 — SelectionReport lists included and excluded items with reasons:**
     - 10 items, budget fits 5. Execute with DiagnosticTraceCollector.
     - Assert: Report.Included has 5 items, each with InclusionReason.Scored
     - Assert: Report.Excluded has 5 items, each with ExclusionReason.BudgetExceeded
     - Assert: Excluded items carry their computed scores
     - Assert: Report.TotalCandidates == 10, TotalTokensConsidered == sum of all tokens

  2. **SC1 supplement — Pinned, dedup, negative tokens:**
     - Items include: 2 pinned, 2 duplicates, 1 negative-token, 5 normal
     - Assert: Pinned items in Included with InclusionReason.Pinned
     - Assert: Duplicate in Excluded with ExclusionReason.Deduplicated and DeduplicatedAgainst set
     - Assert: Negative-token item in Excluded with ExclusionReason.NegativeTokens

  3. **SC2 — DryRun idempotency:**
     - Call DryRun() twice with identical input
     - Assert: both results have identical Items (count, order, content)
     - Assert: both reports have identical Included and Excluded lists
     - Assert: Report is always non-null from DryRun()

  4. **SC3 — OverflowStrategy behaviors:**
     - Create scenario where pinned items cause sliced+pinned > TargetTokens
     - Test Throw: assert OverflowException
     - Test Truncate: assert items removed, ExclusionReason.PinnedOverride in report
     - Test Proceed: assert callback invoked with correct OverflowEvent

  5. **SC4 — Observer callback details:**
     - Proceed strategy with callback
     - Assert: OverflowEvent.TokensOverBudget is correct (mergedTokens - targetTokens)
     - Assert: OverflowEvent.OverflowingItems contains the items causing overflow
     - Assert: OverflowEvent.Budget is the pipeline's budget

  Use helper methods for item creation (follow patterns from existing CupelPipelineTests.cs). Use RecencyScorer or ReflexiveScorer for deterministic scoring.
  </action>
  <verify>
  `rtk dotnet test tests/Wollax.Cupel.Tests/ --filter "ExplainabilityIntegration"` — all pass.
  `rtk dotnet test tests/Wollax.Cupel.Tests/` — full suite passes.
  </verify>
  <done>
  - Integration tests validate all 4 phase success criteria
  - Tests cover realistic multi-stage scenarios (pinned + dedup + budget)
  - Full test suite passes with zero failures
  </done>
</task>
</tasks>

<verification>
```bash
rtk dotnet build
rtk dotnet test tests/Wollax.Cupel.Tests/
```
- Zero build warnings (including RS0016/RS0017)
- Full test suite passes
- Phase 7 success criteria verified by integration tests
</verification>

<success_criteria>
1. PublicAPI.Unshipped.txt is complete — zero RS0016/RS0017 warnings
2. Integration tests cover all 4 phase success criteria
3. Full test suite passes with zero failures
4. Phase 7 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-explainability-overflow-handling/07-03-SUMMARY.md`
</output>
