---
phase: 01-project-scaffold-core-models
plan: 04
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/Wollax.Cupel/ContextBudget.cs
  - tests/Wollax.Cupel.Tests/Models/ContextBudgetTests.cs
autonomous: true

must_haves:
  truths:
    - "ContextBudget validates all inputs: negative MaxTokens/TargetTokens/OutputReserve throw, margin outside 0-100 throws, TargetTokens > MaxTokens throws"
    - "ContextBudget exposes all budget parameters as read-only properties"
    - "ContextBudget defaults: OutputReserve=0, ReservedSlots=empty, EstimationSafetyMarginPercent=0"
    - "ContextBudget round-trips through JSON serialization including ReservedSlots with ContextKind keys"
    - "ContextBudget is a sealed class (not a record) — no with-expressions that bypass validation"
  artifacts:
    - path: "src/Wollax.Cupel/ContextBudget.cs"
      provides: "Validated budget model with all PIPE-02 properties"
      contains: "sealed class ContextBudget"
    - path: "tests/Wollax.Cupel.Tests/Models/ContextBudgetTests.cs"
      provides: "TDD tests for construction, validation, defaults, JSON round-trip"
  key_links:
    - from: "ContextBudget.ReservedSlots"
      to: "ContextKind"
      via: "Dictionary<ContextKind, int> — ContextKind as dictionary key requires custom JSON handling"
      pattern: "Dictionary<ContextKind, int>"
    - from: "ContextBudget constructor"
      to: "JsonConstructor attribute"
      via: "[JsonConstructor] enables STJ deserialization through the validated constructor"
      pattern: "JsonConstructor"
---

<objective>
Implement ContextBudget as a validated sealed class using TDD — the budget constraint model that controls how much context the pipeline can select.

Purpose: ContextBudget defines the hard limits and soft goals that slicers use to make selection decisions. Constructor validation ensures no invalid budget configuration can exist at runtime. The ReservedSlots dictionary using ContextKind as key exercises the smart enum's dictionary key serialization.

Output: A sealed class with all 5 PIPE-02 properties, comprehensive constructor validation, [JsonConstructor] for deserialization, and tests covering valid construction, all validation error paths, and JSON round-trip.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/01-project-scaffold-core-models/01-RESEARCH.md (sections: Pattern 3 — Validated Model, Pitfall 4 — JsonConstructor, Pitfall 5 — Smart Enum Dictionary Key Serialization)
@.planning/phases/pending/01-project-scaffold-core-models/phase-1-CONTEXT.md (section: ContextBudget semantics)
@.planning/REQUIREMENTS.md (PIPE-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — ContextBudget validated model</name>
  <files>
    src/Wollax.Cupel/ContextBudget.cs
    tests/Wollax.Cupel.Tests/Models/ContextBudgetTests.cs
  </files>
  <action>
    Follow RED-GREEN-REFACTOR for ContextBudget:

    **RED — Write failing tests** in `tests/Wollax.Cupel.Tests/Models/ContextBudgetTests.cs`:

    Valid construction:
    - Minimal: `new ContextBudget(maxTokens: 100_000, targetTokens: 80_000)` succeeds and properties are readable
    - Full: all 5 parameters provided, all properties return correct values
    - Defaults: OutputReserve defaults to 0, ReservedSlots defaults to empty dictionary, EstimationSafetyMarginPercent defaults to 0

    Validation — each must throw ArgumentOutOfRangeException or ArgumentException:
    - Negative MaxTokens throws
    - Negative TargetTokens throws
    - Negative OutputReserve throws
    - EstimationSafetyMarginPercent < 0 throws
    - EstimationSafetyMarginPercent > 100 throws
    - TargetTokens > MaxTokens throws ArgumentException

    Edge cases:
    - MaxTokens = 0 and TargetTokens = 0 is valid (degenerate but legal)
    - EstimationSafetyMarginPercent = 0 and = 100 are both valid boundary values
    - ReservedSlots with ContextKind keys works: `new Dictionary<ContextKind, int> { [ContextKind.Message] = 1000 }`

    JSON serialization:
    - Round-trip: serialize then deserialize produces a ContextBudget with identical property values
    - ReservedSlots serializes with ContextKind keys as strings (e.g., `{"Message": 1000}`)
    - [JsonPropertyName] attributes produce correct camelCase JSON keys: "maxTokens", "targetTokens", "outputReserve", "reservedSlots", "estimationSafetyMarginPercent"

    Run tests — must FAIL.

    **GREEN — Implement ContextBudget** in `src/Wollax.Cupel/ContextBudget.cs`:
    - `public sealed class ContextBudget` (NOT a record — validation in constructor means with-expressions would bypass checks)
    - Properties (all read-only, set only in constructor):
      - `[JsonPropertyName("maxTokens")] public int MaxTokens { get; }`
      - `[JsonPropertyName("targetTokens")] public int TargetTokens { get; }`
      - `[JsonPropertyName("outputReserve")] public int OutputReserve { get; }`
      - `[JsonPropertyName("reservedSlots")] public IReadOnlyDictionary<ContextKind, int> ReservedSlots { get; }`
      - `[JsonPropertyName("estimationSafetyMarginPercent")] public double EstimationSafetyMarginPercent { get; }`
    - Constructor with `[JsonConstructor]`:
      ```
      [JsonConstructor]
      public ContextBudget(
          int maxTokens,
          int targetTokens,
          int outputReserve = 0,
          IReadOnlyDictionary<ContextKind, int>? reservedSlots = null,
          double estimationSafetyMarginPercent = 0)
      ```
    - Validation in constructor body:
      - `ArgumentOutOfRangeException.ThrowIfNegative(maxTokens)`
      - `ArgumentOutOfRangeException.ThrowIfNegative(targetTokens)`
      - `ArgumentOutOfRangeException.ThrowIfNegative(outputReserve)`
      - `ArgumentOutOfRangeException.ThrowIfNegative(estimationSafetyMarginPercent)`
      - `ArgumentOutOfRangeException.ThrowIfGreaterThan(estimationSafetyMarginPercent, 100)`
      - If `targetTokens > maxTokens`, throw `ArgumentException`

    **Dictionary key serialization for ReservedSlots:**
    Handle the Pitfall 5 issue — ContextKind as dictionary key needs custom STJ handling. Options:
    - Preferred: Create a `ContextKindDictionaryConverter : JsonConverter<IReadOnlyDictionary<ContextKind, int>>` that reads/writes ContextKind keys as strings. Apply via `[JsonConverter]` on the ReservedSlots property.
    - Alternative: If STJ handles it automatically via ContextKind's existing converter, verify with a test and skip the custom converter.

    Test which approach works first. If auto-handling fails with NotSupportedException, implement the custom dictionary converter.

    Run tests — must PASS.

    **REFACTOR** — Clean up if needed. Run tests to confirm.
  </action>
  <verify>
    `dotnet test tests/Wollax.Cupel.Tests` — all ContextBudget tests pass alongside all prior tests.
    `dotnet build Cupel.sln` — zero warnings.
    Specifically verify ReservedSlots JSON round-trip works with ContextKind keys.
  </verify>
  <done>ContextBudget sealed class with all 5 PIPE-02 properties, comprehensive constructor validation covering every error path, [JsonConstructor] deserialization, ReservedSlots dictionary key serialization, and sensible defaults — all proven by tests.</done>
</task>

</tasks>

<verification>
1. `dotnet test tests/Wollax.Cupel.Tests` — all tests pass (smart enums + ContextItem + ContextBudget)
2. `dotnet build Cupel.sln` — zero errors, zero warnings
3. Negative MaxTokens/TargetTokens/OutputReserve throw ArgumentOutOfRangeException
4. EstimationSafetyMarginPercent outside 0-100 throws
5. TargetTokens > MaxTokens throws ArgumentException
6. ReservedSlots with ContextKind keys serializes/deserializes correctly
</verification>

<success_criteria>
- ContextBudget is a sealed class (not record) with constructor validation
- All 5 PIPE-02 properties are present and read-only
- Every invalid input path throws the appropriate exception
- JSON round-trip works including ReservedSlots with ContextKind dictionary keys
- [JsonConstructor] enables deserialization through the validated constructor
- Requirement PIPE-02 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/pending/01-project-scaffold-core-models/01-04-SUMMARY.md`
</output>
